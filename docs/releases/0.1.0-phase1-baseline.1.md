SF1 PHASE 1 LAUNCHER-READY BASELINE
===================================

- Rebuilds the selected c24ce313 SF1 native source line.
- Uses faithful first-run defaults: 640x480, 4:3, 20 Hz, and optional
  presentation/input enhancements off.
- Keeps retail-style controller input and remappable keyboard/mouse input.
- Keeps chase-camera mouse-look and all existing features available, but off by
  default.
- Requires the exact USA v1.1 SCUS-94240 retail executable identity.
- Uses no BIOS, packages no BIOS, and exposes no fast-boot bypass.
- Includes hash-bound build and package provenance plus launcher-readable policy
  metadata.

This package is promoted only as a launcher-ready baseline. It does not claim
campaign, mission, enhancement, full-game, or release readiness.
