# SF2 devlog: first playable bring-up

**Date:** 2026-07-28  
**Status:** Early native integration; not yet a playable campaign

Today *Syphon Filter 2* crossed from archive compatibility into recognizable,
controllable gameplay. This is still an engineering scene-test rather than a
finished port, but the disc is now providing the real mission geometry,
textures, actors, animation data, object records, and item definitions.

No proprietary game data was added to the repository. Everything described
below is decoded at runtime from a user-supplied NTSC-U BIN/CUE image.

## What now works

- Both SF2 discs are recognized from their executable profiles.
- All 21 SF2 campaign mission packages construct successfully:
  - Disc 1: 8/8
  - Disc 2: 13/13
- Mission resources, titles, room tables, actors, objects, animations, texture
  banks, and initial player locations are mapped.
- Native high-resolution rendering, PGXP, widescreen presentation, and high
  refresh rates run with SF2 mission data.
- The modern keyboard movement contract and horizontal/vertical mouse-look are
  active in chase and first-person aiming views.
- Gabe and Lian can move, run, strafe, roll, crouch, aim, fire, reload, and
  interact through the shared native controller.
- Native gunfire raycasts against actors and the world, including impact
  decals.
- SF2/SF3's eight-direction movement semantics are translated to WASD.
- Flagged sequel object classes are decoded by their base family. For example,
  `0x5000e` is handled as a door and `0x20034` as an exit-family object.
- Closed sequel doors block movement; interacting opens linked door halves
  together.
- Lockers can swap to their authored open model, and switches can operate their
  linked source.
- SF2 NPC and pickup item numbers are translated through the sequel item table
  instead of being incorrectly cast as SF1 inventory slots.
- Native campaign carry snapshots can now preserve sequel inventory,
  ammunition, health, and armor.

## Mission 2: McKenzie Airbase Interior

AIRBASE was the first useful grounded traversal test. Lian now starts at the
authored source and can leave the opening room, enter the hallway, and roam
through multiple rooms. This was confirmed interactively.

The original failure was not a closed door. SF1's taller collision capsule and
an XZ-only wall-edge test turned below-floor and overhead doorway edges into
full-height barriers. SF2/SF3 now use the narrower sequel capsule, require
vertical overlap, and test a quad's perimeter rather than its internal
diagonal.

AIRBASE also now has the beginning of its mission state:

- Lian starts without a weapon or armor.
- The two-minute adrenaline countdown is displayed.
- The authored adrenaline source stops the countdown and restores health.
- The linked combat-gear chain grants the .45, armor, and hand taser.

At the end of the original 2026-07-28 session, low starting health and the
complete objective/parameter state were still deferred. The continuation below
adds a documented reversible approximation and the first native state.

## Mission 3: Colorado Interstate 70

I-70 renders recognizably and Gabe starts at the correct authored location.
The level now gives him the knife seen in the original opening rather than the
SF1 default pistol loadout.

The knife is an explicit sequel item using SF1's otherwise unused inventory
slot 3. Its recovered executable record supplies:

- `KNIFE.GMD`
- 100 close-range damage
- zero ranged damage
- no ammunition

The model loads successfully and native knife attacks use a short melee trace
without a muzzle flash or wall bullet decal.

I-70 is not wired correctly as an encounter yet. In the original mission,
Chance and the allied soldiers occupy the enemy's attention while Gabe begins
behind a guard, performs the opening knife takedown, and takes his weapon.
Our actors currently enter generic native AI instead of that authored
distraction sequence. Reconstructing this opening encounter is the next
mission-specific gameplay boundary.

The authored supply sources are already decoded as the Glock 17, M16, shotgun,
armor, and fragmentation grenade.

## Data-format findings

Several apparent rendering failures were format deltas rather than corrupt
assets:

- Only EMD flag bit 4 selects `VRAM1.HOG`; the rest of the high nibble contains
  unrelated scene flags.
- SF1/SF2 EMD transformed-vertex offsets use three-byte units; SF3 uses
  two-byte units.
- SF1/SF2 non-flat HMD transformed vertices use twelve-byte units, while SF3
  uses eight.
- Empty sequel GMD/HMD parts may retain the retail inverted-bounds sentinel.
  It is accepted only when the corresponding part has no vertices.
- AIRBASE begins in visibility state 2 while standing on an always-resident
  connector model. Resident geometry must not take ownership of the current
  portal state.

These corrections also allowed all 19 currently mapped SF3 campaign packages
to construct, although SF3 gameplay remains later work.

## Known gaps

- SF2 overlay scripts and shared mission objectives are not executing yet.
- I-70's scripted allied distraction and opening takedown setup are absent.
- AIRBASE objective completion and its final escape transition are incomplete.
- Mission exits/portal events need their shared sequel semantics.
- Mission audio, speech, music cues, and ambient playback are not connected.
- Some allies lack their authored behavior and remain idle.
- Camera collision still needs validation in steep outdoor terrain.
- Later missions currently depend on provisional defaults unless entered
  through a valid campaign carry.
- H11 and crossbow inventory/presentation are not yet implemented; their item
  records remain intentionally unmapped instead of becoming the wrong weapon.
- This work has not received a full campaign playthrough.

## Validation

The Release build completes successfully, `git diff --check` is clean, and all
21 automated tests pass. Offline probes additionally confirm:

- AIRBASE: no weapon, no armor, 120-second timer, correct starting room.
- I-70: knife selected, `KNIFE.GMD` loaded, correct starting room.
- Every SF2 and SF3 mapped mission constructs through scene, object, actor,
  animation, texture, and VRAM inspection.

The scene-test mission argument is one-based:

```powershell
# Mission 2: McKenzie Airbase Interior
& ".\syphon_filter.exe" --no-launcher --mission=2 --scene-test "X:\path\to\Syphon Filter 2 (USA) (Disc 1).cue"

# Mission 3: Colorado Interstate 70
& ".\syphon_filter.exe" --no-launcher --mission=3 --scene-test "X:\path\to\Syphon Filter 2 (USA) (Disc 1).cue"
```

## Next session

Resume at the I-70 opening encounter:

1. identify the authored ally/enemy links and initial combat targets;
2. keep the relevant enemies focused on Chance's group;
3. preserve the guard positioned for Gabe's knife takedown;
4. verify the dead guard drops the correctly translated weapon;
5. connect that encounter to the existing pickup and inventory layer; and
6. then return to AIRBASE objective/exit flow and sequel audio.

---

## Autonomous continuation — 2026-07-29

The Mission 3 opening milestone is now implemented as a native, data-anchored
reconstruction and covered by a deterministic disc probe.

### Mission 3: authored opening encounter

Inspection of `HWAY.BIN` separated the opening roles:

- Chance is source 45 and carries an M16.
- Gabe is source 159.
- source 197 begins facing away from Gabe and carries an M16; it is the
  knife-takedown guard.
- hostile sources 278, 279, and 280 have five-point routes which converge on
  Chance. Sources 278/279 carry M16s and source 280 carries a Glock 17.

Sources 278–280 now retain Chance as their opening target even though Gabe is
closer. Chance reciprocally engages source 278, then falls back to the shared
target selector as actors die. The forced exchange ends for an enemy when Gabe
damages it. Until then, damage from that forced group cannot kill Chance; this
prevents generic native accuracy from turning the authored distraction into a
hidden countdown before the player can perform the takedown.

The knife now has contextual rear damage. Its recovered item record deals 100
close damage, while the authored guard has 200 health. A rear strike doubles
that damage to 200; ordinary knife attacks remain 100. The resulting death
creates a translated M16 pickup at the guard's location. Native sequel weapon
drops are rendered through the existing retail pickup-icon presentation and
can be collected into the inventory.

The offline probe performs an actual rear knife input rather than directly
editing the guard's health. It confirms:

- Gabe's authored spawn is behind source 197;
- the guard carries weapon slot 9 (M16);
- all three diversion actors select Chance and move substantially closer;
- Chance enters combat and damages one of them;
- Chance survives while the authored diversion remains forced;
- the rear knife input kills the 200-health guard;
- exactly one M16 pickup appears; and
- the next pickup update equips the M16.

The opening parachute presentation is not implemented. The native scene starts
at the authored grounded player record behind the guard.

### Shared sequel combat and inventory

- The knife uses the short NPC melee range as well as the short player trace.
- Native SF2/SF3 armed hostile deaths create translated weapon pickups.
- Electrical and fire deaths select their corresponding actor death channels.
- Electrical takedowns do not violate AIRBASE's no-lethal-force parameter.
- Dropped weapons currently grant their standard full magazine and reserve
  loadout. Remaining ammunition is not yet recovered from the defeated actor.

### Mission 2: AIRBASE progression

The pause menu now exposes the three mission objectives and parameters:

Objectives:

1. Find Adrenaline Booster
2. Get Combat Gear
3. Escape Holding Facility

Parameters:

1. Get adrenaline before you black out in 2 minutes
2. Do not kill any airbase personnel
3. Do not allow yourself to be detected

The corresponding native state now:

- fails the timer parameter when the two-minute countdown expires;
- completes the adrenaline objective and stops the timer at source 103;
- completes the combat-gear objective after sources 97–100 are collected;
- fails on hostile line-of-sight detection;
- fails the lethal-force parameter when airbase personnel are killed by
  non-electrical damage;
- lets source 104 operate its BIN-linked glass pair;
- gives overlay-scripted switch source 105 the missing operation on paired
  security doors 31/32; and
- completes the escape objective at source 24, the third class-`0x4c` event
  volume at the documented end corridor.

Lian now begins at 15/150 health, the repeatedly documented ten-percent retail
ratio. This is functional progression, not a claim that the AIRBASE overlay
has been reimplemented. The Phagan scene, conversations, patrol choreography,
objective notifications, checkpoint presentation, and final movie transition
remain absent.

### Deterministic probe

`sf_sf2_native_mission_probe` was added behind `SF_BUILD_ROM_PROBES`. Against
the NTSC-U Disc 1 image it reports:

```text
mission=2 source-index=1 initial-state=1 adrenaline-complete=1 gear-complete=1 security-doors-open=1 escape-complete=1 electrical-nonlethal=1 lethal-parameter-failed=1 timer-parameter-failed=1
mission=3 source-index=2 player-behind-guard=1 guard-source=197 guard-weapon=9 diversion-configured=1 chance-health-before=250 chance-health-after=250 chance-behavior=7 chance-damaged=0 camera-segment-clear=1
source=278 active=1 health=50 behavior=6 position=2588.32,24.5996,-25290.3 chance-distance-before=4557.24 chance-distance-after=2740.54
source=279 active=1 health=200 behavior=7 position=2221.17,17.9003,-25869.5 chance-distance-before=3914.57 chance-distance-after=2058.91
source=280 active=1 health=200 behavior=7 position=1897.49,-28.785,-26215.4 chance-distance-before=3806.04 chance-distance-after=1615.13
placed-for-takedown=1 takedown-lethal=1 dropped-items=1 m16-dropped=1
m16-collected=1
```

The camera line test confirms the stationary opening camera does not cross
authored world geometry. It does not clear the reported clipping while moving
through steep terrain; that remains an interactive test.

### Assumptions and reversible approximations

- source 197 is the takedown guard based on its authored facing relative to
  Gabe, its route, and its M16. The tiny overlay does not expose that role as a
  direct source-index constant.
- The rear threshold is the back half-plane (90 degrees or farther from the
  target's forward direction). The 2x multiplier is inferred from the recovered
  100-damage knife record and the authored guard's 200 health; the exact retail
  branch has not yet been disassembled.
- The opening exchange treats the authored target as visible within 5200 world
  units. This compensates for currently incomplete outdoor scene/vertical
  reconstruction blocking native line-of-sight between actors whose retail
  routes explicitly converge.
- Chance is protected only from the still-forced diversion actors. Normal
  damage resumes after Gabe engages them. This is a conservative substitute
  for the missing mission parameter/script boundary.
- AIRBASE source 105 opening doors 31/32 is based on spatial records and the
  documented post-locker route; unlike source 104, source 105 has no direct BIN
  link.
- AIRBASE source 24 is treated as the finale trigger because it is the third
  ordered event volume and lies at the documented end corridor. The final
  overlay/movie handoff is not running.
- AIRBASE's 10% starting-health ratio is corroborated by retail playthrough
  descriptions but has not been recovered as an overlay constant.
- The objective/parameter wording is corroborated by retail-era walkthrough
  and briefing transcriptions. The localized `SLF.RFF` table has not yet been
  decoded into a reusable sequel text loader.

### Audio and script-runtime blocker

The early inspection correctly established that `AIRBASE.SS` is compiled
stateful data rather than PCM, but its scope was initially described too
narrowly. Later executable analysis proves that `.SS` is the shared compiled
mission-program format: the named programs own objectives, actor
choreography, doors, dialogue, timers, and progression as well as sound cues.
Native sequel sessions still have no owner for the complete retail
XA/CD-ROM/SPU timeline, while the existing exact audio path is owned by the SF1
guest runtime. Connecting mission audio safely requires the shared `.SS`
runtime bindings plus sequel XA/SPU support. No placeholder music or guessed
sound mapping was added.

### Validation

- Complete Release build: passed.
- Complete CTest suite: 21/21 passed.
- `sf_combat_ai_tests`: passed, including the rear-melee boundary.
- `sf_sf2_native_mission_probe`: passed.
- SF1 `sf_g3_ai_combat_probe` mission 0: passed.
- SF1 `sf_g3_gameplay_probe` mission 0: passed, including replay,
  objective/failure/success, dynamic-lifetime, and actor-presentation gates.
- `git diff --check`: passed.
- No commit, push, release, external message, proprietary asset, or Redux
  change was made.

### Exact one-based launch commands

```powershell
# Mission 2: McKenzie Airbase Interior
& "D:\Temp\sf-pc-port-sanitized-build\Release\syphon_filter.exe" --no-launcher --mission=2 --scene-test "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 1).cue"

# Mission 3: Colorado Interstate 70
& "D:\Temp\sf-pc-port-sanitized-build\Release\syphon_filter.exe" --no-launcher --mission=3 --scene-test "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 1).cue"

# Deterministic Mission 2 + Mission 3 offline probe
& "D:\Temp\sf-pc-port-sanitized-build\Release\sf_sf2_native_mission_probe.exe" "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 1).cue"
```

### Remaining high-value gaps

1. Interactive validation and tuning of the Mission 3 encounter.
2. Mission 3 parachute opening and any dynamically spawned allied soldiers.
3. AIRBASE cinematic/event choreography, patrol timing, exact starting-health
   provenance, checkpoint, notifications, and final STR transition.
4. Exact sequel overlay/object-script execution rather than source-specific
   native bridges.
5. Remaining-ammo weapon drops and campaign transitions between SF2 missions.
6. Steep outdoor camera clipping, distant outdoor visibility, and sky/backdrop.
7. Shared sequel mission audio, music, speech, and ambience.

## Whole-game reverse-engineering continuation — 2026-07-29

The continuation deliberately moved beyond Missions 2 and 3. The goal was to
map reusable campaign-wide code and data boundaries across both SF2 discs and
SF3 before adding more mission-specific approximations.

### Whole-executable and call-flow maps

`sf_tool` now emits:

- 2,960 SF2 function seeds from either individual disc and a 2,964-seed
  whole-campaign union of both mission/resident overlay closures;
- 2,917 SF3 seeds;
- unique exact and normalized structural SF2↔SF3 function matches;
- seed provenance (`entry`, `direct_call`, `overlay`, or `prologue_only`);
- exact embedded-asset-range markers for function seeds and call sites.

The asset markers matter. SF2 has 329 seeds inside proven embedded
animation/texture/archive data, 323 of them discovered only as apparent
prologues. The cleaner executable baseline is 2,631 seeds. Function
coalescing also now folds a preceding linear compiler prefix into the
following stack prologue instead of reporting two false functions. The final
entry of each embedded archive remains an upper-bound range and is not
automatically excluded.

The two SF2 discs have byte-identical executables and resident overlays, but
their mission overlays contribute different extra entry points. The union
mapper verifies executable identity before combining those closures.

### Resident overlays and embedded archives

The resident runtime bases are now proven from internal calls, prologues, and
adjacent overlay extents:

- SF2 `MENU`/`MOVIE` at `0x80142150`, `TITLE` at `0x8014b950`, and `INIT`
  at `0x80158878`;
- SF3 `MENU`/`MENU2`/`MOVIE` at `0x80146950`, `TITLE`/`TITLE2` at
  `0x80150950`, and `INIT` at `0x8015e978`.

SF2 contributes 493 resident function seeds; SF3 contributes 691 across its
six variants. There are 257 unique structural resident matches, including 148
`INIT` pairs and 56 `MOVIE` pairs.

Five embedded HOG-like archives were catalogued in each sequel executable.
SF2 has 365 named entries: 166 animations, 87 UI/inventory textures, 97
weapon/effect textures, eight resident-package entries, and seven title
assets. SF3 has 398 entries in the same organization.

### Full mission-object corpus

The raw sequel object tail is preserved as four handler parameters plus one
handler-state halfword. Neutral names are intentional because meanings differ
by class.

The maps cover:

- SF2: 666 definitions and 3,791 placed objects across all 21 missions;
- SF3: 541 mapped definitions and 3,869 placed objects across all 19 missions;
- source index, class, models, room set, transform, attributes, AI parameter,
  path, link, health, and raw handler fields.

SF2 uses 121 class families. SF3 adds family `0x84` (`OBJ_VEH`), proving that
its object-handler table contains 133 entries rather than SF2's 132. The
mapper's previous SF3 bound was corrected.

AIRBASE sources 22/23/24 are paired with 25/26/27 as authored
class-`4c`/`4d` records. Source 24 is therefore not simply an arbitrary “third
volume.” Its native finale bridge remains provisional until the shared script
program is bound.

### `.SS` container and VM

The retail INIT loaders at SF2 `0x80164fdc` and SF3 `0x8016b348` load each
mission's `.SS`, relocate six program pointers, reset program state, locate
the program named `LEVEL`, and activate it.

The parsed retail corpus contains:

| Build | Programs | Event records | Reachable action/control words |
|---|---:|---:|---:|
| SF2 Disc 1 | 66 | 792 | 4,292 |
| SF2 Disc 2 | 161 | 2,325 | 11,158 |
| SF3 | 360 | 4,573 | 23,837 |

Confirmed program fields now include format byte, mutable-variable count,
first serialized variable (or `0xff`), timer count, event stream, variable
table, timer table, name pointer, and serialized blob size. Event records
carry an event ID, selector/filter, length, and action stream. Event `0`
decrements timers and dispatches event `8` on expiry.

The native parser now retains the initial variable and signed timer
halfwords, validates both tables against each program's serialized extent,
and passed all 227 SF2 plus 360 SF3 retail programs. This is reusable VM state,
not an AIRBASE/HWAY approximation.

The core SF2 VM is mapped from predicate decode at `0x800b2b08` through
program event dispatch at `0x800b36a0`, active-list iteration, name lookup,
activation/deactivation, save-state serialization, restore, and `LEVEL`
startup. Exact/aligned SF3 counterparts are recorded for every core entry.

The VM has:

- 64 predicate descriptors (55 populated in SF2, 59 in SF3);
- 128 action descriptors, all populated in both games;
- 118 action slots used by the SF2 retail corpus;
- 108 action slots used by SF3;
- fourteen used only by SF2 and four used only by SF3.

The action walker follows both predicate outcomes, deduplicates loops, and
validates every reachable target against its enclosing event record. It does
not execute handler side effects. No guessed native interpreter was added.

### Shared subsystem anchors

The reusable map now covers:

- application state and mission scheduling;
- `.SS` mission programs and general object events;
- actor/player handlers and actor-event paths;
- pickups, dropped items, held models, and inventory integration;
- world collision and player-frame boundaries;
- scene-XA archive selection, positioned sound, and cue dispatch;
- SF3 object/script/geometry deltas.

The detailed maps are:

- `docs/SF2_EXECUTABLE_MAP.md`
- `docs/SF2_SHARED_SYSTEM_MAP.md`
- `docs/SF2_MISSION_SCRIPT_VM.md`
- `docs/research/sf2-function-symbols.csv`
- `docs/research/sf2-function-catalog.csv`

### Confirmed non-findings and blockers

- `AIRBASE.OVL` contains no ordinary objective/timer/exit/door progression.
  It only registers a small special-object callback. The missing authored
  behavior is in `AIRBASE.SS` and shared handlers.
- Camera-adjacent actor render/collision preparation maps provisionally to
  SF2 `0x80049940` / SF3 `0x8004ac88` through a unique two-helper call graph.
  Full caller disassembly rejects the tempting chase-camera label. The exact
  chase update still needs a guest write probe; steep outdoor clipping must
  not be changed from this address match.
- Retail reserve/loaded writes, capacity clamping, and reload transfer are now
  named by direct data flow. The remaining gap is binding those shared retail
  rules to every native sequel item, especially unsupported H11/crossbow
  presentation.
- Audio has three separate unfinished native layers: SPU/VAB positional
  effects, `.SS` cue state, and streamed scene XA.
- Function hashes are analysis seeds, not complete control-flow recovery.
  Indirect handlers are recovered through retail descriptor/object tables.

### Assumptions

- Field and function names are descriptive clean-room names.
- Structural matches establish a shared boundary, not identical side effects.
- Unknown `.SS` pointers and descriptor metadata remain neutral.
- Native AIRBASE and HWAY bridges remain reversible bring-up code until shared
  retail programs produce the same deterministic results.
- No behavior is claimed from action-table alignment alone; handler effects
  require disassembly or a deterministic guest probe.

### Whole-game validation

- Complete Release build, including ROM probes: passed.
- Complete CTest suite: 21/21 passed.
- Positive and malformed `.SS` container/event tests: passed.
- All 21 SF2 and all 19 SF3 mission archives: passed native archive,
  layout, object, and virtual-CD construction.
- All 40 sequel missions: passed complete native `MissionPackage` and
  `GameplaySession` construction.
- All 3,117 SF2 and 4,573 SF3 event records: parsed and bounded.
- All 15,450 SF2 and 23,837 SF3 reachable action/control words: control-flow
  targets stayed inside their authored event records.
- Mission-script string inventories: 66 strings on SF2 Disc 1, 203 on Disc 2,
  and 411 in SF3. These are generic `.SS` strings, not an audio-only format.
- Descriptor-handler call maps: 88 descriptor rows / 82 unique physical calls
  from 67 SF2 handlers, and 95 rows / 89 physical calls from 68 SF3 handlers.
  Program activation (`0x0b`) and deactivation (`0x0c`) now have confirmed
  native handler boundaries.
- Campaign-shell boundaries are now named: application-state push/pop,
  gameplay and loading state targets, and campaign advance/complete. SF2
  advances mission indices below 20 before its final branch; SF3 preserves
  the role with a below-18 test and separate final-mission handling.
- The pickup path is split into confirmed reusable stages: packed item decode,
  quantity resolution, a bounded ten-entry per-actor pickup queue, queue
  removal, and world-list detachment. Earlier candidate functions are now
  correctly classified as pickup presentation/queue processing rather than
  unproven magazine or reserve-ammunition writes.
- Continued queue-consumer disassembly then located the actual inventory
  core: ownership-bit count/acquire/remove/replace and clamped per-item
  quantity writers at `0x80062ff4`–`0x800634e0`, with structural SF3 matches.
  Retail reload is now confirmed at `0x800642f4` (SF3 `0x80066738`): it
  transfers `min(reserve, magazine capacity - loaded)` from the first item
  halfword into the second and refreshes the equipped-weapon display.
- Program-table operands for action `0x0b`/`0x0c` are decoded back to names.
  The full SF2 corpus has 255 activation and 106 deactivation instructions
  (321 unique named relationships); SF3 has 390/205 (527 unique). AIRBASE and
  HWAY now have reproducible authored program handoff graphs instead of
  mission-specific guesses.
- Four more shared action boundaries are mapped: actor scripted activation
  (`0x1b`, strong), night-vision state (`0x31`, strong), fixed object event 1
  (`0x68`, confirmed), and general object/event dispatch (`0x75`, confirmed).
- The shared mutable-state language is now mapped rather than inferred from
  Mission 2/3 behavior: program-variable assign/increment/decrement/add/
  subtract/result/random operations, object-health reads, package-bit
  set/clear, and ten object-state flag operations have direct SF2/SF3
  boundaries. The package bitset is saved and restored, but its higher-level
  retail noun remains intentionally neutral.
- Mission overlay integration is now more precise: adjacent callback setters
  at `0x80089298`/`0x800892a8` are respectively the actor-damage and
  actor-death hooks, proven by their shared call sites; SF3 preserves them at
  `0x8008b8d4`/`0x8008b8e4`. AIRBASE registers neither, reinforcing that its
  authored actor choreography belongs to `.SS`.
- Scripted scene speech now has direct shared boundaries: actions `0x16`,
  `0x17`, `0x77`, and `0x78` start source/mode variants; action `0x6a`
  stops the stream, schedules completion, and resets its active state.
- The camera search found SF2 `0x80049940` / SF3 `0x8004ac88` through a
  unique two-helper relationship, then correctly rejected it as the chase
  controller after caller/field disassembly showed actor render/attachment
  preparation. The narrower provisional boundary is retained as a useful
  negative result.
- SF2 Disc 1/Disc 2 opcode descriptors: identical.
- SF2 whole-campaign function union: 2,964 seeds after executable identity
  verification.
- SF2↔SF3 unique function comparison: 279 byte-exact plus 1,832 structural
  matches (2,111 total).
- SF2 Mission 2/3 native probe: passed adrenaline/gear/doors/escape,
  electrical/lethal/timer parameters, diversion, rear takedown, M16 drop, and
  collection.
- SF1 AI/combat and gameplay probes, mission 1: passed.
- SF2 executable-entry probes on both discs: returned normally after 486,150
  guest instructions and produced identical register/CD/DMA state.
- `git diff --check`: passed.
- No commit, push, release, external message, proprietary asset, or Redux
  change was made.

### Broad-map continuation after the Mission 3 milestone

Work continued across the retail runtime rather than stopping at AIRBASE and
HWAY:

- Mission-overlay address recovery was corrected for the 40-byte overlay
  header. The emitted load address and first executable address are now
  distinct. Regenerated maps contain 52 Disc 1, 34 Disc 2, and 26 SF3
  overlay-derived function seeds.
- The mission-script parser now retains each program's exact serialized
  bytes and every event's encoded action/control halfwords. This makes a
  future interpreter and deterministic reserialization possible without
  returning to proprietary disc extraction or guessing operands.
- All 55 populated SF2 predicate slots are recorded in
  `docs/research/sf2-script-predicates.csv`, including their storage-level
  tests and the side-effecting descriptor aliases. SF3's four additional
  predicates (`0x27`–`0x2a`) are mapped separately rather than forced into
  the SF2 ABI.
- The complete actor event table (`2`–`40`) is recorded in
  `docs/research/sf2-actor-events.csv`. Nine slots have native behavior and
  the remaining 30 share the no-op epilogue. The complete 132-family SF2
  object-handler table is recorded in
  `docs/research/sf2-object-class-handlers.csv`; mission-overlay ownership is
  explicit rather than inferred from a class name.
- A tempting player-frame label was rejected after full disassembly:
  `0x80066b54`/`0x80066be0` are render-view queue add/remove operations, not
  player or chase-camera updates. This negative result prevents camera
  clipping work from patching the wrong subsystem.
- Script action `0x32` is the exact authored inventory-grant boundary. It
  decodes item and quantity operands, applies the retail item-table quantity
  multiplier, and calls `InventoryItem_Acquire`. Action `0x55` dispatches
  current-player events `0x81`/`0x82`; action `0x70` starts a full-screen
  application transition. These names come from direct argument/data flow,
  not table position.
- Campaign completion is now traced through
  `CampaignProgress_CommitMission` and `PlayerCarry_Snapshot`. SF2 carries
  health, armor, equipped item, two ownership words, and 34 reserve/loaded
  pairs; SF3 carries 40. The mission commit also updates completion/unlock
  state and conditionally copies the mission's 38-byte progress record.
- Native sequel enemy drops now preserve the defeated actor's live magazine
  and reserve. Picking up an already-owned weapon adds and clamps its
  quantities instead of replacing Gabe's current ammunition. The deterministic
  HWAY probe verifies that source 197's exact M16 quantity survives both the
  drop and collection.
- `sf_tool map-xa-streams` maps physical Mode-2 Form-2 audio sectors without
  extracting or redistributing audio. Disc 1 has 52 synchronized groups
  (780 playable channel clips), Disc 2 has 74 (1,110), and SF3 has 76
  (1,140). All use file 1, mono 37.8-kHz ADPCM, playable channels 0–14, and
  filler channel 16.
- The low-level XA cue formula is exact:
  `group = cue / 15`, `channel = cue % 15`; the group LBA comes from the
  archive base plus its cumulative group-offset table. XA decoding, seek,
  filtering, and output are no longer reverse-engineering blockers. The
  remaining gap is executing the authored script action/event which requests
  each cue.
- `docs/research/sf2-mission-xa-cues.csv` maps all 534 reachable scene-speech
  action occurrences to cue/group/channel coverage for every mission. Disc 1
  contributes 191 occurrences / 148 distinct cues and Disc 2 contributes
  343 / 241. All are literal cue operands; no variable-indirected cue was
  assumed resolvable offline.
- A frequency-guided action-handler pass maps direct native effects for
  10,384 of 10,655 reachable ordinary SF2 action occurrences (97.5%).
  High-value recovered primitives include timer assignment, actor
  activate/deactivate/health/equipped-item state, broad actor flag families,
  player-to-object interaction, scene-speech volume, and the authored
  mission-success request. Storage-level names are retained where the retail
  source-language noun is unknown; no generic interpreter is claimed yet.
- The low-frequency action tail is now decoded into exact actor/object field
  writes, scheduler records, inventory-quantity reads, and auxiliary-resource
  lifecycle calls. Action `0x79` is a fully recovered seven-mode NPC/player
  resource lifecycle multiplexer. Action `0x29` is now a confirmed
  player/object interaction start: all 62 selectors are valid objects in their
  own mission, modes 0/1 queue distinct player events, and its completion
  callback schedules event 5 back to the selected object. The designer-facing
  names of its three modes remain intentionally neutral.
- COLO's missing sky is localized without inventing a render fix. Native
  sessions parse `SCRIM.EMD` and the renderer has a correct semantic
  background pass, but only the live guest presentation currently publishes
  the authored SCRIM matrix/visibility, mutable page cycle, clear color, fog,
  and depth cue. Native sequel environment-state production is the missing
  boundary; identity-transform SCRIM rendering would be a guess.
- The action decoder itself is now mapped at the bytecode-ABI level. Operand
  0 uses inline-7 or extended-16 encoding; operand 1 uses extended-16 except
  for the proven extended-primary/inline-6 form. Program/parent-variable,
  result-register, all-class-2, matching-class-5, and sequential-variable
  selector expansions are distinguished. The mapper now emits both operands
  and their exact serialized sources instead of losing operand 1.
- Decoder-level control flow corrected action `0x28`: its descriptor target is
  a no-op marker, but the decoder recognizes that exact pointer and
  redispatches event 11 to the current program with the decoded selector. It
  is therefore not a semantic no-op.

The Release unit suite and the AIRBASE/HWAY deterministic native probe pass
after the remaining-ammo implementation. The HWAY output includes
`quantity-preserved=1`.

### 2026-07-29 autonomous whole-campaign handoff

This continuation deliberately did not stop after the Mission 3 encounter.
The reusable campaign-facing result is:

- 2,964 conservative SF2 campaign function seeds, with 179 compact named
  SF2/SF3 boundaries, a checked-in full source-only union catalog, and
  reproducible call/hash maps;
- corrected function discovery around compiler pre-prologues and embedded
  asset ranges;
- exact resident and per-mission overlay address/ownership maps;
- all 227 SF2 `.SS` programs, 3,117 event records, and 15,450 reachable
  action/control words parsed across both discs;
- complete predicate, actor-event, and object-class dispatch inventories;
- a checked-in 128-slot SF2/SF3 action descriptor crosswalk with handler,
  metadata, and whole-corpus occurrence counts;
- a checked-in 38-ID `.SS` event inventory covering all 3,117 SF2 records,
  plus the confirmed scheduler callback that decodes packed event/selector
  words and fans them into every active program;
- 97.5% occurrence coverage for ordinary SF2 action effects, plus the exact
  descriptor-driven operand and selector-expansion ABI;
- exact storage contracts for the remaining low-frequency actions: indexed
  object-byte assignment, linked handler-state clear, retail approximate
  object distance, and indexed orientation-scalar assignment;
- exact inventory ownership, quantity-pair, pickup, reload, drop, carry, and
  mission-commit boundaries;
- exact XA physical stream topology, cue arithmetic, and all 534 reachable
  authored scene-speech requests;
- explicit SF3 deltas for functions, script descriptors, predicates, items,
  carry width, overlays, object families, and XA;
- a native Mission 3 opening slice with diversion actors, rear takedown,
  correct translated M16 drop, live remaining ammunition, collection, and
  reload-compatible inventory state.

The broad validation pass completed with:

- complete Release build: passed;
- CTest: 21/21 passed;
- all 21 SF2 packages, split correctly across both discs: reconstructed;
- all 19 SF3 packages: reconstructed;
- SF2 Disc 1 and Disc 2 executable-entry probes: identical normal return
  after 486,150 guest instructions each;
- SF2 AIRBASE/HWAY deterministic probe: passed, including
  `camera-segment-clear=1`, `takedown-lethal=1`, `m16-dropped=1`,
  `quantity-preserved=1`, and `m16-collected=1`;
- SF1 Mission 1 gameplay and AI/combat probes: passed;
- all 15,450 SF2 and 23,837 SF3 reachable action/control words remapped after
  the two-operand mapper change without a control-flow or bounds failure;
- `git diff --check`: passed;
- untracked-file sanitation scan: no disc images, executables, archives,
  movies, audio, textures, models, overlays, or extracted scripts.

No commit, push, release, external message, proprietary asset, or Redux
change was made.

#### Confirmed remaining gaps

1. The generic native `.SS` interpreter is not yet connected to
   `GameplaySession`. The parser and ABI map are ready, but a large campaign
   subset still depends on mission-specific native scaffolding.
2. The designer-facing names of action `0x29`'s three player/object
   interaction modes remain unknown, although their object targeting,
   player-event, and completion-event behavior is now implementation-grade.
3. Native sequel environment production is absent. `SCRIM.EMD` is parsed and
   the renderer can draw it, but only the guest bridge currently publishes
   its authored transform, visibility, mutable page cycle, clear color, fog,
   and depth cue.
4. The exact sequel chase-camera owner is still unnamed. Native segment
   collision and near-plane clipping exist, but steep COLO framing needs
   visual validation rather than another guessed camera patch.
5. XA decoding and cue resolution are complete, but native `.SS` speech,
   music, VAB effects, and ambient requests are not dispatched through the
   mission runtime.
6. Campaign commit/carry is mapped but native mission success does not yet
   run the complete retail transition shell.
7. AIRBASE objectives, switches, timer consequences, and pickups are only
   partially reconstructed. Mission 2's open starting door is confirmed;
   that is not evidence that the full mission is finishable.
8. Unsupported sequel item/model entries, especially H11/crossbow-family
   presentation, remain explicit rather than being aliased to an incorrect
   SF1 asset.

#### Assumptions and risk controls

- CLI mission numbers are one-based; executable/package indices and research
  CSVs are zero-based.
- Storage-level action names describe observed writes and calls. They are not
  claimed original symbols or inferred designer terminology.
- Mission 3's native opening is a conservative authored-data reconstruction,
  not proof that the generic retail script executed.
- No identity-transform or camera-relative SCRIM fallback was added because
  neither transform is supported by retail evidence.
- Remaining-ammo collection is additive and clamped, matching the recovered
  retail quantity core. `grant` retains replace semantics for deterministic
  loadout construction.

### Prioritized morning gameplay tests

```powershell
$exe = "D:\Temp\sf-pc-port-sanitized-build\Release\syphon_filter.exe"
$sf2d1 = "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 1).cue"
$sf2d2 = "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 2).cue"
$sf1 = "Z:\Emulators\PS1 Games\Syphon Filter (USA) (Rev 1).cue"
$sf3 = "Z:\Emulators\PS1 Games\Syphon Filter 3 (USA).cue"

# SF2 Mission 3 — I-70 opening encounter
& $exe --no-launcher --mission=3 --scene-test $sf2d1

# SF2 Mission 2 — AIRBASE movement, doors, timer, pickups
& $exe --no-launcher --mission=2 --scene-test $sf2d1

# SF2 Mission 1 — COLO parachute/outdoor rendering and camera
& $exe --no-launcher --mission=1 --scene-test $sf2d1

# SF2 Mission 9 — first Disc 2 mission smoke test
& $exe --no-launcher --mission=9 --scene-test $sf2d2

# SF2 Mission 17 — script-heavy Bio-Lab smoke test
& $exe --no-launcher --mission=17 --scene-test $sf2d2

# SF2 Mission 21 — final mission/boss package smoke test
& $exe --no-launcher --mission=21 --scene-test $sf2d2

# SF3 Mission 7 — shared sequel-system compatibility smoke test
& $exe --no-launcher --mission=7 --scene-test $sf3

# SF1 Mission 1 — regression check
& $exe --no-launcher --mission=1 --scene-test $sf1
```

1. **Mission 3 / Disc 1 — authored opening.** Do not fire immediately. Watch
   whether Chance and all three allied/diversion actors engage the intended
   enemies while the knife guard remains exposed. Rear-knife source 197,
   collect exactly one M16, note its loaded/reserve ammunition, empty the
   magazine, and reload. Expected: lethal takedown, one M16, preserved nonzero
   ammunition, successful collection, firing, and reload; Chance should not
   take damage during the deterministic opening window.
2. **Mission 2 / Disc 1 — AIRBASE shell.** Confirm Lian starts unarmed, at low
   health, with a 120-second timer; walk through the already-open starting
   door; test the first switches, security doors, pickups, shooting, and
   reload. Expected: the doorway is physically passable and basic actors,
   collision, and weapon interaction remain stable. Full objectives and
   completion are still expected gaps.
3. **Mission 1 / Disc 1 — COLO rendering/camera.** Inspect the opening spawn,
   rotate on steep slopes, move the camera against terrain, enter/leave first
   person, and capture one screenshot facing the horizon. Expected: no
   behind-camera polygon spikes or crash. Black/missing authored sky and
   incomplete parachute choreography are known failures to report, not pass
   conditions.
4. **Mission 9 / Disc 2 — first second-disc smoke test.** Verify the Pharcom
   package, spawn, HUD, movement, mouselook, collision, firing, bullet impact,
   and reload. Record the first inert door, actor, objective, audio request,
   or visibility transition.
5. **Mission 17 / Disc 2 — script-heavy Bio-Lab.** Repeat the same checks and
   exercise the first interaction/pickup. This is the best high-density test
   for the shared VM work once execution is connected.
6. **Mission 21 / Disc 2 — final campaign package.** Verify stable load, Chance
   model/spawn, HUD, movement, combat, and camera. Completion/boss scripting is
   not yet expected.
7. **SF3 Mission 7.** Verify that sequel parsing, controls, textures, collision,
   and HUD did not regress. No SF3-specific mission behavior is claimed.
8. **SF1 Mission 1.** Verify the existing mouse-look, WASD/strafe/lean,
   first-person firing, reload, HUD, audio, camera, and mission progression
   still behave as before.

## 2026-07-29 gameplay-feedback follow-up

The first broad user playtest confirmed that all requested SF2 packages
launch, but exposed three shared renderer/data assumptions and two authored
mission gaps:

- Missions 1, 3, and 9 exhausted the pickup-sprite primitive packet budget
  when native combat created a weapon drop. The budget counted only
  guest-presentation drops even though rendering merges those with
  `GameplaySession::nativeDroppedItems()`.
- AIRBASE failed immediately because sources 84, 85, and 86 were initialized
  live. The retail `LEVEL` and `COME_WITH_ME_DOCTOR` programs activate those
  guards later with action `0x06`; source 84 consequently appeared outside
  Lian's starting room before its authored activation and triggered the
  already-working stealth check.
- CHINBOSS uses BOMB polygon selector `0x9f`, physical page 31, without
  declaring that page in its VLF mask. Page 31 has no logical-page alias, so
  rejecting it as ambiguous was incorrect.
- SF3 moved `WEPTOP.TIM` from page 13 to page 28, relocated five weapon HUD
  layers into lower streamed pages, left two more crossing the restored HUD
  CLUT rows, and uses both `0x84000000` and `0x80000000` TIM metadata forms.
- COLO still lacks its parachute opening, and BIO-LAB still lacks the script
  progression needed to leave its initial gameplay state. These are
  confirmed feature gaps, not regressions fixed in this follow-up.

### Implemented

1. Pickup primitive storage now reserves the icon-layer count for both
   presentation drops and native drops before the ordering table receives
   any primitive pointers. This addresses the common Missions 1/3/9 packet
   exhaustion without increasing an arbitrary global limit.
2. AIRBASE sources 84, 85, and 86 now begin inactive and script-hidden. A
   deterministic probe verifies all three delayed states. The existing
   parameter-failure probe explicitly activates its isolated test guard so
   the two behaviors remain independently covered.
3. EMD texture selection now accepts an unaliased direct physical page when
   neither VLF bank claims the selector. It continues to reject selectors
   for which direct and shifted logical ownership are genuinely ambiguous.
4. The retail weapon-crate overlay now obtains and streams each of its three
   authored physical pages rather than requiring the SF1/SF2 `12/13/13`
   arrangement.
5. Seven exact SF3 HUD rectangles are relocated into verified unused
   resident-atlas space: `GASGRENA`, `SHOT2A`, `SHOT2B`, `SHOT2C`, `SNIFFER`,
   `SNIPER1C`, and `G3A`. Unknown layouts remain rejected.
6. TIM parsing now accepts observed subsets of SF3 metadata mask
   `0x84000000`; the decoded pixel mode, palette contract, and payload
   validation are unchanged. TIM errors also report their raw flag value,
   and INTERFACE parsing reports the responsible asset name.
7. The first interactive Mission 3 retest exposed three SF2 icon layers that
   cross the resident CLUT rows: `KEYCARDB`, `SNIPER1C`, and `SUPER1A`.
   Their exact retail rectangles now relocate into the verified free
   `x=170..211, y=0..24` atlas region, after reserving the optional localized
   font sheets. Mission 3 subsequently submitted frames for 12 seconds
   without another HUD-layout rejection.
8. The same retest confirmed the knife takedown, visible translated M16 drop,
   and collection in live play. It also showed that native sequel
   first-person aim inherited the SF1 host's 50-unit vertical offset despite
   SF2/SF3 using a grounded player root. Sequel manual aim now uses the
   existing 220-unit third-person weapon-ray height, placing both camera and
   shot ray on the upper-chest/muzzle line while leaving SF1 unchanged.

### Runtime split initiated

The project no longer treats SF2/SF3 as an implicit "not SF1" gameplay
runtime. Explicit SF1, SF2, and SF3 profiles now own decode strides, player
collision, first-person height, guest/native runtime selection, mission-item
and interaction opt-in, HUD-atlas identity, and environment-atlas ownership.
SF2 item translation and AIRBASE delayed actors moved under the SF2 module.
SF3 no longer inherits those systems without its own implementation.

The architectural rules and next extraction sequence are recorded in
`docs/GAME_RUNTIME_ARCHITECTURE.md`. The next functional slice is SF2 combat
parity in Mission 3, not further speculative SF3 generalization.

### Evidence and validation

- Complete Release build: passed.
- CTest: 21/21 passed.
- AIRBASE/HWAY deterministic probe: passed. AIRBASE reports
  `initial-state=1 delayed-guards=1`; Mission 3 reports
  `diversion-configured=1 takedown-lethal=1 m16-dropped=1
  quantity-preserved=1 m16-collected=1`.
- CHINBOSS Mission 21 hidden launch smoke: rendered for 12 seconds with
  `submitted=11035`; the prior ambiguous EMD selector error did not recur.
- SF3 Mission 7 hidden launch smoke: rendered room 43 for 12 seconds with
  `submitted=4652`; neither the weapon-crate error, HUD-layout errors, nor
  TIM-flag error recurred.
- SF1 Mission 1: user-confirmed working after the broad sequel changes.
- `git diff --check`: rerun before handoff.

The hidden launch smokes prove initialization and sustained frame submission,
not visual correctness or authored mission progression. The pickup-budget
fix is structurally covered by reserving the same two collections that the
renderer merges, and the native probe proves the drop is generated and
collected; an actual knife/combat retest is still required to confirm the
on-screen path.

### Remaining known gaps and risks

1. Mission 1 has no reconstructed parachute sequence and retains the known
   outdoor/background and camera limitations.
2. Mission 2's premature guard is removed, but the complete timer, guard
   activation choreography, objectives, switches, and exit sequence still
   depend on broader `.SS` runtime integration.
3. Mission 3's deterministic opening relationships exist, but live authored
   AI choreography and full mission continuation remain incomplete.
4. Mission 17 launches but has no implemented initial progression.
5. Mission 21 and SF3 Mission 7 are startup/render smoke passes only.
6. Missions 3 and 9 require user combat confirmation because the packet
   failure occurred only after an interactive kill.

### Next gameplay tests

All commands use one-based retail mission numbers:

```powershell
$exe = "D:\Temp\sf-pc-port-sanitized-build\Release\syphon_filter.exe"
$sf2d1 = "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 1).cue"
$sf2d2 = "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 2).cue"
$sf3 = "Z:\Emulators\PS1 Games\Syphon Filter 3 (USA).cue"

# Highest priority: native drop path and Mission 3 opening
& $exe --no-launcher --mission=3 --scene-test $sf2d1

# AIRBASE premature-guard regression
& $exe --no-launcher --mission=2 --scene-test $sf2d1

# Second combat/drop path
& $exe --no-launcher --mission=9 --scene-test $sf2d2

# Previously rejected texture layouts
& $exe --no-launcher --mission=21 --scene-test $sf2d2
& $exe --no-launcher --mission=7 --scene-test $sf3
```

1. In Mission 3, rear-knife the opening guard and collect the dropped M16.
   Expected: no packet-budget error, one visible M16 pickup, collection with
   live ammunition, firing, and reload. Also record whether the allied
   diversion actually runs in live play; only its deterministic
   configuration is currently proven.
2. In Mission 2, wait near the starting door before moving into the hallway.
   Expected: no floating guard outside the door and no immediate stealth
   failure. Later authored guard activation and full progression are not yet
   claimed.
3. In Mission 9, kill the first armed enemy and inspect/collect the drop.
   Expected: no packet-budget error and stable continued rendering.
4. In Mission 21, rotate the camera around the first visible BOMB model and
   enter combat if available. Expected: no ambiguous source-page error;
   visual texture correctness still needs confirmation.
5. In SF3 Mission 7, inspect the HUD and the first weapon crate from several
   angles. Expected: stable rendering with correctly placed icon layers and
   crate textures; this is not a claim of SF3 mission scripting.

## 2026-07-29 architecture correction

The earlier plan to reconstruct SF2 gameplay in native C++ was wrong. The
upstream SF1 port is a hybrid runtime: original R3000A gameplay remains live
while native code provides presentation and platform services. SF2 is now
pivoting to the same model.

The pre-pivot work was checkpointed locally at commit `8c4d317` on
`recovery/sf2-native-2026-07-29`; nothing was pushed. Continued work is on
`feature/sf2-guest-runtime`.

### What remains useful

- sequel disc, FOG, overlay, executable, mission, and asset support;
- the PSX machine, BIOS/CD bindings, snapshots, and guest execution pump;
- executable/overlay/script maps and deterministic probes;
- renderer, audio, input, mouse-look, and per-game runtime profiles.

Native SF2 mission choreography, combat, drops, and interactions are retained
only as diagnostic scaffolding. They will not be extended as the product
gameplay implementation.

### First corrected gate

`sf_tool probe-sf2-guest-bootstrap` now:

1. runs the untouched SF2 executable entry and `Game_Main`;
2. observes entry to the retail fourteen-state loop at `0x80029700`;
3. advances through eight distinct guest GPU submissions at `0x800f2e24`,
   the unique structural match for SF1's verified native-presentation
   boundary;
4. schedules PSX hardware and CD-ready callbacks between bounded execution
   slices;
5. restores a state-loop snapshot and requires the complete boundary trace to
   replay identically.

Both retail discs produce the same trace. The final two submissions come from
`0x800f157c` after the six initialization submissions from `0x800f18c0`, and
the retrace counter advances, proving progress beyond repeatedly yielding at
one PC.

The same probe now restores its state-loop snapshot and executes retail
`Common_Init(4)` at `0x8002a518`. It returns normally after 141,961 guest
instructions when PSX hardware slices and CD-ready callbacks are serviced
between bounded interpreter runs. This exposed one missing general BIOS
primitive, `A0:18 strncmp`; its bounded HLE and unit tests were added.

Exact commands:

```powershell
.\build\windows-msvc\Release\sf_tool.exe probe-sf2-guest-bootstrap `
  "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 1).cue" 20000000

.\build\windows-msvc\Release\sf_tool.exe probe-sf2-guest-bootstrap `
  "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 2).cue" 20000000
```

This proves deterministic resident guest execution, a native-presentation
boundary, and completion of the mission application-state initializer. It
does **not** yet prove mission selection, a mission-overlay load, guest
presentation publication, or playability. `Common_Init(4)` alone leaves the
application state at zero and neither COLO nor INIT code appears at the
expected runtime overlay window; the SF1-style select-mission transition is
therefore still missing. The next gate is to identify and execute that SF2
transition, verify the requested FOG/overlay bytes in guest RAM, then boot
Mission 3.

## 2026-07-29 Mission 3 transition probe

Retail overlay disassembly resolves the missing mission-selection boundary.
Both MENU stage select and TITLE new-game code call
`MissionArchive_Open` at `0x8002A338`; MENU passes the selected zero-based
mission index followed by `(1, 1)`. Mission 3 is therefore the exact guest call
`MissionArchive_Open(2, 1, 1)`.

The function:

1. validates selection indices `0..42`;
2. records the selection at `0x8012B02C`;
3. builds the FOG and SLF paths from the retail mission table;
4. opens the archive and installs its heap end;
5. requests state 12 through `0x8002BC44`.

The new `probe-sf2-mission-transition` command executes that boundary. It also
corrected three initially misread state addresses: MIPS `lui 0x8012` followed
by a negative immediate resolves into the `0x8011...` range. The verified
current-state, depth, transition, and state-stack addresses are now
`0x8011EE90`, `0x8011EE8C`, `0x8011EE94`, and `0x8010C5E4`.

### Confirmed blocker

The startup loop does not yet make retail `INIT.OVL` resident. The transition
probe therefore seeds the untouched disc bytes at proven load address
`0x80158878`; this is explicitly a probe bridge. Direct calls also use an
unused low-RAM stack because `MissionArchive_Open` clears the retail heap and
would otherwise erase the synthetic high-RAM invoke frame.

With those accommodations the transition reaches the retail state-9 loading
loop for 186 GPU boundaries and then advances to state 1. The FOG file-open
contract is still incomplete: the archive/heap-end pointer remains zero, no
exact HWAY FOG header appears in guest RAM, and no byte of the expected
header-stripped `HWAY.OVL` matches at `0x8014B978`. State 1 consequently calls
the retail memory-clear routine at `0x8015CF84` with a null address; its first
decrement produces `s0=0xFFFFFFFC` and a deterministic memory fault.

The probe-only fallback invokes the exact state-12 push after the failed
archive open so the downstream loader can be observed. It does not claim the
retail transition completed and is not a product boot path.

### Validation and exact commands

Release and the complete configured CTest suite pass. The suite currently
contains 18 tests; all 18 passed. Both retail discs retain identical
bootstrap/replay traces.

```powershell
cmake --build build/windows-msvc --config Release -- `
  --maxcpucount:1 /nodeReuse:false

ctest --test-dir build/windows-msvc -C Release --output-on-failure

.\build\windows-msvc\Release\sf_tool.exe probe-sf2-guest-bootstrap `
  "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 1).cue" 20000000

.\build\windows-msvc\Release\sf_tool.exe probe-sf2-guest-bootstrap `
  "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 2).cue" 20000000

# Expected diagnostic failure until the SF2 FOG open/heap-end contract works.
.\build\windows-msvc\Release\sf_tool.exe probe-sf2-mission-transition `
  "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 1).cue" 300000

git diff --check
```

The next slice is to trace `0x8002608C`/`0x80026234` through the physical-CD
file search and make the returned FOG catalog establish the nonzero heap end
consumed by `0x80025B48`. Only then can the probe require exact HWAY FOG and
mission-overlay bytes and proceed to mission initialization.

## 2026-07-29 Mission 3 transition gate closed

The FOG/file/heap work described above is now complete at the deterministic
probe boundary. `probe-sf2-mission-transition` exits successfully and repeats
identically.

The probe connects only platform and presentation-facing services:

- generic ISO `CdSearchFile` results and exact resident-overlay loads;
- exact `SLF.RFF` open/bulk-load into the retail downward heap;
- a catalog-copy compatibility hook needed because the current
  observe/continue boundary otherwise discards that guest-memory write; and
- neutral pad state at the resident input boundary.

Retail guest code still owns the FOG/RFF interpretation, archive heap
placement, state stack, loading lifecycle, mission initialization, and frame
loop. In particular, `MissionArchive_Open(2, 1, 1)` performs the state-12 push;
the probe no longer supplies a fallback transition.

The verified checkpoint is:

- state 9 at depth 3 immediately after the retail mission-open call;
- 76 retail state-9 loading frames through callback `0x8002C188`;
- retail state-12 readiness and pop, followed by Mission 3 state 13;
- the exact 2 KiB HWAY FOG header at guest address `0x801ECC90`;
- all 14,296 expected `HWAY.OVL` bytes at `0x8014B978`;
- eight stable GPU-submission boundaries with no state-loop restart; and
- 20 neutral guest pad polls across those stable frames.

The resident `INIT.OVL` mode-1 call now retains its initialization side
effects. Because that direct diagnostic call pushes application state 1, the
probe invokes the exact retail state-pop entry at `0x8002BC80` before opening
the mission; this restores the state-0 caller contract without rolling guest
RAM back. Exact INIT and MOVIE overlay bytes still enter RAM through the
probe-only resident-overlay bridge. One resident resource callback at
`0x8015BA54` remains a no-op platform bridge because its upstream resource
input is not yet resident.

### Validation

Release builds cleanly, all 18 configured tests pass, and `git diff --check`
is clean. Disc 1 and Disc 2 retain byte-identical bootstrap traces at the full
20,000,000-instruction budget. The successful Mission 3 evidence run uses the
3,000,000-instruction budget:

```powershell
cmake --build build/windows-msvc --config Release -- `
  --maxcpucount:1 /nodeReuse:false

ctest --test-dir build/windows-msvc -C Release --output-on-failure

.\build\windows-msvc\Release\sf_tool.exe probe-sf2-guest-bootstrap `
  "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 1).cue" 20000000

.\build\windows-msvc\Release\sf_tool.exe probe-sf2-guest-bootstrap `
  "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 2).cue" 20000000

.\build\windows-msvc\Release\sf_tool.exe probe-sf2-mission-transition `
  "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 1).cue" 3000000

git diff --check
```

## 2026-07-29 native GPU presentation snapshot

`Sf2PresentationFrame` is the first sequel-native publication boundary. It
accepts a complete 2 MiB guest RAM view only at capture time and publishes a
deep-copied packet DTO; consumers do not retain VM or RAM access.

The capture follows the PSX DMA linked list rooted at the retail render
callback argument and validates:

- aligned links contained within guest RAM;
- bounded packet and GP0 word counts;
- no repeated address/cycle; and
- an exact `0x00FFFFFF` chain terminator.

The Mission 3 probe now requires eight valid publications. They alternate
roots `0x801F8CA8` and `0x801F8CE8`; each contains four packets, twelve GP0
words, and four commands. Synthetic regression coverage proves a valid chain
is deep-copied and cyclic/truncated RAM inputs fail closed.

This is a valid structural presentation snapshot, not yet a visible Mission 3
frame. The current packets contain draw-environment and VRAM-transfer commands
but no polygon, line, or rectangle command (`draws:0`). Product runtime
selection therefore remains disabled. The next blocker is the missing input
to the resident resource callback at `0x8015BA54`; the probe currently
bridges that callback as a no-op, and state 13 cannot yet publish world/player/
actor/HUD primitives. The callback's input is now identified exactly:
`lui 0x8016` plus signed immediate `0x8AE8` resolves to `0x80158AE8`, which
contains `\COMMON\GLOBAL.DAT;1`. Reading it as `0x80168AE8` repeats the same
signed-immediate error that previously mislocated the application-state
globals.

## 2026-07-29 GLOBAL payload and state-13 dependency

The logical `\COMMON\GLOBAL.DAT;1` input is now resolved exactly. It is not
`HWAY.DAT`: that 2 KiB FOG member is the Mission 3 level-layout file, and
feeding it to the common parser deterministically faults in the first indexed
resource callback. The physical retail payload is `DISK1.INF` on Disc 1
(`DISK2.INF` on Disc 2). Its leading 33 selection labels are followed by the
indexed common strings expected by the INIT parser. The platform
`CdSearchFile` bridge now maps only the logical `GLOBAL.DAT` request to that
exact physical extent.

The probe also corrected the runtime-entry interpretation: resident overlays
are seeded byte-for-byte at `0x80158878`, including their 40-byte file header,
so `0x8015BA54` is the actual parser entry in guest RAM. Stripping the header
again during offline address calculation falsely moves the entry to
`0x8015BA2C`.

The visible-frame blocker is now localized to the retail disc-mode lifecycle.
State 0 reads signed byte `0x8011F26C`; when it remains `-1`,
`0x800A4450` pushes state 13. The normal initializer at `0x8002A518` resolves
one of two disc paths stored at `0x8011F410` and writes mode 0 or 1. Those
pointers are common-callback output. The current synthetic direct-call path
cannot yet retain the complete frontend GPU/stack-frame context needed while
running all four common callbacks, so the stable probe continues to isolate
the callback and does not write the mode byte natively. Mission FOG/overlay
placement and the structural presentation snapshot remain deterministic; the
next gate is to run common initialization in its continuous retail state-loop
frame and then require state 0 plus draw primitives.

## 2026-07-29 continuous resident resources and Mission 3 opening

The earlier `GLOBAL.DAT -> DISK1.INF/DISK2.INF` conclusion was a false
physical-ISO fallback and is superseded. The supported SF2 executable contains
an eight-entry resident HOG at `0x801B92A8`; its exact entries include:

- `BEEPSX.VB` (5,504 bytes);
- `BEEPSX.VH` (1,236 bytes);
- `GLOBAL.DAT` (6,152 bytes);
- `INIT.OVL` and `MOVIE.OVL`; and
- the resident title resources.

The mission probe now exposes this archive at the retail platform file
boundary. `0x80026234` allocates a normal five-slot file record and
`0x80026414` synchronously copies the exact embedded bytes, padded only to the
retail sector-sized read request. INIT remains the authority for archive
parsing and callback-table construction. With the exact bytes, its file pool
finishes valid (`0/CACA/CACA/CACA/CACA`) instead of the former corrupted
`0/168F/168F/168F/168F`.

The synthetic direct-call boundary was also the wrong lifetime for full INIT:
its own callback path can legitimately encounter the suspended caller PC.
The probe now injects nested calls into the captured retail state-loop frame
with a unique low-RAM return trampoline and restores the suspended CPU frame
after the nested return while retaining guest RAM/device side effects.

This continuous path now proves:

- full resident INIT returns after 148,631 guest instructions;
- one exact resident-file open and two exact resident reads occur before
  mission selection;
- `MissionArchive_Open(2, 1, 1)` returns to the suspended loop in state 9,
  depth 3;
- retail FOG search/catalog and SLF open/load still occur;
- the loop advances through application states `9 -> 1 -> 4`; and
- 32 distinct state-4 `CdSync` polls execute without the former
  null-heap, bad callback, or `0x80168334` data-as-code fault.

MOVIE uses `B0:56 GetC0Table` to inspect and patch Sony's low-RAM exception
handler. The ROM-less HLE can now optionally expose the guest C0/B0 table
contract; the option is enabled only for this continuous sequel path and has
focused regression coverage. A probe-local exception-return trampoline
preserves the interrupted `ra` across the host-owned low exception boundary.

The state-4 STR/device lifecycle does not yet complete into state 12. It is
therefore the exact next continuous-path blocker. After proving the 32 CD
polls, the probe restores its pre-transition snapshot and reruns the prior
deterministic HWAY checkpoint: exact FOG header, exact 14,296-byte overlay,
eight stable GPU boundaries, and the immutable four-packet presentation
snapshot. That second checkpoint remains structural (`draws:0`) and is not
misrepresented as a continuous product boot. Product SF2 selection remains
disabled.

Release validation remains green: all 18 configured tests pass, both retail
discs retain identical eight-boundary bootstrap traces, and two independent
Mission 3 runs produce byte-identical diagnostics. The mission evidence run
uses the existing 3,000,000-instruction slice budget; `git diff --check` is
clean.

## 2026-07-29 exact TITLE ownership and primitive-bearing presentation

The previous state-4 diagnosis was incomplete. `0x800F5D48` is indeed the
retail `CdSync` boundary used by the common asynchronous-file manager, but
state 4 itself is the `TITLE.OVL` frame. Its fixed callback at `0x80153E24`
was eight zero words in the continuous path, so waiting for more CdSync polls
could never progress it.

The executable's resident-overlay map proves that callback belongs to the
53,032-byte `TITLE.OVL` loaded at `0x8014B950`. The retail application-state
selector at `0x8002B9A8` requests exactly:

- name `TITLE.OVL`;
- destination `0x8014B950`; and
- load mode 4.

The probe now invokes that selector after resident INIT mode 1 returns and its
temporary state is popped. The untouched mode-4 loader opens TITLE through
the embedded-HOG file boundary, reads all 53,032 bytes, and performs its own
heap-boundary side effects. A byte-for-byte comparison against the embedded
HOG entry is required before mission selection proceeds. This is not a
callback injection or a native overlay implementation.

With TITLE resident, the continuous application loop still advances through
states `9 -> 1 -> 4`, but state 4 now executes retail code and yields at the
GPU submission boundary. Its internal staged resource loader advances title
state 3 to state 0 and performs five additional embedded-file opens. Across
1,400 observed state-4 boundaries, native capture finds a best immutable frame
with:

- 18 DMA packets;
- 54 GP0 words;
- 18 GPU commands; and
- two draw commands, both opcode `0x2A` textured polygons.

The final idle title frame is correctly still a clear/environment-only
command, so the probe retains the best primitive-bearing immutable snapshot
as semantic evidence rather than assuming the last frame is representative.

This proves that the previously missing state-4 callback is TITLE-owned; it
does not prove that the retail movie/frontend lifecycle is complete. The
continuous path has not yet loaded `HWAY.OVL`, and the direct mission-open
call remains a diagnostic boundary. Product runtime selection remains
disabled until the authoritative frontend path loads exact HWAY bytes and
publishes Mission 3 world/HUD primitives.

## 2026-07-29 MOVIE lifecycle and embedded-HOG lifetime correction

A follow-up experiment enabled exact `MOVIE.OVL` loading during its real
`Common_Init(4)` owner rather than after that call. This exposes the missing
frontend dependency:

- TITLE advances through internal states `15 -> 3 -> 0`;
- it then oscillates between states 0 and 12 because MOVIE's shared active
  flag at `0x80146710` never remains set;
- the exact `MOVIE1.HOG` ISO search succeeds, but the STR/device lifecycle
  does not complete; and
- the title input dispatcher at `0x801538C4` polls only in state 3. A scripted
  confirm edge (bit 6, mask `0x0040`) does not skip the missing lifecycle.

This also establishes a strict resource lifetime. The embedded resident HOG
starts at `0x801B92A8` and its directory offset word is initially `0x38`.
Mission 3's 536,576-byte `SLF.RFF` is allocated downward across that address,
legitimately overwriting the raw HOG. TITLE and its nested `TITLE.HOG`
resources must therefore be consumed before mission archive allocation.
Calling `MissionArchive_Open` while the MOVIE/TITLE frontend still needs the
resident archive is not a valid retail ordering.

The green deterministic checkpoint has been restored: MOVIE loading remains
disabled during the diagnostic common-init call, exact INIT and TITLE are
consumed first, the direct mission call demonstrates `9 -> 1 -> 4`, and the
best immutable title frame retains 18 packets, 54 GP0 words, 18 commands, and
two opcode-`0x2A` textured polygons. The older exact HWAY checkpoint still
runs afterward. This diagnostic evidence remains useful, but it does not
supersede the actual next blocker: implement the MOVIE-owned
`MOVIE1.HOG`/STR platform contract, return through TITLE's own states
16/17/18 and `0x80153D30`, and only then allocate the mission archive.

## 2026-07-29 catalog, scheduler, and lifetime correction

Further guest tracing separates three boundaries that the previous note
treated as one MOVIE/STR problem.

First, TITLE's request at `0x80143AF0` belongs to its asynchronous memory-card
manager. `0x80143A40` installs worker `0x80145020` in root-counter callback
slot 7. The ROM-less probe did not dispatch that slot, and its generic event
HLE did not deliver the registered no-card callbacks. An isolated experiment
serviced the exact worker and invoked the two retail no-card event handlers;
the job then completed at state 0 and depth `0xFFFFFFFF`. This is scheduler
and memory-card evidence, not STR playback.

Second, the executable's movie catalog initializer is `0x8002C2CC`. It opens
`\MOVIE1.HOG;1`, calls `0x8002723C`, and reads exactly one 2 KiB header sector
through `0x80026414`; the guest parses its count and offsets into the catalog
at `0x801279D0`/`0x80127A3C`. The existing external open path did not complete
in the ROM-less probe. A temporary exact five-slot file-record bridge plus the
untouched first ISO sector made the catalog nonzero and caused `0x8002C188`
to enter the retail playback initializer at `0x80142E60`.

That exposes the third and current boundary: the STR ring is never populated.
The retail poll at `0x800FFDCC` ran 22,213 times without observing a ready
sector. MOVIE then dereferenced its still-untouched timeout sentinel at
`0x80142874`. No decoder or movie-selection logic was replaced natively, and
the experimental external catalog bridge was removed from the green probe.

This also corrects the earlier continuous `9 -> 1 -> 4` statement. A direct
diagnostic `MissionArchive_Open(2, 1, 1)` still reaches state 9 at depth 3,
but its `SLF.RFF` allocation overwrites the embedded HOG before TITLE cleanup.
The following state-1 HOG lookup therefore stops deterministically at
`0x80026D80`; the honest continuous prefix is `9 -> 1`. The probe now records
that exact lifetime blocker, restores the independent pre-transition
snapshot, and continues to require the older exact HWAY assertions:
`0x801ECC90`, exact overlay bytes, retail state 13, eight alternating roots,
and the valid four-packet presentation frame.

The next slice is no longer “find MOVIE1.HOG.” It is to dispatch the exact
root-counter/event callbacks, implement the platform STR sector-ring contract,
return through MOVIE into TITLE states 16/17/18 and `0x80153D30`, and only
then permit mission archive allocation. Product runtime selection remains
disabled.

Release validation after restoring that checkpoint passed:

- full MSVC Release build;
- all 18 CTest targets;
- 20,000,000-instruction Disc 1 and Disc 2 guest bootstraps; and
- two byte-identical 3,000,000-instruction Mission 3 transition probes.

## 2026-07-29 retail task scheduler and STR-start correction

The previous “ring never populated” conclusion was one layer too late. A
call trace of MOVIE's exported lifecycle showed:

- `0x80142E60` initializes the ring and playback state;
- the executable calls `MOVIE_Update` at `0x80143160`;
- that update first calls `0x80142500`, which asks `0x8002686C` to start an
  asynchronous file operation;
- `0x8002686C` queues callback `0x800266D4` through the executable task-list
  function at `0x800226D4`; and
- no call reached `0x800265A8`, `0x8002619C`, `0x8002676C`, or
  `0x800F7808` before the old `0x80142874` stop.

The missing owner was therefore the retail task scheduler. `Common_Init(1)`
normally calls `0x800226A0`, which registers worker `0x80022584` in interrupt
callback slot 4. The live callback implementation at `0x80100F1C` stores its
eight slots from `0x8011D0D4`; slot 4 is `0x8011D0E4`. The similarly shaped
table at `0x8011D108` belongs to a different interrupt implementation and was
an incorrect earlier inference.

`LegacyGameplayVm::servicePsxCallbackSlot` now executes the registered target
clock-neutrally, restores the interrupted CPU context, and preserves the
callback's guest-RAM side effects. The SF2 diagnostic scheduler dispatches
slots 4 and 7 at the existing fixed 20 Hz guest-update cadence. This keeps
the executable worker and its queued callback authoritative; the host does
not implement a movie task or ring filler.

The next trace exposed two paired libcd state requirements:

1. host-bound asynchronous `CdControl` must publish command `0x15` in the
   guest pending-command word so retail `CdSync` can observe the later INT2;
2. the completion status byte must be mirrored into `0x80141A10`, from which
   `CdSync` copies its result, as well as the active response pointer
   (`0x80141A18` in this path).

Before the mirror, the controller correctly returned status `0x02`, but
`CdSync` delivered byte `0x00` to `0x800266D4`, so the one-shot task never
entered the stream starter. After preserving both contracts, untouched guest
code reaches:

- seek/start wrapper `0x800265A8`;
- file seek `0x8002619C`;
- stream-mode entry `0x8002676C`;
- STR callback registration `0x800F7808`; and
- one executable data-ready dispatch at `0x800F703C`.

The bounded MOVIE preflight now reports:

```text
task=0x80022584 catalog=2/1 stream-start=1 ready=1
stop=returned@0xFFFFFFF0
```

It no longer stops at `0x80142874`. The preflight runs in an isolated VM
snapshot and restores that snapshot before the independent Mission 3
diagnostic, so it does not relax the embedded-HOG lifetime rule or claim a
continuous campaign transition. Focused tests cover callback-slot CPU
preservation, async command publication, paired completion-result mirroring,
and the neutral BIOS `_card_info` no-card contract.

The next continuous-path task is now the retail MOVIE-to-TITLE completion and
TITLE states 16/17/18 through `0x80153D30`, before any mission archive
allocation. Product SF2 selection remains disabled.

Release validation for this slice passed:

- full MSVC Release build with warnings as errors;
- all 18 CTest targets;
- 20,000,000-instruction Disc 1 and Disc 2 bootstrap probes;
- two byte-identical 3,000,000-instruction Mission 3 probes
  (`SHA-256 89AEB8548E92D67072776DB00DE63471922D078342E10E4C252B3AD94DD7EE23`);
  and
- clean `git diff --check`.

Both mission replays include the same MOVIE scheduler preflight and retain the
same independent exact HWAY result: state 13, 32 loading boundaries, eight
stable alternating roots, and a valid four-packet/twelve-word presentation
snapshot. The direct continuous experiment still stops deterministically at
the documented embedded-HOG lifetime fault `0x80026D80`.

## 2026-07-29 asynchronous CD callback correction

The apparent MOVIE and `Common_Init(4)` returns above were false positives.
The host scheduler invoked the retail CD callback while another guest function
was suspended, but did not restore that function's CPU context afterward. The
probe therefore saw the callback's return sentinel (`0xFFFFFFFF`) and reported
it as if the original function had returned.

The callback boundary now matches interrupt ownership:

- both data-ready INT1 and command-completion INT2 callbacks run on the
  isolated guest stack at `0x807F0000`;
- the complete interrupted CPU state is restored after callback execution;
- INT1 continues to enter the retail data-ready callback at `0x800F703C`;
- INT2 resolves the guest callback pointer from `0x8011D1BC`, whose current
  target is the exact sync callback at `0x800F7008`; and
- INT2 mirrors the active response, clears the pending command, publishes
  completion state 5, acknowledges the controller IRQ, and only then calls the
  guest target.

The callback slot address is significant. The instruction using base
`0x80120000` has signed displacement `-0x2E44`, so it resolves to
`0x8011D1BC`, not `0x8012D1BC`.

The underlying event contract was corrected at the same time. `CdControl`
waits synchronously only for each command's INT3 acknowledgement; Stop, Pause,
Init, and SeekL retain their later asynchronous INT2 completion. A
clock-neutral completion request now chooses the pending CD event with the
earliest deadline, preventing an older, later-due sector event from being
delivered ahead of a command ACK. Probe execution suppresses guest hardware
interrupt delivery while the host owns these callback boundaries, and derives
retrace increments from PSX CPU ticks at 60 Hz instead of once per
20,000-instruction scheduler slice.

With CPU preservation in place, `Common_Init(4)` no longer “returns” from the
state-loop snapshot. It continues honestly into `CdRead`/`CdReadSync`. That
snapshot still owns an active bootstrap read, however, so starting a second
one-sector init read there mixes stale INT1 sectors with the new request. The
sector-header transfer itself is valid and the expected sector eventually
arrives, but the stale interrupt has already made `CdReadSync` report failure
and retry. An experiment that simply ran forward to a convenient overlay PC
also failed to establish an executable application-frame boundary and was
removed.

The next blocker is therefore to obtain a quiescent, executable-owned
bootstrap continuation before invoking `Common_Init(4)`. Only then can MOVIE
finish through TITLE states 16/17/18 and `0x80153D30`, followed by continuous
state 12, exact HWAY placement, stable Mission 3 frames, and semantic native
presentation. The earlier exact-HWAY and primitive-bearing TITLE snapshots
remain independent diagnostics; they are not evidence of a continuous boot.
Product SF2 selection remains disabled.

Focused Release regressions now cover:

- preservation and isolated-stack arguments for direct INT1 callbacks;
- pointer-based INT2 sync callback dispatch, PsyQ state/result mirroring, IRQ
  acknowledgement, and full interrupted CPU preservation; and
- earliest-deadline selection when command and sector CD events coexist.

## 2026-07-29 continuous Mission 3 presentation checkpoint

The continuous retail path now reaches Mission 3 and publishes semantic native
presentation. Three independent faults were corrected without adding native
gameplay:

1. The deterministic TITLE selected-state entry skipped the normal frontend
   call that balances a temporary GPU packet-arena reservation. State 8 built
   the correct mission arena, but the following state-0 pass restored the stale
   TITLE arena and allowed mission heap allocation to overwrite live render
   objects. Calling the exact retail restore function `0x80015510(0, 0)` after
   TITLE teardown preserves state 8's mission arena.
2. The old renderer crash at `0x8001CB88` disappeared, exposing a model bounds
   scan at `0x8001BAD4`. DMA3 destinations advanced correctly; the source was
   wrong. Mission descriptors requested low sector `0x0E`, which is relative
   to the mounted FOG, while `DiscCdRomMedia` treated it as physical disc LBA
   `0x0E`. A scoped FOG-relative extent mapping now translates that platform
   address while leaving retail CD control, DMA3, parsing, and model ownership
   untouched.
3. The standalone Disc 1/Disc 2 bootstrap readiness check had inherited
   Mission-3-only INIT and disc-index requirements. Those checks now apply only
   to the mission transition probe, restoring the independent cross-disc
   bootstrap contract.

The verified application-state path is:

```text
0 -> 1 -> 8 -> 0
```

The probe reaches eight stable GPU-submission boundaries in 64 frames. The
best immutable `Sf2PresentationFrame` contains:

- 894 linked-list packets;
- 8,119 GP0 words;
- 894 decoded GPU commands; and
- 830 polygon/line/rectangle draw commands.

This supersedes the earlier independent TITLE/HWAY snapshots and the
embedded-HOG lifetime blocker as the primary continuous checkpoint. The
retail executable, INIT, HWAY overlay, mission resources, object/model code,
camera, and renderer remain authoritative. Product SF2 selection is still
disabled until the narrow input/presentation/audio handoffs and interactive
Mission 3/campaign validation are complete.

Release validation for this checkpoint:

- full MSVC Release build;
- all 18 CTest targets;
- focused CPU/CD/XA/SPU/DMA regressions (5/5);
- 20,000,000-instruction Disc 1 and Disc 2 bootstrap probes with identical
  traces;
- two byte-identical 30,000,000-instruction Mission 3 probes
  (`SHA-256 04E536143361C93EC06E62EF09C1646721F3FD0A5F8BDCDAD2D9CD2C370BAD50`);
  and
- Mission 3 presentation validation at eight stable boundaries.

## 2026-07-30 Mission 3 product presentation alpha

The continuous checkpoint is now connected to the Mission 3 scene-test
product path. `Sf2GuestMissionRuntime` owns the exact Disc 1 TITLE-to-HWAY
transition and publishes only complete authored world lists. The earlier
612-packet list was a loading-card/interface submission (489 sprites and 91
lines); the first accepted world frame instead has 893 packets, 8,118 GP0
words, 830 draws, 451 textured gouraud triangles, and 309 textured gouraud
quads.

PsyCross reconstructs the retail polygon/line/sprite primitives rather than
copying a PSX DMA header into a host primitive header. Raw TPAGE and CLUT
values are relocated through the existing mission texture-residency maps.
That relocation changed the product capture from a gray viewport with a few
white fragments to the textured Mission 3 tunnel/opening geometry. VRAM
upload/fill/copy packets remain validated separately because PsyCross
`LoadImage` needs a host payload pointer.

The scene loop now:

- samples the configured keyboard/mouse actions and controller state;
- quantizes relative mouse aim/turn into retail pad axes without replacing
  guest collision or camera logic;
- publishes retail display submissions at 60 Hz while retail gameplay and PAD
  logic retain their authored 20 Hz cadence;
- replays immutable guest GP0 output; and
- drains guest SPU/XA PCM into the normal host audio queue.

Retail registration function `0x800222BC` returns the processed PAD record at
`0x80122FEC`, using a 60-byte stride. The earlier `0x80112FEC` candidate is
the registration table itself; writing samples there forced a false Start
sequence and the state-7/state-9 loop. Input is now written only at the exact
returned record. Two independent 1,400-update neutral runs are byte-identical
(`GP0 SHA-256 00409c4dd8e4983c4bebba0d89cad01b5fefb86e24c6d38ebf882878027abebd`,
1,683 packets, 1,586 draws, 88,200 PCM frames). Holding forward preserves
application state 0 and PCM handoff but changes the authored stream to 1,115
packets, 1,032 draws, and
`70dfa6480334b9cf22a58c4fc2c4f4fbec6e07690d05db600e9c347382023bcb`.
This proves that product input reaches retail gameplay rather than merely
changing host presentation.

The first in-mission checkpoint is captured by exact retail function
`0x800AD48C`. After validating its serialized command stream, the runtime
retains a complete CPU/RAM/CD/SPU snapshot at that boundary. Retail later
enters restore function `0x800AD9F4`; the host atomically reinstates that exact
snapshot because the synchronous checkpoint-CD restart remains outside the
current machine contract. No native mission state is manufactured. A
5,000-update neutral run remains in application state 0 through two such
restores, continues publishing 1,404-draw authored frames, and continues
draining PCM. Consumed GP0 side-effect words are compacted while incomplete
VRAM uploads are retained, bounding long-run capture storage.

Final validation for this alpha:

- the focused `sf_tests` binary passes;
- the full MSVC Release build and all 18 CTest targets pass;
- 20,000,000-instruction Disc 1 and Disc 2 bootstrap probes pass with
  identical traces;
- the 20,000,000-instruction Mission 3 transition probe preserves the
  authoritative `0/1/8/0` path and 894-packet/8,119-word/830-draw best frame;
- neutral replay is deterministic and forward input diverges as described
  above;
- the 5,000-update product probe survives two checkpoint cycles; and
- the Release PsyCross executable builds and sustains a bounded Mission 3
  product smoke run.

The current user-facing entry is:

```powershell
.\build\windows-psycross\Release\syphon_filter.exe `
  --no-launcher --mission=3 --scene-test `
  "Z:\Emulators\PS1 Games\Syphon Filter 2 (USA) (Disc 1).cue"
```

This was initially labelled a playable Mission 3 alpha. Interactive review
subsequently disproved that label: presentation cadence, page flipping,
mission-script activation, HUD, and scene XA had not been validated
semantically. The command remains a development entry point, not a playable
release claim.

### Product black-screen correction

The first interactive product run exposed a presentation-only mismatch which
the GP0 hash probe could not detect. SF2's retail `E1` packet clears the PS1
draw-to-display bit while drawing normally into VRAM; PsyCross interprets that
bit as selection of an offscreen framebuffer, so the guest advanced while the
native swap target stayed black. Native replay now forces only that host
display-selection bit after relocating the retail TPAGE.

The bridge had also added a 384x240 host centre to packet coordinates even
though the same immutable list replays retail's `E5` draw offset. Removing that
duplicate transform restores the authored framing. A captured 1707x960
no-MSAA product framebuffer now contains the full textured Mission 3 opening
rather than an empty swap target. Retail geometry, draw offset, packet order,
textures, and camera remain authoritative.

## 2026-07-30 interactive-alpha audit and correction

The first sustained user test exposed failures which deterministic GP0 hashes
could not detect:

- one public runtime update was paced at 20 Hz even though it represented one
  of SF2's 60 Hz display submissions; gameplay therefore advanced at roughly
  one third speed;
- retail alternates 384x240 draw areas at VRAM y=0 and y=240, while PsyCross
  presents one host framebuffer; replaying the second page literally produced
  black alternate frames;
- padding each display submission without accounting for utility ordering
  tables generated 629,090 PCM frames over 600 displayed frames instead of
  the exact 441,000, overflowing the bounded host audio FIFO; and
- `HWAY.SS` was relocated, but its eight-entry registry appeared
  asynchronously after the direct TITLE handoff and no program was active.
  The global dispatcher ran an empty list, so the authored opening target
  assignments never occurred and the tunnel soldiers attacked Gabe.

The runtime now advances through utility submissions until one complete
retail display list is published, and the product loop paces that boundary at
60 Hz. Hardware/SPU time is advanced by exactly one 60 Hz slice per published
frame; a 600-frame probe now produces exactly 441,000 44.1 kHz PCM frames.
Native replay removes only the y=240 page component from E3/E4/E5 commands.
A 45-second product smoke run survives beyond the former audio-FIFO failure.

Mission script startup now waits until the relocated registry contains all
eight HWAY programs. It resets the exact retail VM and activates the LEVEL
record obtained from retail's own table; predicates, actions, actors, and
subsequent program activation remain guest-owned. With LEVEL active, a
3,000-frame neutral probe keeps Gabe at 150 health with no checkpoint restore,
where the former path restarted at frame 1,239 after health fell as low as 25.
A held-forward run over the same interval moves Gabe from x=4206 to x=1389,
providing an actual player-position control gate rather than GP0-hash
divergence alone.

The audit does **not** restore the playable-alpha label yet. No HUD
rectangle/sprite command has been observed, and scene-speech requests still
do not reach the retail XA cue resolver. Interactive confirmation of camera
handoff, enemy choreography, HUD, controls, and audio is required before
calling this playable.

### Interactive gameplay validation and remaining presentation gaps

The corrected build has now passed the missing semantic user test. The
in-engine opening runs at normal cadence without alternating black frames,
the soldiers engage Chance instead of Gabe, the authored camera hands control
to gameplay, target lock works, and contextual execution animation executes.
Gabe can be controlled through the retail movement model for sustained
gameplay. This validates the mission-script activation and player-control
gates which the earlier GP0-only claim did not test.

Two visible product gaps remain: the gameplay HUD is absent and the product is
silent. PCM measurement distinguishes an output-device problem from a guest
generation problem: a 1,500-frame probe emits exactly 1,102,500 stereo frames,
but every sample is zero, no SPU voice is active, and no XA filtered stream is
started. Scene speech reaches `SceneSpeech_Start` and its callback repeatedly
but remains deferred before `XaCue_ResolveAndPlay`. The common asynchronous
file list remains non-empty at `0x8011F998`; its current four-sector request
targets guest RAM `0x801D71FC` and carries completion callback `0x800A2370`.
That CD/resource lifecycle is the next audio investigation boundary. No
speculative forced completion is retained in the runtime.

The PC adapter currently supplies retail PAD semantics. Consequently A/D map
to original L2/R2 corner movement rather than unconditional modern strafing,
and relative mouse input does not replace the retail chase camera. Weapon
cycle actions also still need an explicit SF2 guest mapping. These are input
adaptation gaps, separate from the now-validated guest gameplay behavior.

### Mission resource and XA audio correction

The silent product path was guest-generated silence, but the host audio sink
was not the cause. Retail's first gameplay resource request completed one
sector DMA and then retried indefinitely for two independent CD-contract
reasons.

First, mission FOG sectors are addressed relative to their mounted extent.
The payload bridge mapped those sectors correctly but retained the parent
disc's absolute MSF bytes in each raw-sector header. Retail validates that
header against its relative LBA, marked every sector invalid, and reset the
four-sector read. `DiscCdRomMedia` now preserves the exact raw payload while
normalizing only the three MSF bytes for a mapped extent.

Second, PsyQ reads a raw sector with a short header DMA followed by a 2 KiB
payload DMA. Reasserting the CD data-request bit must continue the active FIFO;
the emulated controller instead rewound it, shifting resource data by 12
bytes. The request register now resets position only on a new request edge.
Focused tests cover both the relative raw-header view and segmented DMA
continuation. The low-level libcd bridge also now supplies both required
`Setfilter` bytes (file and channel) and safely retires a scheduled
Stop/Pause/Init/Seek completion before an immediate follow-up command.

After these corrections the request callback at `0x800A2370` completes,
scene-speech I/O reports ready, and retail reaches both XA cue resolution and
stream start. A 1,500-frame forward probe still produces the exact 1,102,500
PCM frames, now with 273,140 nonzero frames, peak sample 15,948, and first XA
audio at display frame 22. The HUD remains absent and requires a separate
guest draw-state investigation.

### Guest-state HUD projection and PC input adaptation

The HUD investigation first ruled out immediate GP0 drawing: the machine
emitted no immediate sprite stream. A later small-list hypothesis was also
rejected rather than shipped. `0x800F2E24` is a generic callback dispatcher,
not an exclusive `DrawOTag` boundary; some callback arguments happen to form
bounded linked lists whose payload begins with values in the GP0 opcode range.
Tracing one candidate exposed model/vertex data and invalid screen
coordinates, so no such list is composited into the retail world frame.

The product now follows the same ownership rule as the established SF1
hybrid: guest gameplay remains authoritative while native presentation reads
a bounded state projection. The live player instance supplies:

- health and armor through its retail health controller;
- equipped sequel item at inventory offset `+0xCC`;
- the two ownership words at `+0x3C`; and
- 34 reserve/magazine pairs beginning at `+0x44`.

The existing `sf2WeaponForItem` table converts only supported sequel items.
The SF2 HUD atlas then draws the original status label/bar, weapon icon,
ammunition text, and weapon ribbon without mutating guest inventory. The
overlay stays hidden for the authored in-engine opening and becomes visible
at its camera-handoff interval. Unsupported H11/crossbow/tool items remain
explicitly unrepresented rather than being aliased to unrelated SF1 slots.

The SF2 product input adapter now uses the configured PC bindings instead of
requiring discovery of the original pad layout:

- A/D feed retail L2/R2 strafing, while Q/E retain keyboard turning;
- relative mouse motion is accumulated until the guest's 20 Hz PAD sampler
  consumes it, preventing two of every three 60 Hz deltas from being lost;
- mouse motion turns the chase camera/body and controls the retail aim sight;
  and
- middle mouse, mouse wheel, and bracket weapon actions emit sampled Select
  edges, with a sampled release between queued wheel notches.

The 1,500-frame forward gate remains stable after the presentation/input
changes: 1,102,500 PCM frames, 273,140 nonzero frames, peak 15,948, 517 PAD
samples, live knife inventory (`item 20`, ownership bit `0x00100000`), 150
health, no checkpoint restore, and no guest fault. The Release product builds,
focused regressions cover guest HUD projection, unsupported-item rejection,
sample-bounded mouse accumulation, and press/release weapon pulses. All 21
CTest targets pass, and `git diff --check` is clean. HUD placement,
weapon switching after the opening pickup, and mouse/strafe feel still require
the final interactive check.

### Interactive combat validation, retail restart, and HUD anchoring

The next user run crossed the first real gameplay bar. XA dialogue is audible
and correctly timed; Gabe can acquire a firearm, switch to the M16, kill a
guard, lock on, and execute the contextual knife animation. First-person mouse
aim works on both axes. Chase mouse yaw works, while chase pitch remains
absent and sensitivity is still provisional. A/D strafing needs one clean
interactive confirmation. SPU sound effects and music remain absent.

The projected HUD was present but inherited the guest frame's centre-origin
E5 draw offset. That placed the health bar near screen centre and displaced
the weapon layer outside its intended footprint. `drawSf2GuestHud` now
establishes an isolated zero-offset 384x240 draw environment before emitting
native overlay primitives. It also draws the first-person reticle; retail
headshot classification is not yet projected.

Both interactive sessions ended within roughly two minutes. The preserved
scene-test log captured a guest fault at `0x8001D87C`, and long neutral,
forward, and combat probes all converged on retail restore function
`0x800AD9F4`. The alpha bridge had replaced that function with an atomic
whole-machine snapshot rewind. Different input histories then failed at
different renderer PCs after the restored guest generation resumed.

The snapshot substitution has been removed. The bridge now observes the
restore edge and executes retail's original checkpoint loader. Its synchronous
CD wait originally exhausted the clock-neutral instruction budget; repeated
instruction-budget slices during an active restart now advance the guest
hardware scheduler so CD commands, DMA, callbacks, SPU/XA state, and the
mission reload can complete. Samples already submitted to OpenAL are discarded
at the restore edge because they belong to the retired audio generation.

A cleaned 7,200-update neutral probe now reaches the authored death boundary
at update 6,834 (health falls to 25), executes retail restore, creates a fresh
full-health player instance, reloads HWAY, and sustains another 366 display
updates without a guest fault. A separate 8,000-update combat stress run
survived 3,470 updates after its restore. The short pre-restart control gate
also remains green at 1,500 forward updates: 517 PAD samples, live player
movement, no restore/fault, and 1,107,057 PCM frames with 273,122 nonzero
frames and peak 15,831. The Release build and all 21 CTest targets pass, and
`git diff --check` is clean.

### Interactive traversal, HUD fidelity, and retail SPU voice service

The follow-up user run confirms that the restart work crossed the practical
playability threshold. Health now anchors in the upper-left; the M16 weapon
icon and ammunition render; the first-person reticle is visible; A/D strafes;
door kicking, door traversal, climbing, death, and retail checkpoint restart
all work. Ordinary play remains stable beyond two minutes. Two presentation
defects remained visible: the native fallback and guest third-person reticles
overlapped, and distant tunnel geometry was incorrect after death/restart.

The reticle duplication is removed by leaving reticle ownership with the
guest. SF2 pistol projection no longer falls through to the intentionally
empty SF1 definition and selects the sequel's embedded `PISTOL2A/B` layers.
An attempted `KNIFEA/B` addition required relocating `KNIFEB` out of the
palette-reserve row; that relocation was withdrawn after the user reported a
clean-run visual regression. The knife icon remains absent until it has
isolated host-atlas residency.

The missing non-XA audio was not a bank, allocator, mixer, or host-device
failure. Retail calls reached `0x800F9F68`, allocated voices, and staged SPU
key masks through `0x800FD020`, but the direct TITLE-to-HWAY bridge omitted
the outer lifecycle which calls the no-argument flush at `0x80104B40`.
Registering that exact retail routine in interrupt callback slot 6 and
servicing it at 120 Hz activates the existing guest voices. No sound event,
voice, or PCM data is synthesized by the host.

The callback is installed after mission handoff and reinstalled after retail
checkpoint reload settles because cleanup clears the interrupt table. Sound
service is suspended while the synchronous checkpoint loader advances CD
hardware: those instruction-budget slices are not normal gameplay callback
boundaries, and invoking the flush there can re-enter resource state during
the reload. A
1,500-frame forward gate now observes 17 peak active voices, 842 key-on
writes, 1,102,500 PCM frames with 1,102,485 nonzero frames, and peak sample
26,340. A 7,200-frame neutral stress gate crosses retail restore at frame
6,834, survives another 366 frames, retains callback slot 6, reaches 23 peak
voices and 4,166 key-on writes, and produces 5,330,740 PCM frames with
5,330,721 nonzero frames and peak sample 28,403. The playable-alpha gate now
fails if it sees no active SPU voice or only initialization key-on writes.
Music remains an interactive verification item.

The first interactive mission-failure run after that change did not corrupt
resident code. It instead exposed a deterministic lifecycle wait at
`0x80014D8C..0x80014DBC`: retail polls `VSync(-1)` until two retraces pass,
but the product-owned clock previously advanced only at DrawOTag submissions,
and this loop submits none. Instruction-budget slices in that exact wait now
advance the hardware scheduler, allowing the failure transition to proceed
to the retail checkpoint loader.

The apparent C-key overlap had a second source beyond PsyCross's merged
keyboard pad. The processed SF2 PAD record derives a face-button vertical
axis, so synthesized Cross produced both the crouch bit and `-127` movement.
PC-synthesized face actions now opt out of those derived axes while physical
controller face buttons retain the retail record. A 1,500-frame held-crouch
probe finishes at `(4208,-149,-25830)` versus neutral
`(4206,-195,-25824)`: posture/root-animation displacement without sustained
backward locomotion.

A later interactive weapon switch faulted while fetching resident renderer
code at `0x8001E4FC`. The expected word `0x84970004` had become
`0x841699B4`; like earlier faults at `0x8001D87C` and `0x8001DCBC`, the high
byte survived while the low 24 bits became a RAM link. That is the signature
of an ordering-table/DMA tag written into code, not an inventory or SPU
failure. The R3000 runtime now supports a narrow write watch, and the SF2
product guards `0x8001D000..0x8001EFFF` after bootstrap. A violating store is
reported with writer PC/instruction, address, value, frame, and restore count
before the corrupted instruction executes. An 8,000-frame combat/restart
probe produces no legitimate write in this range.

The reported clean-run tunnel corruption was ultimately native framebuffer
accumulation, not damaged world textures. The persistent SF2 scene loop called
`PsyX_EndScene` after each presentation but never began the next scene.
Consequently PsyCross performed neither swap-buffer setup nor a color clear,
and semi-transparent snow retained every previous position. Each SF2
presentation now installs a black 384x240 background draw environment and
calls `PsyX_BeginScene` before replaying the guest frame. The clear targets
only the native framebuffer; emulated VRAM and the restored HUD atlas remain
intact.

### Deep-tunnel stability and guest-owned texture residency

The next interactive run confirms that the framebuffer clear fixed the snow
trails and broad distant-scene corruption. Repeated retail death/restart
cycles complete, ordinary traversal reaches substantially farther into HWAY,
and the preserved product log contains no `SF2 guest runtime stopped` fault.
The user did encounter a conversation which did not release control; that
audio/script gate remains to be localized from the exact conversation.

One isolated texture defect remained after leaving the tunnel: a right-side
wall could sample high-contrast black-and-white noise while its geometry,
lighting, collision, and the rest of the scene stayed correct. The SF2 product
loop still selected one global texture bank from `native_residency.camera()`.
That camera and its active-room set never moved because the retail guest—not
the native shell—owns gameplay. The renderer therefore kept initial-room
texture provenance after the guest crossed later portals.

`GameplaySession::synchronizeGuestResidency` now advances only the native
active-room shell from the authoritative guest player coordinates. The SF2
loop calls it before each presentation, reconciles `TextureStreamer`, and
selects the draw bank at the guest position. Room/bank transitions are logged
with the guest coordinates so a remaining portal ambiguity can be diagnosed
from a user log.

The retained guest VRAM setup was also an append-only command history:
ordering-table A0 uploads were added forever and the entire history replayed
before every frame. It now represents current retained state. A newer upload
to the same destination rectangle replaces the older one and moves to the end,
preserving chronological ordering against overlapping rectangles without
unbounded duplication of repeated uploads.

The PsyCross Release product and probes build after these changes. All 21
CTest targets pass and `git diff --check` is clean. The standalone
`probe-sf2-mission-transition` still exits in its pre-existing continuous
frontend/MOVIE diagnostic before the mission transition; it does not exercise
the product-owned direct HWAY scene path or this residency change.

The first interactive run with that residency synchronization exposed two new
deep-traversal failures. At medium range, visible surfaces flickered in and out
even with a stationary camera. The product log records native presentation
residency transitions `27 -> 21 -> 20 -> 19 -> 20 -> 21`, all in bank zero.
Repeated `TextureStreamer::ensure` rebuilds at those boundaries are therefore a
credible presentation-side cause even though the retail guest owns the actual
ordering-table geometry. The synchronization is experimental, not validated.

The same run also allowed Gabe to fall through floor geometry and die. Native
presentation residency cannot mutate guest position or collision state, so
that is tracked separately as a retail room/collision-streaming failure in the
direct mission bootstrap rather than attributed to the texture-bank change.
Both defects are disclosed in the public alpha release notes.

### Disc 1 generalization and authoritative guest diagnostics

The experimental native-residency synchronization above was withdrawn after
the log proved that all six oscillating room estimates selected texture bank
zero. Those transitions came from an approximate native ground query, not
from retail room ownership, and repeatedly rebuilt the host atlas underneath
an otherwise stable guest frame. The product now seeds the native atlas once
and leaves streamed world residency to the guest's captured VRAM uploads and
ordering tables.

The retail current-room path is now independently mapped from the live
collision owner at `0x8012A654`, whose active object stores the room at
offset `0x1A0`. The resident collision room count and table are read from
`0x8011F660` and `0x8011F680`; each `0x40`-byte slot points to its loaded
collision record and collision list. Product logs report authoritative room
changes, record/list residency, player coordinates, and the latest
`WorldCollision_Scan` context. The deterministic probe additionally fails
open in diagnostics by counting every frame where a valid current room lacks
either resident pointer. Mission 3 crossed 7,000 neutral frames with zero
such gaps, including a retail checkpoint restore at frame 6,834. The reported
floor fall therefore remains a collision-state or position-transition defect,
not evidence of a missing room table/list. Checkpoint logs now preserve the
pre-restore position and collision context so another occurrence can be
localized.

The direct guest bootstrap is no longer hard-coded to Mission 3/HWAY. It
selects the requested Disc 1 mission in retail TITLE state, resolves the
mission resource and `FOG/<resource>.FOG` from the supported-game table, and
finds the `LEVEL` script through retail `MissionScript_FindByName` rather than
assuming a fixed script slot. Missions 1 through 8 (`COLO`, `AIRBASE`,
`HWAY`, `BRIDGE`, `AIRBASEX`, `TRAIN`, `TRAIN2`, and `WRECK`) all publish
valid guest frames with the expected selected-mission index, `LEVEL` program,
and resident collision state in short deterministic gates. An 800-frame
neutral Mission 1 gate reaches room 12 after the parachute opening with live
PCM, SPU voices, dialogue, and no checkpoint restore.

The conversation stall was not caused by a globally missing script completion
bridge. Retail speech polling at `0x8008D8A8` reaches script event 5 normally:
a 2,500-frame Mission 3 gate records paired speech starts, XA starts/stops,
and three event-5 dispatches. A bounded chronological timeline now records
speech start/stop, XA start/stop, and event 5 with guest frame, retail clock,
and arguments. The product emits new timeline entries live, allowing the
specific deeper cue which failed to release control to be distinguished from
the already-working generic completion path.

Disc 2 is not enabled by this generalization. Although both retail discs
share the executable hash and resident addresses, a Mission 9 probe reaches
executable initialization and frontend quiescence but fails inside the TITLE
frontend before mission handoff. The direct bootstrap still invokes
Disc-1-derived TITLE overlay entries, so the product remains explicitly gated
to Disc 1 until the Disc 2 TITLE overlay contract is mapped independently.

### Host-only HUD texture residency

Interactive testing tied the remaining right-wall corruption to selecting the
shotgun. The exact resident INTERFACE layouts explain the correlation: M16
layers occupy atlas rows 17–54, while the shotgun layers occupy rows 109–180.
The native overlay had relocated those TIM pixels into PS1 framebuffer page
zero, so selecting the shotgun issued raw image uploads through the visible
middle of the framebuffer. The apparent wall texture noise was HUD source
data leaking into presentation, not damaged guest world residency.

Four pages at the end of PsyCross's host-only texture alias pool are now
reserved for the complete 256x256-word HUD atlas and excluded from mission
texture allocation. HUD uploads target those alias pages, and every HUD draw
encodes the corresponding host page in TPAGE while retaining the authored
palette. No weapon or font pixel upload can touch either retail framebuffer.
The single SF2 sniper layer which crossed an alias-page boundary was repacked
into a non-overlapping slot, and construction now rejects any future crossing.

Because guest ordering tables cannot address host alias pages, the SF2 loop no
longer invalidates and re-uploads every font and weapon layer after every
frame; it refreshes only the guest-writable native palette row unless the HUD
state actually changes. SF2 pistol residency and drawing now share
`PISTOL2A/B`, and the newly safe full-height alias page enables
`KNIFEA/KNIFEB`, fixing the missing pistol and knife silhouettes. The Release
product smoke-boots through authored HWAY presentation with the alias atlas,
the VRAM-specific test passes, all 21 CTest targets pass, and
`git diff --check` is clean. White guest pickup rectangles remain a separate
retail-sprite upload/presentation issue.

For deep traversal testing, the SF2 product now exposes one process-local
whole-machine quick state: F5 captures and F9 restores it. The snapshot
includes CPU, RAM, scratchpad, MMIO, CD/SPU/device state, host file offsets,
callback clocks, and retained VRAM setup. Published ordering tables are
transient host presentation and are regenerated after load.
F9 clears queued host PCM and invalidates the native HUD cache before
continuing, avoiding audio or overlay state from the abandoned timeline. It
is deliberately in-memory only and is replaced by the next F5.

The `quickstate` product probe saves at one-third of a 1,200-frame run,
advances under mutated input, restores at two-thirds, and verifies the saved
retail clock, player coordinates, and room exactly before continuing to a
freshly published guest frame. The gate passes with live PCM, SPU voices,
scripts, and collision residency.

### HUD page-state isolation and floor-query tracing

The next interactive traversal reported severe medium-range
flicker/disappearance and two falls through the floor, while confirming that
F5/F9 worked within the process. The lack of cross-process persistence is
intentional for this first quick-state gate; the snapshot has no stable disk
format yet.

Native HUD drawing selects host-only alias TPAGEs, while retail `SPRT`
packets do not encode a TPAGE and may appear before the next guest E1
draw-mode packet. An initial attempt restored native PS1 page zero after the
overlay; later interactive evidence below proved that a guessed TPAGE is not
enough because the overlay also replaces clip, offset, format, and display
drawing state.

The remaining half-white knife icon had an independent exact cause.
`KNIFEB.TIM` occupies source rows 240..255; the host FT4's exclusive bottom
UV became 256 and wrapped to zero in the byte-sized PS1 coordinate. That layer
is now packed at `(192,32)` in the otherwise unused fourth host HUD page, so
its entire sampling interval stays within one page.

The authoritative player call to `WorldCollision_Scan` at return address
`0x80084E7C` is now instrumented through its request's result-byte pointer.
Diagnostics count both byte values and the current/maximum zero-result streak.
The product logs any streak of at least 24 samples with player position, room,
and collision residency, and checkpoint logs include the complete floor-query
totals. A 7,200-frame neutral baseline crossed the retail restart at frame
6,834 with all collision rooms resident, zero residency gaps, 1,089 player
floor probes, and a maximum zero-result streak of 16. This supplies a bounded
comparison for the next real fall instead of guessing from the generic
collision-call count.

PsyX's fixed scene-test log is now copied to a timestamped file under `logs/`
before the next scene-test launch, so a short follow-up session cannot
overwrite the only useful crash or fall trace. The Release product/tool build,
all 21 CTest targets, and `git diff --check` pass.

### Exact draw-environment restoration after quick load

The next interactive run confirmed the knife relocation and exposed the
presentation defect unambiguously. Pressing F5 produced a vertically split,
red-tinted frame; after F9, presentation alternated between a complete world
and a black frame containing only HUD, with some UI doubled. A guest status
string also rendered the `M` in `M-16 Ammo picked up` as a white block. The
screenshots prove that gameplay RAM and native HUD remained live while the
world pass targeted or sampled the wrong presentation state.

The native HUD pass used `PutDrawEnv` to install its top-left environment.
Restoring only page zero afterward discarded the retail clip rectangle,
centre-origin draw offset, texture format/page, dithering, and draw-to-display
state. Ordering tables whose first primitives preceded their next complete
E1/E3/E4/E5 sequence therefore inherited the native HUD environment. This
accounts for the black alternation, split frame, duplicated pixels, and
missing guest glyph through one state leak.

`drawSf2GuestHud` now snapshots the complete active guest `DRAWENV` after the
world pass, uses an isolated environment for native HUD primitives, and
restores the exact guest environment after `DrawSync`. F9 also no longer
restores a saved `shared_ptr` to an old published ordering table. That object
belongs to the abandoned host framebuffer generation; restored guest RAM now
publishes a fresh authored OT before presentation resumes.

The revised 1,200-frame quick-state gate restores the exact retail
clock/player/room, republishes 1,258 live draw commands, retains nonzero SPU
PCM and scripts, and reports no collision-residency gap. The Release build and
all 21 CTest targets pass. Interactive F5/F9 validation remains the final gate
for this correction.

### Resident renderer overwrite caught outside quick state

The first run of the corrected draw-environment build crashed during ordinary
gameplay without using F5 or F9, proving that this fault is independent of
quick-state restoration. The product log stopped at `0x8001E55C` with fetched
word `0x0316964C` and return address `0x8001D018`. Disc executable text expects
`0x03CB5804` at that address. The high byte remained `0x03` while the low 24
bits became the RAM address `0x16964C`: exactly the mutation performed when a
PS1 ordering-table link treats executable text as a primitive packet.

The existing write watch retained only one hit until the next GPU-submission
boundary. It also watched a second resident range containing a known benign
callback self-patch; that write could occupy the single slot and mask a later
renderer overwrite in the same execution slice. Protection is now limited to
the repeatedly corrupted `0x8001D000..0x8001EFFF` text range. Once bootstrap
has completed, a write-watch hit becomes an immediate interpreter breakpoint
before the following guest instruction can fetch damaged text. The existing
SF2 boundary diagnostic will therefore report the exact writer PC,
instruction, destination, value, frame, and restore count on the next
occurrence rather than the later victim syscall.

A 2,500-frame combat gate produces no false text write, live PCM and scripts
continue, the Release product/tool builds, and all 21 CTest targets pass.
Interactive traversal is required to exercise the path that supplies the bad
primitive pointer.

### Resident renderer integrity repair reproduces the combat corruption

The next interactive run stopped immediately when the M16 fired at
`0x8001D63C`, where disc text contains `0x000D0E02` but guest RAM contained
`0x001697FC`. This is a second resident-renderer victim with the same
ordering-table-link signature. A breakpoint-only write watch still did not
identify the writer, so the runtime now keeps the verified post-bootstrap
8-KiB renderer image and compares it before each guest execution slice.
Changed words are restored individually and exposed through product/tool
diagnostics. This is a narrow fail-safe while the malformed OT owner is
located, not a substitute for fixing that owner.

Retail legitimately clears the callback trampoline word at `0x8001DA9C` on
first use, sometimes two boundaries after bootstrap. The integrity mechanism
adopts that one proven `0x01204009` to zero transition and splits the write
watch around its four-byte address; every other renderer word remains
protected.

A 4,000-frame combat gate then reproduced the actual corruption without
crashing: `0x8001D5DC` changed from executable word `0x08007553` to
`0x081697B0`, whose low 24 bits are another RAM/OT-style link. The guard
restored the instruction, reported one repair, and completed all 4,000 frames
with live gameplay, collision, scripts, and PCM. This supports the user's M16
correlation: firing enters the affected rendering/effect path, though it does
not yet prove that weapon code creates the bad link.

### Objective overlay flicker and post-F9 false exit

Interactive completion of the first objective produced alternating complete
world frames and cleared frames containing only the native HUD. The product
log showed no new renderer-text repair before the flicker. Retail ordering
tables rely on persistent PS1 GPU E3/E4/E5 state and do not repeat the draw
area/offset on every submission. `beginSf2GuestFrame` selected a top-left
environment to clear the collapsed host framebuffer but left that environment
active for the following guest list. Complete lists re-established the retail
centre-origin environment; shorter objective/HUD lists inherited the host
environment and drew the world offscreen. The clear now snapshots the active
guest `DRAWENV`, clears through a temporary native environment, and restores
the guest environment before OT replay.

The same run pressed F9 after the flicker. Guest RAM restored successfully,
but `presentation_frame_` was intentionally empty while the old host OT was
retired. The first restored display submission was a utility list, so
`advanceHostUpdate` returned false with no runtime fault and the scene treated
that as a crash. It now continues through bounded guest submissions until a
fresh authored world frame is published.

As an independent guard, captured ordering tables that traverse
`0x8001D000..0x8001EFFF` are rejected and leave the last known-good world
frame active. Such a packet address can only be the proven malformed OT/code
alias, never a legitimate renderer primitive. Product diagnostics log the
root, packet, guest frame, and rejection count. The 1,200-frame F5/F9 gate,
4,000-frame combat gate, Release build, and all 21 CTest targets pass;
interactive objective-overlay validation remains required.

### Same-slice OTC overwrite and transient collision-room loss

The next interactive traversal remained unstable after the objective event,
nearly fell through the world, and finally crashed after F9 at `0x8001E5BC`
with word `0x001696F8`. The log also recorded several
`21 -> 0xFFFF -> 21` collision-owner room transitions and cleared both the
room record and collision-list pointer while Gabe remained within the same
tunnel region. This connects the reported near fall directly to retail guest
state rather than presentation geometry. Hundreds of PsyCross
`Unhandled zero length 70 primitive` messages began after a later mission
event, providing a second malformed-OT symptom.

The between-boundary renderer repair cannot protect against OTC DMA that
writes a link and reaches the damaged function before the next public
submission. `R3000Runtime` now has opt-in write-watch suppression: watched
guest stores still complete architecturally and retain the first writer
diagnostic, but protected RAM is not changed. SF2 enables it only for the
verified resident-renderer ranges split around retail's legitimate
`0x8001DA9C` callback self-patch. Boundary diagnostics consume a suppressed
write as a renderer repair instead of terminating the scene.

Mission 3 also now retains the immediately preceding valid retail collision
room when the collision owner transiently reports `0xFFFF`. Any subsequent
valid resolver result replaces the fallback; no approximate host geometry is
introduced. The count and retained room are exposed in product/tool logs and
are re-seeded from restored guest RAM after F9.

A 1,800-frame F5/F9 gate completed with two repeated renderer writes
suppressed. A 5,200-frame combat gate completed through a retail checkpoint
restart with one blocked renderer overwrite, 43 transient room fallbacks, no
collision-residency gap, and a post-restart maximum floor-miss streak of
three. All 21 CTest targets pass, and direct write-watch suppression has a
dedicated R3000 regression test. Interactive shotgun/objective traversal is
the next gate.

### Truck interaction isolates malformed renderer vertex input

The next interactive run completed the first truck objective, skipped its
short cutscene with crouch, and crashed while moving back toward Chance. The
renderer-text suppression remained effective: executable word
`0x84990010` at `0x8001DC84` was intact. It faulted because `a0` was the
unaligned, unreadable value `0xFFFF0823`.

Disassembly identifies `0x8001DA48` as a large world-object projection
routine. Its inner loop at `0x8001DC50` reads a sentinel-terminated array of
vertex-pair pointers; `0x8001DC84` then loads the first screen-space halfword
at pointer offset `0x10`. The runtime now observes the exact list-read
instruction, validates all six halfwords consumed from each candidate, and
replaces only an unreadable candidate with retail's `0xFFFFFFFF` terminator.
Diagnostics retain the list cursor and rejected pointer. A 4,000-frame
ordinary combat gate produced zero rejections, establishing that valid RAM
and scratchpad vertex pairs are not filtered.

The repeated PsyCross `Unhandled zero length 70 primitive` output is also
classified. Retail fixed-size TILE opcodes legally use the low raw-texture
bit even though TILE ignores it; PsyCross's dispatcher masks semitransparency
but not that bit and rejects `0x71`/`0x73`. SF2 replay now constructs TILE,
TILE_1, TILE_8, TILE_16, SPRT_8, and SPRT_16 primitives explicitly and
normalizes only TILE's ignored raw-texture bit. Objective/status lists no
longer enter the generic parser for these commands.

`sf_tool` gained diagnostic-only authored event injection, program activation,
and player-position APIs. Its `objective` mode activates `CHANCE_PINNED` and
dispatches the mapped event-10 selector `0x11E`, whose action stream contains
the retail action-`0x29` interaction starter for completion selector `0x32`.
The script surface executes deterministically, but the complete truck grant
still requires live object-interaction context and room residency; directly
injecting the event is intentionally not treated as equivalent proof.

### Real truck-object gate and renderer call-edge repair

The authored selector probe reproduced the truck speech scene but, as
expected, did not award inventory or armor because it bypassed the live
vehicle object. The `OBJ_VEH` import stub resolves through the dynamically
loaded module at `0x80010000`, but tracing the selector alone was not a useful
playability gate.

`probe-sf2-product-runtime ... objective` now performs a real retail
traversal instead. A feedback controller follows coordinates captured from
the successful interactive route through rooms 27, 21, 20, and 19, presses
the retail Action button at the truck, detects the guest-owned inventory
transition, sends Cross to skip the short scene, and begins navigating back
toward Chance. Diagnostic-only health retention keeps hostile fire from
making the route nondeterministic; it does not change product gameplay.

That route first reproduced the latest crash at frame 2,198. OTC had replaced
the retail NOP at `0x8001DC5C` with link word `0x0016940C`; decoding it as a
MIPS syscall stopped the guest before the next public GPU boundary could run
the existing integrity repair. The world renderer is now observed at its
`0x8001D010` call edge. It compares and repairs the verified immutable
8-KiB image immediately before executing the renderer, closing the
same-slice DMA-to-instruction-fetch window while retaining all attempted
write diagnostics.

With that guard active, the same route repairs the attempted overwrite and
continues. The 3,300-frame objective gate reaches within 14 units of its
retail route, loots the truck at frame 2,574, observes owned items changing
from `0x00100000` to `0x00100114`, sends the cutscene-skip input, and remains
stable for another 726 presentation frames during the return attempt. It
also retains live PCM, SPU voices, scripts, collision, and PAD cadence with
no checkpoint restart or renderer-vertex rejection. Unlike the earlier
authored-event probe, failure to observe the real inventory transition now
fails the objective gate.

Final Release validation passes:

- the 3,300-frame real-object gate loots at frame 2,574 and survives through
  frame 3,300 with zero checkpoint restores or rejected vertex entries;
- the 4,000-frame combat gate retains live PCM, SPU, scripts, collision, and
  PAD cadence;
- the 1,800-frame F5/F9 gate restores the guest clock, player, room, audio
  generation, and fresh authored presentation;
- the full `syphon_filter.exe` Release target links; and
- all 21 CTest targets pass.

### Post-objective endurance, exact texture residency, and collision call edge

The real truck-object controller was extended to 5,000 presentation frames.
It still loots the authored vehicle at frame 2,574, skips the interaction
scene, and remains live for more than 2,400 additional submissions. Retail
PCM/SPU, scripts, PAD sampling, room collision, and world publication all
continue, with no checkpoint restore, malformed OT rejection, or invalid
vertex entry.

That longer run confirms three renderer-text aliases at deterministic frames
615, 1,395, and 1,677. Each replacement is shaped like an ordering-table
header/link (`0x081697B0`, `0x00169604`, and `0x01169AA4`). The immutable
renderer guard repairs all three and the product remains stable, but their
upstream allocator/source is not yet classified. Diagnostics now retain the
attempted writer PC/instruction when the scalar write watch observes one.
Host bulk writes also honor write-watch suppression, with a dedicated R3000
regression, so protected executable RAM cannot be bypassed through
`loadBytes`.

The GP0 side-effect scanner previously advanced one word at a time. Polygon
payloads and upload pixels can contain command-looking high bytes, allowing
the scanner to invent a false command boundary and miss later VRAM state.
It now parses complete fixed, variable polyline, copy, and upload command
lengths and buffers incomplete uploads across guest boundaries. Meaningful
VRAM-to-VRAM copies can be retained; retail's observed two-pixel `(0,0)` to
`(0,0)` synchronization no-op is not replayed as permanent setup state.
Unit coverage includes a 12-word textured quad with command-looking payload
bytes, a copy, and complete/incomplete uploads.

The native seed texture atlas had been frozen to the opening room to avoid an
earlier coordinate-based residency oscillation. That prevented the flicker
but also left later tunnel rooms using the opening residency, matching the
reported white pickup boxes and intermittent wall texture corruption. The
frontend now follows the guest's exact retail collision-room ID rather than
guessing from overlapping native bounds. A room must remain stable for six
60 Hz submissions (two retail logic ticks) before `TextureStreamer` changes
the active room/bank, preventing boundary chatter from thrashing the atlas.

Collision-room stabilization also runs at the retail world-scan call edge.
A transient `0xFFFF` resolver value can otherwise be published and consumed
within one guest slice, before the public display-boundary repair runs. The
scanner now sees the immediately preceding valid retail room in that window;
the next valid retail result still replaces it.

Release validation after these changes:

- the 5,000-frame real truck-object gate loots at frame 2,574 and remains
  stable through frame 5,000;
- the independent 4,000-frame combat gate passes;
- the 1,800-frame F5/F9 gate restores guest machine and presentation state;
- the full PsyCross Release product links; and
- all 21 CTest targets pass.

The exact-room texture change is presentation-only and still needs visual
confirmation in the next interactive session. The remaining deterministic
renderer-link writes are contained and logged, but finding their allocator
root remains the highest-priority integrity investigation.

### Ordering-table escape root cause and retail LoadImage handoff

High-resolution execution slicing localized the first renderer mutation to
the second `AddPrim` call returning to `0x800135C0`. The retail renderer
passed OT entry `0x8021D5DC` and primitive `0x001697B0` to the helper at
`0x80013000`. Because PS1 main RAM is mirrored every 2 MiB, that OT address
aliases physical `0x0001D5DC` and overwrote the resident instruction at
`0x8001D5DC`.

The escaped address is not an unexplained DMA writer. The active table base
was `0x801F8C6C`, its verified bucket count was 16, and the renderer used raw
bucket `0x925C`:

`0x801F8C6C + 0x925C * 4 = 0x8021D5DC`.

The host now observes only the two renderer `AddPrim` call sites
(`0x8001354C` and `0x800135C0`). It validates the requested entry against
the table base selected through the retail GP-relative table and the live
bucket count in `s6`; an escaped entry is clamped to the last valid bucket.
The 5,000-frame truck route records three clamps and zero renderer-text
repairs. The temporary 10-instruction integrity sampler and repeated 8-KiB
text comparisons were removed, restoring the normal 50,000-instruction
scheduler slice.

The additional retail-runtime handoff independently identifies the room
texture path:

- `0x800A21B4(room)` activates the room's 32-page mask and bank;
- `0x800A212C(page, bank)` queues a missing page;
- async callback `0x800A1B44` updates residency and uploads the page; and
- retail `LoadImage` enters at `0x800F1598`.

Instrumentation confirms this exact chain on the deterministic route. By
frame 5,000, room activation has requested page 4/bank 0, the async callback
has completed, and the next `LoadImage` has arrived. The bridge now captures
bounded `LoadImage` rectangles and payloads at that HLE boundary and retains
the latest upload for each destination rectangle. This recovers GPU uploads
which travel through retail's direct/DMA path and were invisible to the raw
GP0 word scanner. The same run retained 544 valid retail uploads across 207
current destinations.

A 10,000-frame objective endurance run then:

- looted the real truck at frame 2,574;
- crossed the previously failing post-objective interval;
- entered retail death/checkpoint restoration at frame 6,588;
- resumed gameplay and room traversal after the restart;
- recorded no renderer repair, malformed OT, invalid vertex entry, or
  collision gap; and
- retained live PCM, SPU, scripts, PAD sampling, room activation, and async
  streaming.

The new upload handoff still needs interactive visual validation. Native
presentation currently replays retained retail VRAM state alongside its
existing relocated mission residency, so the next test should specifically
check the truck objective, shotgun selection, pickup boxes, wall textures,
HUD glyphs, and post-death texture state before the older native
`TextureStreamer` fallback is removed.

Independent regression gates also pass: the 4,000-frame combat route and the
1,800-frame F5/F9 route retain deterministic presentation, audio, scripts,
PAD cadence, and collision with zero renderer-text repairs or malformed
presentation lists. All 21 Release CTest targets pass, and the full PsyCross
product links.

### Retail TIM ownership and relocated CLUT updates

LoadImage caller accounting classifies the initial 545 calls:

- 263 calls at `0x800257C4` upload a TIM pixel rectangle;
- 255 calls at `0x80025818` upload the same TIM's optional CLUT;
- two calls at `0x800C2334` upload a generated/interpolated CLUT;
- seven calls at `0x800C29F4` upload transformed texture pixels; and
- the remaining calls are bounded executable/overlay upload sites.

The current retained state contains 543 valid uploads and 296,377 halfwords.
Its destination-page mask is `0xFFCFFBC0`: no rectangle overlaps native
framebuffer pages 0..5 and none is a fullscreen image. Retaining the TIM
uploads is therefore required for the previously white UI, weapon, pickup,
and effect atlases and does not reintroduce the old backbuffer-persistence
failure.

Six retained rectangles target retail's ZCLUT source rows, including the full
`(768,480,256,32)` block and dynamic one-row updates. Native mission residency
moves those rows to framebuffer-safe storage beginning at `(0,192)` and
relocates guest CLUT selectors to match. The direct LoadImage replay now
mirrors each source row through the live texture-bank row map into that same
native residency. A focused VRAM regression covers full and partial rows plus
all source-boundary rejection cases.

The exact post-change gate set passes:

- 10,000-frame truck objective, including loot at frame 2,574 and retail
  death/restart at frame 6,588;
- 4,000-frame combat;
- 1,800-frame F5/F9; and
- the focused PsyCross VRAM test.

All gates report zero renderer-text repair, malformed OT, invalid vertex
entry, or collision gap. Interactive confirmation remains required for the
visual effects of TIM/ZCLUT restoration.

### Directional retail weapon selection and PC quick slots

The product input adapter previously mapped middle click, both bracket
directions, and every wheel notch to the same single Select edge. Wheel sign
was discarded, and the `1..9,0` quick-key array was never consumed. This made
the documented previous and direct-selection controls inaccurate even though
the retail Change Weapon action itself worked.

`probe-sf2-product-runtime ... weapons` now follows the real truck route,
waits for the authored loot/cutscene boundary, and injects twelve
sample-separated retail Select edges without writing guest inventory. The
observed equipped-item sequence is:

`20 -> 2 -> 4 -> 8 -> 20`

and repeats exactly for three revolutions. With the post-truck ownership mask
`0x00100114`, this proves the ascending owned-item ring for knife, pistol,
M16, and shotgun in Mission 3.

The PC adapter now computes a forward Select-edge distance from the live guest
equipped item and ownership words. Next advances once; previous wraps in the
opposite direction; wheel sign and magnitude are preserved; and number keys
target the corresponding owned, supported retail item. Missing slots are
ignored. The sampled press/release queue is unchanged, so every synthesized
command still crosses the retail 20 Hz PAD boundary and all actual selection,
draw, ammunition, and inventory mutations remain guest-owned.

Focused tests cover signed wrap, direct slots, selecting the current weapon,
missing inventory, and the bounded sampled pulse queue. `sf_tests` and all 21
Release CTest targets pass. The PsyCross backend compiles; the final product
relink and interactive direction/number-key check remain pending until the
currently running frozen test process exits.

The same long interactive run remained live beyond 29,000 guest submissions,
crossed retail death/restart, multiple F5/F9 restores, the previously blocked
deep dialogue sequence, and rooms 27 through 5 without a guest fault. Its
audio diagnostics did expose repeated callback starvation. The old FIFO-bound
failure is gone; this was a smaller cadence mismatch: OpenAL began consuming
after one 10 ms device update while SF2 supplies 735 PCM frames only once per
16.7 ms presentation update. The SF2 product sink now waits for 12 128-frame
blocks (about 35 ms) before initial start or starvation recovery. The bounded
half-second ring and byte-exact guest PCM ordering are unchanged.

The user then completed the same uninterrupted traversal. It advanced through
the later dialogue chains and rooms 5, 4, and 3; at guest frame 32,940 retail
issued its final XA stop sequence, cleared the player and collision roots, and
transitioned room `3 -> 0` with player position `(0,0,0)`. The process did not
fault. This is the first interactive end-to-end completion of Mission 3 in the
guest runtime. Interactive observation confirms that the retail save menu
appears at that transition and the authored post-mission cinematic follows.
In-session F5/F9 quick save/load also remained reliable throughout the run.

The remaining Mission 3 defects are now bounded enough to separate from basic
playability:

- collision or room residency can still become intermittently unstable; one
  crouch/backward traversal fell through the floor and forced a death restart;
- HUD state appears several seconds after initial gameplay and after retail
  checkpoint restart rather than being ready on the first playable frame;
- the explosives-truck dialogue has uneven pauses and may still stall if the
  player does not skip it; and
- some UI/TIM elements remain incomplete even though additional elements can
  appear while presentation is in a corrupted or duplicated state.

Despite those defects, Mission 3 is now verified playable and finishable from
its in-engine opening through gameplay, objectives, dialogue, combat,
checkpoint death/restart, mission completion, save flow, and the following
cinematic. This makes other mission packages reasonable interactive-test
candidates after the collision/residency and dialogue timing paths receive
focused deterministic gates.

### Captured room-14 fall and floor-request repair

The completion-run log contains the first complete fall-through trace. Gabe
entered room 14 normally near `(-2097,-105,-7155)`. At approximately
`(-3159,-81,-5544)`, the collision owner began publishing `0xFFFF`; gravity
then accelerated Y through `3130` while X remained fixed and Z advanced only
eight units per observation. Retail death/restart followed. This distinguishes
an actual fall from the long zero-result streak seen while scripted dialogue
held an otherwise stationary player.

The existing fallback rewrote only the collision owner's room field. The
verified player call at `0x80084E7C` passes a separate world-scan request whose
room lives at offset `+0x0C`; that request could remain `0xFFFF`, causing
`WorldCollision_Scan` to see no starting room even after the owner was
repaired. The call-edge guard now replaces only a `0xFFFF`/`0xFFFFFFFF`
player-floor request with the immediately preceding valid retail room. A
separate diagnostic counter records these request repairs.

All 21 Release tests pass. The 10,000-frame real truck/checkpoint endurance
route remains bit-stable before and after the change and reports zero request
repairs, as expected because it does not reach the captured room-14 edge.
Interactive traversal through that exact area remains the authoritative
validation for the new guard.

### F5/F9 cadence divergence and poisoned renderer-list recovery

The release-candidate interactive run exposed a delayed F9 failure. Loading a
state saved in room 17 immediately produced alternating world presentation,
then retail's list merge at `0x800F4B00` followed a `0x005A5A5A` packet link
and alignment-faulted at `0x800F4B54`. This was not an async CD pointer or a
native-only framebuffer failure: the value was a poisoned/freed guest
ordering-table link.

The original quick-state probe checked only the restored clock, player
position, and room. A new replay gate now saves at a public presentation
boundary, records the exact host PAD transcript for 3,332 updates, restores,
replays that transcript, and compares all 2 MiB of guest RAM. The first run
failed. Its earliest differences included the retail retrace and XA clocks,
and the replay was three retraces ahead.

The cause was `restoreQuickState()` discarding the saved immutable
`presentation_frame_`. `advanceHostUpdate()` consequently consumed extra
guest GPU boundaries while searching restored RAM for a new authored world
OT. That changed retail timing immediately, explained the visible black/world
alternation, and eventually changed renderer allocation/list lifetime. Quick
states now retain and restore the visible presentation frame. This gives the
native backend a coherent frame immediately and preserves the exact retail
boundary cadence.

`PsxMachineState` now also preserves any in-flight GP0 word tail. A narrow
guard at retail's list merge validates only its 24-bit RAM link chain; if a
future list is already poisoned, the runtime skips that one object merge
instead of allowing retail to dereference an invalid physical address.
Healthy lists execute unchanged in guest code, and diagnostics retain the
descriptor, root, cursor, and tag for further root-cause work.

Two independent 10,000-update gates now pass:

- the ordinary F5/F9 route replays 3,332 forward-input updates with a
  byte-identical full-RAM result; and
- the real truck-objective route saves after authored loot, replays its exact
  PAD transcript byte-identically, and survives two retail
  death/checkpoint-restoration cycles.

Neither route required the malformed-list recovery guard. The guard therefore
remains a last-resort containment path for the exact interactive poison
signature rather than part of normal rendering.

### Disc 1 mission matrix and AIRBASE sound-bank containment

Work continued beyond the interactively completed I-70 mission. All eight
USA Disc 1 resources (`COLO`, `AIRBASE`, `HWAY`, `BRIDGE`, `AIRBASEX`,
`TRAIN`, `TRAIN2`, and `WRECK`) now reach a valid retail gameplay
presentation with player, world, collision, scripts, SPU, and PCM state.
Exact quick-state replay also passed for Missions 1 and 4–8. Mission 2 was the
only new deterministic failure.

AIRBASE neutral input remained stable, but sustained forward input
reproducibly alignment-faulted at `0x800FA750`. Disassembly identifies
`0x800FA720` as a sound-bank variant selector: it indexes an eight-byte cue
table and returns matching 24-byte variants. The failing bank at
`0x801EFFBC` retained the `SBNK` magic and remained linked between AIRBASE's
other banks, but its PL02 payload fields had already been repurposed before
the first public gameplay update. In particular, its cue-table pointer was
unaligned and pointed into unrelated text/data. The equivalent PL03 bank in
HWAY retained a complete, aligned header and table.

The runtime now validates only the table and variant pointers consumed by
this selector. An unreadable table returns the routine's existing empty
selection result rather than allowing retail to issue an unaligned load.
Valid banks continue entirely in guest code. Diagnostics count rejections and
retain the bank, table, and cue index; this preserves the evidence needed to
recover AIRBASE's exact PL02 lifetime later instead of silently rewriting the
bank.

Validation after the containment:

- AIRBASE 10,000-update quick-state replay passes with byte-identical full
  guest RAM after replay and survives repeated retail restart cycles;
- AIRBASE 5,000-update forward and combat stress routes complete without a
  guest fault; and
- HWAY's 3,000-update exact quick-state regression passes with zero rejected
  sound-bank lookups.

The missing AIRBASE player-bank cues remain an audio fidelity gap, but the
mission is no longer blocked from interactive play by the stale retail
resource.

An actual PsyCross product-window smoke test then held forward from the
starting cell. Lian moved through the open doorway and the captured frame
contained the correctly textured hospital gown, walls, floor, and door
geometry. This confirms that the fix reaches the shipped scene path rather
than only the headless probe. A desktop Mission 2 test shortcut now launches
that exact path for interactive progression testing.

The remaining Disc 1 packages also completed 5,000-update combat/input stress
runs. BRIDGE and WRECK crossed repeated retail checkpoint restores; AIRBASEX,
TRAIN, and TRAIN2 stayed in uninterrupted gameplay. None produced a guest
fault or a rejected sound-bank lookup. The product log now reports any future
stale-bank rejection with the bank, table, cue index, player position, and
room so an interactive failure retains the same evidence as the probe.

### WRECK clean-start parachute handoff and collision-probe correction

WRECK originally appeared to have a catastrophic collision failure: Gabe
fell from the authored parachute camera and retail restored the checkpoint
every 129 submissions. A neutral 5,000-update run repeated that cycle 38
times. The failure was deterministic, but it was not evidence that the
mission floor was absent.

`WRECK.OVL` has a 40-byte file header and maps its payload at `0x8014B978`.
Its mission initializer at `0x8014BA84` reads the checkpoint-present flag at
`0x8011F61C`. On a clean start it dispatches event `0x72` to the player,
which arms the parachute actor/callback path. On a checkpoint restore it
intentionally skips that event. The direct TITLE-to-mission bootstrap had
forced the flag to one before loading every mission, so WRECK always entered
the restore branch even though no authored checkpoint existed.

Fresh mission handoff now writes zero. Retail's normal post-initialization
checkpoint capture remains responsible for setting the flag once a real
checkpoint is ready. With that change, WRECK dispatches its landing event,
hands control over at guest frame 427, and completes a 1,000-update neutral
run with no restore. The player lands in room 0 and continues moving on the
authored terrain. All eight Disc 1 missions also pass a 1,000-update neutral
clean-start sweep with zero bootstrap restores.

This investigation also invalidated an earlier diagnostic assumption. The
WorldCollision call returning through `0x80084E7C` is reached by shared actor
scans; it is not an authoritative player-floor probe. The later experimental
return site was likewise insufficient to classify the request as Gabe's
floor query. The request-level room rewrite and the true/false "floor" probe
were therefore retired. The owner-room repair remains only for Mission 3,
where the room-14 `0xFFFF` transition was captured directly. Other missions
can legitimately publish no current room during authored traversal states,
including WRECK while airborne.

Post-correction regressions:

- HWAY passed a 3,000-update exact F5/F9 replay and the 5,000-update
  quick-objective/truck path with no fault or checkpoint restore;
- AIRBASE passed 3,000 updates of combat input, including a retail
  death/checkpoint restart; and
- WRECK passed its neutral parachute/landing route. Its synthetic combat
  driver can subsequently walk the player off terrain and trigger normal
  retail restarts, so that path is not used as a clean-start assertion.

### Disc 2 campaign handoff, LABS2 audio containment, and SPU save states

The USA Disc 2 executable is now accepted by the product guest runtime. Its
resident `INIT.OVL` is byte-identical to Disc 1, but its frontend file bridge
must resolve `MOVIE2.HOG`; the previous hard-coded `MOVIE1.HOG` lookup
terminated TITLE before mission handoff.

The PsyCross scene dispatcher also retained an older Disc 1-only condition:
SF2 mission indices 8 and above fell through to the archived native
reconstruction even after the guest runtime itself supported them. The
dispatcher now routes every SF2 mission package through
`runSf2GuestScene`. A shipped Release executable smoke test launched Mission
9 from Disc 2, logged the retail TITLE-to-mission guest path, entered room 0
at `(135,-883,1624)`, and remained live until the host ended the smoke test.

Disc 2 also exposes two different mission number spaces. Player-facing
campaign indices 8–20 map to retail `MissionArchive_Open` slots:

| Campaign index | Resource | Archive slot |
|---:|---|---:|
| 8 | `DISCO` | 10 |
| 9 | `DARKMUSE` | 8 |
| 10 | `MOSCOW` | 11 |
| 11 | `MOSCOW2` | 12 |
| 12 | `MOSCOW3` | 13 |
| 13 | `GARAGE` | 20 |
| 14 | `GULAG` | 14 |
| 15 | `GULAG2` | 15 |
| 16 | `LABS1` | 16 |
| 17 | `LABS2` | 17 |
| 18 | `SLUMS` | 18 |
| 19 | `SLUMS2` | 19 |
| 20 | `CHINBOSS` | 9 |

Calling `MissionArchive_Open` directly initially appeared sufficient, but it
bypassed the retail application-state lifecycle and processed sound resources
twice. That produced self-linked sound lists and invalid animation pointers.
The final path instead writes the mapped archive slot and invokes the same
TITLE mission wrapper as Disc 1. DISCO then reaches state 0 with its expected
player position, scripts, collision, rendering, PCM, and SPU state. Its
3,000-update neutral, combat/death/restart, and exact F5/F9 routes pass.

A full neutral sweep reached live gameplay for all thirteen Disc 2 packages.
LABS2 alone exposed a later deterministic fault. A released sound variant at
`0x801F6B8C` had been repurposed by the sequence player; writes from
`0x800FE380` replaced its voice byte, and the later pitch update passed values
such as `0xD0` to `0x801058AC`. That retail routine assumes one of the PS1's
24 physical voices and otherwise writes beyond the SPU MMIO voice bank. The
host boundary now rejects only indices 24 or greater, records the value and
caller, and leaves all valid guest updates native. LABS2 subsequently passed
3,000-update neutral and combat routes, each crossing its retail restart,
with three contained stale updates and no guest fault.

The same package revealed why some late F9 attempts could reject an otherwise
healthy save. A high-pitch SPU voice can consume its final ADPCM sample and
become inactive before the remaining phase is reduced below one sample
period. The dormant accumulator is harmless and key-on resets it, but the
snapshot validator previously applied the active-voice phase bound to every
voice. Validation now requires the bound only while a voice is active.
LABS2's 3,000-update exact quick-state route now restores successfully across
two retail checkpoint cycles. A focused SPU regression covers both the valid
dormant phase and the still-rejected active out-of-range phase.

Final regression results:

- all 18 Release CTest targets pass;
- all thirteen Disc 2 missions pass 3,000-update combat/input stress routes;
  nine cross at least one retail restart and none fault;
- all thirteen Disc 2 missions pass 3,000-update exact F5/F9 replay routes;
  Missions 12, 14, and 16–18 replay through retail restart activity without
  divergence;
- HWAY passes independent 3,000-update combat and exact F5/F9 routes;
- WRECK passes its 1,200-update neutral parachute/landing route;
- LABS2 passes 3,000-update neutral, combat, and exact F5/F9 routes; and
- the Release PsyCross product executable rebuilds successfully; and
- the shipped Release scene path boots Disc 2 Mission 9 through the guest
  runtime rather than the archived native fallback.

A second shipped-product smoke ran LABS2 for 50 seconds. The product log
recorded exactly the three deterministic stale voice indices from the
headless gate (`208`, `215`, and `68`), then retail triggered and completed a
checkpoint restart. The player reacquired room 0 and the process remained
live until the host ended the smoke test.

## Product-renderer F5/F9 gate — 2026-07-31

The exact guest quick-state probes did not cover PsyCross's host-side VRAM
mirrors. A new opt-in product harness accepts `SF2_QUICK_SAVE_FRAME` and
`SF2_QUICK_LOAD_FRAME` alongside the existing `SF2_CAPTURE_FRAME`, allowing
the shipped scene path to press F5/F9 and capture a deterministic presentation
frame without manual input.

The deterministic route did not reproduce the user's deep-traversal flicker.
With HWAY saved at presented frame 1,200 and loaded at 1,800, captures 1,900
through 1,903 all contain the same complete authored scene. They use the same
immutable guest publication (sequence/frame 548, 1,276 packets, 996 draws),
and the pre/post-residency capture at parity 1,901 is byte-identical. This is
useful negative evidence: an early, same-room F9 is stable, so the reported
failure needs a later room/texture transition in its reproduction route.

The audit did expose one real omitted owner even though this short route does
not visibly trigger it. F9 restored guest RAM and the retained GP0 upload
journal, but left PsyCross's CPU/GL VRAM mirrors containing room, palette, and
copy-source texels written after F5. Quick load now synchronizes the restored
retail room, invalidates the host texture streamer, uploads its authored base
residency again, and only then replays the restored retail journal. This makes
host texture state obey the same rollback boundary as guest state. HWAY
captures 1,901 and 1,903 remain complete after the change, as does Disc 2
Mission 9 (`DISCO`) at parity 1,901. A later interactive or automated
cross-room F5/F9 route remains required before calling the original flicker
closed.

`PsyX_TakeScreenshot` itself had been reading the unresolved multisample draw
target and produced black BMPs. PsyCross now resolves and reads its native
framebuffer explicitly and flips OpenGL's bottom-up rows before saving. This
turns the product harness into a usable visual regression boundary rather
than a packet-only assertion. A later HWAY capture at presented frame 3,000
(retail clock 417) shows the complete gameplay world, player, snow, upper-left
health overlay, and knife presentation after the camera/HUD handoff.

Validation:

- all 18 Release CTest targets pass;
- HWAY passes the 3,000-update exact guest quick-state replay;
- HWAY's post-F9 parities 1,900–1,903 capture complete scenes through the real
  Release PsyCross executable;
- DISCO's post-F9 parity 1,901 captures a complete scene through the same
  Release executable; and
- the Release PsyCross product rebuilds successfully.

## Checkpoint HUD continuity — 2026-07-31

The native guest-HUD supplement was hidden by testing the live retail clock
against the opening-sequence threshold on every frame. Retail checkpoint
restart rewinds that clock, so the HUD disappeared for several seconds after
every death even though gameplay had already handed control back to the
player.

HUD handoff is now a session presentation latch. It remains hidden for the
initial authored camera sequence, becomes visible at the existing handoff
threshold, and remains visible across retail checkpoint restart. F5 records
the latch alongside the guest quick state and F9 restores it, so loading a
pre-handoff state still hides the overlay while loading an ordinary gameplay
state restores it immediately.

The continuation gate moved to Mission 1 (`COLO`) rather than stopping at the
completed HWAY route:

- 10,000 combat updates crossed one retail death/checkpoint restart with no
  guest, renderer, collision, or presentation rejection;
- a 6,000-update exact F5/F9 route replayed byte-identically;
- a real Release PsyCross F5/F9 capture at presented frame 3,450 retained the
  complete outdoor world, upper-left armor HUD, player model, weather, and
  pistol/ammunition overlay; and
- both routes retained live SPU/XA output and produced no rejected sound-bank
  or voice accesses.

The low-level probe uses zero-based resource indices even though the product
CLI is one-based. A first pass accidentally attributed AIRBASE's index-1
stress results to Mission 1; the figures above are the corrected COLO index-0
gates and the product capture independently used `--mission=1`.

The stale-bank diagnostic now retains the retail caller, four-byte bank
signature, and entry count in addition to the bank/table/index tuple. The
AIRBASE index-1 neutral route produces zero rejected lookups. Combat input
first rejects its known `0x801EFFBC` bank at update 1,011, immediately after
that stimulus begins, through return `0x800FA1D4` in the voice-start path
rooted at `0x800F9F68`. The first rejected object still carries `SBNK`; later
calls observe the same allocation after its header has been repurposed. This
localizes AIRBASE's missing fidelity to the player/action cue-bank lifetime.
The supplied retail notes do not recover that ownership path, so the runtime
continues to reject the invalid selection rather than synthesizing a guessed
replacement bank.

## Deep product F5/F9 regression — 2026-07-31

The headless truck-objective route had a false-positive loot detector: it
compared the live inventory against one assumed mask, declared success as
soon as navigation began, and skipped directly to its return path. It now
captures the real post-intro armor/inventory baseline and recognizes the
authored truck transition. The corrected HWAY index-2 route reaches within 14
world units of its waypoint, loots the truck at update 2,544, crosses multiple
retail rooms, survives two checkpoint restarts, and completes the exact
F5/F9 replay without a guest-RAM difference.

The same controller is available to the product renderer only when
`SF2_AUTOPLAY_OBJECTIVE=1`. Its controller state is included in the diagnostic
F5/F9 boundary so replay does not splice post-save navigation into restored
guest state. `SF2_CAPTURE_COUNT` additionally captures up to eight consecutive
product frames for parity/flicker inspection.

A real Release PsyCross run saved in room 22, continued through rooms 21, 20,
and 19, then loaded the room-22 state. Presented frames 6,800–6,803 after F9
all contain the complete tunnel world, player, enemies, vehicles, lighting,
and native HUD. Frames 6,800/6,801 are byte-identical because they present the
same guest sequence; 6,802/6,803 are likewise identical for the next guest
sequence. Both distinct images are complete. This closes the deterministic
cross-room alternating-black-frame gate which the earlier same-room harness
could not exercise. Interactive validation remains appropriate for the
original long-session report, but the known host-VRAM rollback omission now
has both a direct fix and a product-renderer regression.

## Broad mission playtest and Disc 2 selector audit — 2026-07-31

Interactive testing reached recognizable live gameplay and authored in-engine
openings across the campaign on both discs. Mission 3 remains the first
verified end-to-end completion. Mission 1 now enters its correct parachute
state, although its preceding authored introduction is shortened. The native
HUD supplement still lacks the radar/minimap, timer, target/threat bars, and
complete retail text path. Interactive HUD/world flicker also recurred despite
the deterministic deep F5/F9 gate above, so presentation stability remains
open rather than closed.

The same playtest reported that product Mission 14 appeared to open Mission 21
content, while Mission 21 appeared to open Mission 10 content. An attempted
direct 8-through-20 archive selector was rejected before shipping: DARKMUSE,
GARAGE, and CHINBOSS each faulted during authored opening when given their
campaign index directly. Restoring the recovered Disc 2 permutation returns
all three to their previously green product-runtime path. The report therefore
remains open as a product identity/selection audit rather than a proven archive
mapping defect. Launch diagnostics must record the CLI campaign index, resource
name, and translated retail archive slot together for the next interactive
reproduction.

## Retained guest TPAGE and first complete HUD projection slice — 2026-07-31

The SF2 product renderer reset the active texture page to zero after every
guest frame. That was intended to prevent the native HUD's host-only alias page
from leaking into the next retail ordering table, but the HUD renderer already
captures and restores the complete guest `DRAWENV`. The extra reset instead
destroyed PS1 state which later retail `SPRT` packets intentionally inherit
when their ordering table contains no new E1 command. This is a direct cause
for intermittent text/UI sampling from world or framebuffer pages. The reset
is removed; the guest environment now survives the native overlay unchanged.

A real Release Mission 3 product capture at presented frames 3,000–3,003 is
stable after the change. Frames 3,000/3,001 are byte-identical, as are
3,002/3,003, matching the retail 20 Hz publication cadence. Both distinct
images contain the complete world, player, weather, health overlay, and knife;
none is black or HUD-only. The capture also makes a separate remaining atlas
defect visible in the `HEALTH` label, which is tracked under HUD completion
rather than presentation flicker.

The first HUD expansion reads the SF2 player instance's retail target
controller at the shared engine offsets (`instance+0x14`, target slot `+0`,
flags `+4`, meter `+0x58`). A valid active 0–100 target meter is projected
read-only into `GameplayHud` and drawn with the original TARGET label/bar
geometry. Synthetic unit coverage verifies its scaling and retirement when
the guest target becomes inactive. DANGER and radar markers remain deferred
until the six-entry guest tracked-target list is validated; no placeholder
values are synthesized.

Product launch logging now prints the player-facing campaign number/title,
resource name, disc, and translated retail archive slot together. This keeps
Disc 2's intentionally non-linear archive selectors from being mistaken for
campaign mission numbers during future reports.

## Retail objective and threat HUD state — 2026-07-31

Interactive validation confirms the retained-TPAGE correction has removed the
Mission 3 world flicker in the tested route. It also separated the remaining
HUD defects: delayed native handoff, absent radar/text/threat presentation,
incorrect TARGET timing, and the zero-health bar retaining a full red
interpolation trail.

The TARGET projection is now gated by the retail lock byte at `0x8012A554` in
addition to the player's target-controller record. This prevents a valid
first-person aim candidate from presenting the third-person TARGET bar. A
zero-health projection now synchronizes both native status endpoints to zero
instead of animating from stale pre-death values.

Objective notifications now follow the state mutated by the recovered retail
routine `MissionObjective_Complete` at `0x8002E6E8`. Its record is rooted at
`GP+0x90C` (`0x8011F570`) and completion bits live at record `+0x0C`. The
headless HWAY truck route validates the expected transition from `0x1` to
`0x9`; newly set bits produce a bounded
`PRIMARY MISSION OBJECTIVE COMPLETE` overlay. F5/F9 preserves both the
observed bitset and the remaining overlay lifetime so restoring a state cannot
invent another completion event.

The earlier attempt to find SF2 actors by shifting SF1 global addresses was
rejected: its claimed object count remained zero. Direct retail references
instead prove the SF2 object-record pointer at `0x8011EEF8` and object count at
`0x8011F564`; the records retain the shared `0x4C` stride. The native threat
projection now walks those real records, identifies Gabe by exact instance
identity, and combines living resident actors whose target-controller slot
equals Gabe's slot using the retail Q12 danger formula. A Mission 3 combat
probe resolves Gabe to object slot 159 and completes with valid threat state.
The DANGER label/bar is therefore restored without a guessed tracked-list
address. Radar remains open because its player-relative marker projection
still needs the verified SF2 actor pose/heading boundary.

## Interactive rejection, signed vitals, and SF2 radar boundary — 2026-07-31

The first interactive check rejected every visible claim in the preceding HUD
slice. TARGET and DANGER did not appear, the objective text did not appear,
health returned to full after reaching zero, and a second run reproduced major
world flicker during the truck/radio camera before Gabe fell into the void.
Those results supersede the earlier capture- and probe-only conclusion.

The corrections are evidence-driven:

- Health and armor are signed retail halfwords. Death uses a negative value;
  treating it as unsigned produced 65535 and clamped the native bar to full.
  Non-positive samples now project as zero and synchronize both animated
  endpoints immediately.
- `0x8012A554` was not a proven target-lock byte and has been removed. The
  exact player target controller is used while the live PC first-person aim
  mode suppresses the third-person TARGET bar.
- The SF2 target controller's Q12 danger scalar is at `+0xE8`, not the SF1
  `+0xD4` offset. A live object-table scan found `0x1000` at full threat;
  the deterministic combat route now resolves three actors targeting Gabe
  and a 100-percent danger endpoint.
- The objective bitset is already `0x9` when the direct TITLE-to-HWAY
  bootstrap first publishes gameplay, so it cannot detect the truck handoff.
  `MissionObjective_Complete` is now observed at its executable entry for
  later objectives. Mission 3's pre-consumed first objective is bridged from
  its authoritative zero-to-nonzero armor grant, with F5/F9 preserving the
  remaining notification lifetime.

The actor pose boundary needed by the radar is now recovered from the same
verified `0x4C` object records. Living instances expose a node at `+0x08`;
the node's PSYQ `MATRIX` pointer is at `+0x0C`, with Q12 rotation at `+0x00`
and translation at `+0x14`. Gabe's local +Z basis (`m02`, `m22`) matches his
observed forward displacement. The sixteen nearest living actor positions and
the exact selected slot are projected through the existing retail radar
geometry, compass, distance colors, and player pointer. This replaces the
missing panel with guest-owned pose data rather than a synthetic minimap.

The void reproduction also showed that the Mission 3 collision-room fallback
must not rewrite an authored `0xFFFF` during scripted camera/dialogue states.
The recovered dialogue state is a three-record queue at `0x80134D1C`,
`0x80134D2C`, and `0x80134D3C`; `-1` is idle. Collision containment and native
texture-room synchronization now consider all three records. The product
texture synchronizer still requires a room to remain stable for six
presentations, so the observed three-frame `20 -> 19 -> 20` radio-camera
transition cannot rebuild the atlas.

A new headless `objective-dialogue` mode follows the real truck route without
injecting the cutscene-skip input. At 4,500 frames it loots the truck at frame
2,544, crosses the first two radio XA starts, retains resident collision, and
reports zero malformed renderer lists or collision fallbacks. Extending to
6,200 frames crosses the subsequent event/failure/restart path without a guest
runtime fault. Interactive confirmation remains necessary after the complete
Release product is rebuilt, but the exact failed sequence now has a repeatable
unskipped regression instead of relying on manual traversal.

The former return-site-only floor repair remains retired. Disassembly of the
shared caller at `0x80084DC0` gives a narrower identity: its
`WorldCollision_Scan` call returns to `0x80084E7C`, retains Gabe in `s0`, and
iterates the current actor in `s1`; the caller itself distinguishes the player
case with `s1 == s0`. A containment guard may therefore replace only an
invalid player request room (`request + 0x0C`) at that exact call edge, using
the last valid room. It cannot rewrite other actors' collision requests.

Instrumentation counts this exact player-request identity separately. The
6,200-frame HWAY route made 225 such scans and the Mission 1 neutral route
made 36; none carried an invalid room and neither required a request repair.
HWAY still crossed its later failure/restart and reported only the two
existing owner-room fallbacks. A first Disc 2 package also completed without
a request repair. This proves the classification is live while the guard is
dormant during those deterministic routes; it does not yet prove the
intermittent floor-loss bug fixed. All 21 Release CTest targets pass and
`git diff --check` is clean after the change.

## Product HUD captures and authoritative sequel timers — 2026-07-31

Hidden Release-product captures now validate the native SF2 HUD in the actual
PsyCross framebuffer rather than only through state probes. HWAY simultaneously
renders the upper-left health, DANGER and TARGET stack, the lower-left radar,
the weapon/ammunition presentation, and the retail world. AIRBASE renders its
low opening health, radar, and countdown without disturbing the room geometry.

The first TARGET capture exposed an empty green bar even though the label was
visible. The target-controller halfword at `+0x58` is not selected-actor
health; it reached zero while the selected guard remained alive. The bridge
now resolves the selected object slot through the proven `0x4C` record table,
reads current health at record `+0x40` and maximum health at `+0x3E`, and
projects the resulting percentage. A second Release capture shows the TARGET
bar filled while the selected guard is healthy. First-person aim still
suppresses this third-person-only panel.

The supplied reverse-engineering notes do not recover a sequel HUD timer
handle or glyph pool. Treating every positive `.SS` timer as visible was also
rejected dynamically: HWAY has an active `CHANCE_*` choreography timer even
though no countdown belongs on its HUD. Host diagnostics now mirror the exact
script activation/deactivation lifecycle and enumerate positive timers only
from active programs; this host mirror is included in F5/F9 state.

AIRBASE provides an exact narrower boundary. Its active `LEVEL` program timer
0 starts at 4,200 retail 20 Hz ticks and decreases once per logic update
(`4,133` at a 200-update sample and `3,799` at 1,200). Only this proven
Mission 2 `LEVEL[0]` timer is projected, rounded up to seconds and centered
above the radar. A Release capture displays `3:13` at the sampled guest state.
Mission 1 exposes no active timer, and HWAY's unrelated program timer remains
hidden. AIRBASE and HWAY both retain exact 3,000-update F5/F9 replay after the
active-program mirror was added.

## Objective compositor, quick-load presentation, and floor-stress evidence — 2026-07-31

The objective notification now has two separate validation layers. The
headless HWAY objective route exercises the real armor-grant transition that
arms the first notification; a diagnostic-only `SF2_FORCE_OBJECTIVE_FRAME`
environment variable exercises only its final native compositor at a chosen
presented frame. A hidden Release capture from that compositor shows the full
`PRIMARY MISSION OBJECTIVE COMPLETE` string centered over a simultaneously
valid world, radar, vitals, threat, and weapon presentation. The forced path
is not treated as evidence for the gameplay trigger; it exists to keep atlas
and placement regressions independently reproducible.

The product quick-load path was also captured as an eight-frame sequence.
Release saved automatically at presented frame 2,500, loaded at frame 3,000,
and captured frames 3,000 through 3,007. Every image contains the complete
world and HUD; none alternates to a black/HUD-only page, duplicates the UI, or
retains a stale draw page. The restored guest frame/clock and retained TPAGE
match the saved values. This directly covers the previously reported F9
framebuffer split/flicker route, while longer interactive coverage remains a
separate gate.

An observation-only experiment tested whether the byte returned beside the
old collision call site represented floor presence. It did not: the byte was
zero for 217 of 225 healthy HWAY player scans and 66 of 71 healthy Mission 1
scans, including long false streaks while Gabe remained correctly grounded.
The observer was removed immediately. No floor-validity policy is based on
that byte.

The deterministic input probe now includes a `crouchback` mode matching the
reported high-risk input combination. A 10,000-update HWAY route crossed a
retail restart with 303 exact player collision requests, zero invalid player
rooms, no rejected ordering table, and stable player height. Separate
10,000-update crouch and weapon routes also crossed restart without malformed
renderer lists or invalid player requests. These runs strengthen the current
containment boundary but do not claim the intermittent floor loss is solved:
the weapon route cycles the inventory made available by the automated truck
path and is not yet a guaranteed late-mission shotgun stress test.

## All-mission quick-state matrix and shotgun floor-risk gate — 2026-07-31

The current Release runtime completed a fresh 3,000-update exact F5/F9 matrix
for all 21 player-facing missions across both discs. Every package booted,
published a valid world, advanced PAD/audio/script state, restored its saved
guest state, and replayed the recorded input interval without divergence. No
route faulted, repaired resident executable text, rejected an ordering table,
rejected a vertex entry, rejected an object-list merge, or used a collision
room/request fallback.

The matrix also records containment instead of treating exit status as the
only result. Missions 10, 17, 18, and 21 expose respectively 2, 6, 12, and 2
invalid player-room requests during authored/restart transitions. They have no
proven preceding room in the current boundary, so the conservative guard
observes but does not rewrite them. Missions 3, 11, 15, and 16 exercise the
existing escaped-ordering-table clamp; none proceeds to executable damage or
a malformed published list. AIRBASE and LABS2 exercise their already-localized
stale sound-bank/voice containment. These are explicit follow-up signals, not
hidden green results.

The `weapons` probe previously changed selection at every available PAD edge,
dwelling only six host frames on each item. It now sends one retail Select
edge per second and requires three complete post-truck selection cycles. When
item 8 (shotgun) is selected it combines crouch, backward motion, and repeated
fire, matching the reported high-risk manual input without writing inventory,
selection, pose, or collision state directly.

The 5,000-update HWAY gate looted the real truck at update 2,544, observed the
exact `20 -> 2 -> 4 -> 8 -> 20` ring three times, and accumulated 180 shotgun
crouch/back/fire frames. It ended with zero invalid player request, collision
fallback, renderer repair/rejection, sound rejection, or guest fault. This
substantially improves coverage of the intermittent floor-loss report while
remaining negative evidence rather than proof that every late-mission floor
case is closed.

The companion 3,000-update combat/input matrix also passes all 21 missions.
It adds run/turn/strafe, lock-on, first-person aim on both axes, repeated fire,
reload, weapon-change edges, damage, and natural retail death/restart activity.
No mission faults, repairs executable text, rejects a renderer list, or uses a
player-request fallback. Combat exposes only the already-localized classes:
AIRBASE's stale action cue bank, LABS2's stale bank/voice updates, bounded OT
clamps, and HWAY's two owner-room retentions. WRECK's synthetic combat driver
produces 104 invalid player requests while repeatedly walking the airborne
opening off terrain and crossing eight retail restarts; its neutral/parachute
route remains the meaningful clean-start assertion and no generic room rewrite
is inferred from this deliberately destructive stimulus.

## Retail HUD handoff and faction-aware radar — 2026-07-31

Interactive Mission 3 validation now confirms that the real truck objective
displays `PRIMARY MISSION OBJECTIVE COMPLETE`. After the subsequent retail
death/restart, the native HUD appears immediately. This separates the earlier
delay into an initial camera-handoff problem rather than a general HUD or
checkpoint initialization failure.

The initial HUD latch no longer waits only for the coarse retail clock value
300. Startup cleanup produces two dialogue-stop calls; the first later retail
scene/dialogue stop marks the authored opening handoff and now activates the
HUD, with the clock threshold retained only for silent openings. A hidden
Release capture observes Mission 3's stop at guest frame 592 / clock 196 and
captures a complete world and HUD at guest frame 623 / clock 206. The existing
latched value still survives death and F5/F9.

The radar previously admitted every living object with an actor node and
target controller, yielding 16 candidates in the headless Mission 3 probe.
Retail's object dispatcher reads the class byte at object record `+0x2A`;
SF2 class families `0x01` and `0x03` are opposition while `0x02` is allied.
The bridge now retains only those actor factions and carries the relationship
to presentation. Emulator comparison corrected a second approximation:
enemy green/yellow/red color follows the per-actor Q12 threat value at target
controller `+0xE8`, not world distance. The existing `0x553` and `0xAA7`
thresholds are the retail one-third/two-thirds threat transitions. Allies use
a blue marker, matching the emulator reference where Chance is a blue dot.
The resulting neutral Mission 3 probe contains four relevant actors rather
than the previous 16 unfiltered target-capable objects.

The native pickup-text boundary now also observes retail
`ActorPickupQueue_Process`'s presentation call at `0x800ABC48` and copies its
bounded guest string for the existing font compositor. Mission 3 diagnostics
decode the observed string as `9mm taken`. The truck's scripted bulk grant
bypasses this ordinary pickup call, so it is explicitly not used as evidence
that the truck notification path is complete.

Release rebuilds of `sf_tool`, `syphon_filter`, and `sf_tests` succeeded after
the handoff/faction/threat slice, and all 21 configured CTests passed. SF1's
low-level legacy VM, CD, and loop probes also pass. The deeper native SF1
bootstrap/level probe still stops at its previously known opening-handoff
mismatch (`protected-cbdc-failure-branch-missing`); this remains a documented
baseline gap rather than being reported as a full SF1 regression pass.

Emulator comparison also closes the melee TARGET rule. Knife and Hand Taser
retain a valid selected actor while locked on, but retail does not raise the
TARGET panel for either; ranged weapons do. The projection now gates TARGET
by the sequel's authoritative equipped item: ranged slots 1..18 and the M-79
slot 21 are admitted, while melee slots 19/20 and later tools/throwables are
suppressed. Unit coverage exercises knife, Hand Taser, Air Taser, M-16, and
first-person suppression.

Two 10,000-update Mission 3 stress routes pass after the HUD changes. Combat
crosses one retail checkpoint restart, holds a selected actor for 7,778
frames, reaches two simultaneous threats/full danger, and ends with four
faction-filtered radar actors. Exact quick-state replay also completes with
four actors and a 48-percent aggregate danger sample. Neither route faults,
repairs executable text, rejects a renderer list, records an invalid player
room, reports a collision gap/floor observer event, or rejects sound state.
The combat route uses three already-contained owner-room retentions and 15
bounded escaped-OT clamps; the quick-state route uses one clamp.

A fresh post-faction 1,000-update neutral sweep then exercises the adapter on
all 21 missions across both discs. Every mission publishes faction-filtered
radar state without a guest fault, executable repair, rejected ordering table,
or collision gap. Mission 14 naturally restarts at update 978, so the short
window fails only the probe's required post-checkpoint dwell; its proper
3,000-update rerun passes through three restarts with no invalid player room
or renderer rejection. Missions 17 and 18 retain respectively six observed
invalid requests during their already-known authored transition windows; no
unproven room is synthesized and neither route faults.

## Initial auxiliary map-list normalization — 2026-07-31

The two-page compositor exposed a deterministic first-life-only full-screen
map grid over gameplay. Interactive comparison established the decisive
lifecycle boundary: the overlay survived quick-load but disappeared after a
retail death/checkpoint restart, while the ordinary HUD and text remained.
Bounded instruction tracing then showed that the auxiliary renderer continued
to submit on both lives. Before the restart its general-list field contained
exactly 113 nodes: 86 line primitives and 27 border/textured primitives which
match the visible grid. After restart that general list was empty; radar,
health, weapon and text traffic continued through the renderer's other lists.

The direct TITLE-to-mission bootstrap now normalizes that one stale frontend
attachment before the first gameplay frame. The repair is deliberately
structural rather than a GPU-packet filter: it unlinks the general list only
when the complete observed 113-node chain, node pool, primitive pool and
terminator all match. A real map opened later can install a fresh list normally.

User validation confirms the initial overlay is gone. A subsequent deterministic
Mission 3 truck route reached the retail `M-16 taken` event at guest frame 2,095
and captured a clean first-life composition containing world geometry, HEALTH,
DANGER, radar and weapon/ammo presentation. The requested capture exit was
clean, the Release product and test executable rebuilt successfully, and the
unit suite passed.

## Retail HUD authority, startup stack safety, and radar primitive parity — 2026-07-31

The product route now makes the captured retail HUD authoritative by default.
The native compatibility HUD remains available when retail auxiliary UI is
disabled, and `SF2_ENABLE_NATIVE_HUD=1` can explicitly force it for diagnostics.
This removes the doubled health/radar/weapon composition seen from ordinary
mission shortcuts while preserving the comparison paths used during bring-up.

A reproducible immediate Windows stack overflow was traced to `Spu::reset()`.
MSVC implemented aggregate assignment of the roughly 594 KiB `SpuState` as a
temporary on the caller's already-deep startup stack. Reset now clears the
owned, trivially-copyable state in place and restores its two nonzero authored
defaults. Ten consecutive Mission 3 product startups completed after the fix.

The remaining radar corruption was not bad map data. SF2 places unused radar
polygons and lines at packed GPU coordinate `0x04000400`. PS1 GP0 vertex fields
are signed 11-bit values, so this means `(-1024,-1024)`; the bridge had decoded
the fields as signed 16-bit `(+1024,+1024)`. Correct sign extension moved the
large wedge to the opposite side and exposed the second missing hardware rule:
retail relies on the PS1 rejecting the resulting oversized primitive, while
PsyCross clipped and rasterized it. Captured polygon/line translation now
recognizes that exact disabled-vertex sentinel and preserves the retail reject.
The golden truck replay and interactive validation both show a clean minimap,
including actor dots and threat cones, without affecting world or other HUD
primitives.

Retail full-width black F4 overlays are also expanded by the active adaptive
aspect scale. Mission 3's authored in-engine cinematic bars now reach both
widescreen edges while retaining their original vertical height; the same rule
keeps full-screen retail black fades full-screen without stretching general UI.

Keyboard `P` now sends the retail Start bit while Escape remains the host scene
exit. The input reaches retail code and plays the expected menu sound, but the
current direct gameplay bootstrap does not complete a valid pause/map state:
pressing `P` again can leave Gabe's shadow while hiding his model. Treat the
mapping as instrumentation for the next menu/map pass, not completed menu flow.

## Cross-mission frontend cleanup and PS1 polygon rejection — 2026-07-31

The initial auxiliary map-list repair is no longer tied to HWAY's allocator
addresses. A trace sweep of all 21 mission packages found the same stale
113-node frontend list and primitive pool but mission-specific dense node
windows. Startup now derives that window from the current head and clears it
only after validating every aligned node, primitive, unique visit and terminal
link. Release captures of Missions 1, 7, 8 and 20 no longer contain the stale
full-screen map grid, while gameplay HUD and cinematic submissions remain.

The exact `(-1024,-1024)` disabled-vertex check fixed the radar wedge but was
too narrow for world projection failures. The PS1 GPU also rejects polygons
whose raw coordinate span reaches 1024 horizontally or 512 vertically;
PsyCross had clipped and rasterized those packets as angle-dependent,
screen-filling vertex explosions. All translated SF2 triangle and quad types
now apply the hardware span rule before submission. Mission 7, previously the
worst Train Race reproduction, retains its train/fire/world geometry in a
fresh frame-1000 capture without the exploding grid, and equivalent captures
of the open Missions 1 and 8 remain intact. The brief uncovered edges during
fast widescreen camera rotation are a separate overscan/presentation issue.

SF2 scoped weapons no longer request SF1's `SCOPED.TIM` and `SCP*.TIM` assets
from the native HUD residency cache. The sequel's guest ordering tables own
their optic presentation and its mission interface archives do not guarantee
those predecessor assets; crossing into a scoped state therefore no longer
raises the fatal missing-HUD-texture error. The Release product, tool and test
targets build successfully and all 21 configured CTests pass.

## Retail pause-menu publication and scoped VRAM residency — 2026-08-01

The retail pause/map flow now publishes its authored state-7 ordering tables
as complete presentation bases. MENU has no 3D world polygon list, so the old
world-only base threshold froze the preceding loading frame even though the
guest had built the full map, labels, borders and exit prompt. Product and
tool diagnostics can now pulse Start on retail PAD sample boundaries; the
deterministic route crosses application states `0 -> 9 -> 7 -> 0` and captures
the complete pause screen.

Closing the menu initially returned to working gameplay with Gabe's shadow but
not his model. Render-queue, synchronized RAM and focused instruction traces
ruled out guest state: Gabe entered visibility and actor-model dispatch once
per tick, and both his 11,759-instruction update path and later 3,143-instruction
draw path matched uninterrupted gameplay. The fault was retained texture
residency. MENU submitted transient uploads at `(768,416,8x64)` and
`(768,510,256x2)`; the single native upload cache then replayed them before
every gameplay frame, leaving the first rectangle over Gabe's texture page.

The runtime now snapshots the gameplay upload cache on the state-9 menu entry,
allows MENU to mutate its own residency while state 7 is active, and restores
the gameplay cache before the first resumed state-0 frame. The post-menu
composition returns from 208 to the clean 206 retained setup packets and Gabe
renders normally. A second deterministic route verifies pause close followed
by F5/F9 with Gabe, world, HUD and radar intact. The product probe reports and
gates one menu VRAM snapshot/restore pair, and all 21 Release CTests pass.

## Success-qualified campaign handoff — 2026-08-01

SF2 gameplay previously treated every stopped guest frame loop as a runtime
fault. That made an authored mission ending fall out through
`return_to_title` immediately before the native save/EOL path. The runtime now
observes the verified retail outcome entries separately: success at
`0x8002D6D8`, failure at `0x8002DA38`, and the shared transition callback at
`0x8002D8D0`. Failure clears the success latch, so the common application
state used by both outcomes cannot accidentally advance the campaign.

Dynamic probing refined the terminal boundary. Accepted MissionSuccess first
pushes application state 11; the overlay lifecycle can then pass through state
9 into movie state 4 without publishing an intermediate display list. Waiting
for the later shared callback loses the live player and inventory tables. The
product therefore hands control to the host on success-qualified state 11,
with state 4 as a bounded missed-boundary fallback. At that point the guest
still has coherent player state and the scene exports the supported SF2
weapons, ammunition, health and armor as `CampaignCarryState`.

The reciprocal campaign entry path now applies that carry directly to the
authoritative SF2 guest inventory and vitals before the first interactive
update, then refreshes retail's checkpoint snapshot. Recognized persistent
weapons replace their standalone package defaults; mission-local and currently
unmapped sequel items remain untouched. Death after a connected transition
therefore restores the carried chapter state instead of reverting to the
mission's scene-test loadout.

`probe-sf2-product-runtime ... complete` force-enters the verified accepted
branch of the retail success routine, proves the real state-11 transition, and
requires exactly one success observation, no failure observation, and one
terminal handoff. The complete probe, the Release product/tool builds, and all
21 configured CTests pass. Normal campaign mode consumes the result through
the existing save-menu/EOL/next-mission path; `--scene-test` deliberately plays
only the selected mission's ending movie and exits.

Mission 1's shortened parachute opening was a bootstrap input leak. Every
mission needs one synthetic Cross sample to dismiss the retail loading gate,
but Colorado Mountains makes its opening scene skippable earlier than the
other packages. Sending the generic pulse at PAD poll 8 could therefore skip
the scene. Mission 1 now receives the required pulse on poll 1, allowing its
release to retire before the parachute choreography becomes skippable; its
1,800-frame neutral probe reaches stable gameplay.

The pause regression probe is now application-state driven rather than tied to
fixed frame 1000/1600 pulses. This matters for long openings such as Mission
20, where state 0 still rejects Start until roughly frame 1600. The probe
retries only while gameplay owns state 0, waits for observed MENU state 7, and
then closes it. Mission 1, Mission 8, and Disc 2 Mission 20 all complete the
authored `0 -> 9 -> 7 -> 0` roundtrip with one scoped VRAM snapshot/restore.

### Realtime audio starvation recovery — 2026-08-01

Long interactive runs reported dialogue drifting late and occasional audio
pops. The current-session audio diagnostic confirmed a callback starvation
after normal startup (`ring=735`, `silence_frames=4140`). Initial playback
correctly waits for the configured 1,536-frame prebuffer, but the callback was
also imposing that startup threshold after every live starvation. While it
emitted silence, exact guest PCM accumulated behind the picture; resuming the
preserved FIFO therefore converted each brief scheduling stall into permanent
additional A/V latency.

The callback now consumes the next available timeline sample immediately
after a live starvation. Initial startup retains its prebuffer, while recovery
adds no second buffering delay. A five-millisecond fade tapers the last partial
callback into silence and ramps the next available samples back in, removing
the hard non-zero/zero edges responsible for the reported pops without
restarting the OpenAL source. Product, tool and test targets build after the
change; all 21 configured CTests pass, as do the Mission 1 neutral,
Mission 3 success/carry, and cross-disc pause/menu guest probes.

### Twenty-one-mission cursor and automatic disc handoff — 2026-08-01

The first success-qualified host path exposed two independent native campaign
assumptions which would have stopped a full SF2 run even though the guest
mission itself completed correctly. `CampaignProgress` and durable save
validation were still bounded by SF1's 20-entry catalog, making New York Sewer
(Mission 20) look final and rejecting Finale (Mission 21) as an invalid save.
Campaign cursors and save transactions now carry the selected game's catalog
size. A focused regression starts on Mission 20, advances to Mission 21,
stores and reloads that cursor, then stages the actual finale and reaches
`campaign_complete`; the unchanged default continues to exercise SF1's
20-mission behavior.

The title host also retained its launch CUE for every later package. At the
Mission 8-to-9 boundary this reopened Disc 1 and could not mount `DISCO` from
Disc 2. SF2 campaign dispatch now verifies which retail disc owns each target
resource, first tries the conventional sibling `Disc 1`/`Disc 2` filename,
then identifies renamed sibling CUEs by supported-game metadata. The resolved
path is used consistently for mission packaging and the guest runtime. If the
matching disc is absent, the campaign logs the required mission and returns
cleanly to title rather than failing inside package construction. The ending
movie skip policy now uses the active game's catalog as well, so SF2 Mission
21—not SF1 Mission 20—is recognized as the protected campaign finale.

The first carry bridge projected SF2 items into the native SF1 weapon enum.
That preserved common firearms but could not represent the second ownership
word or sequel-only inventory slots, contradicting retail
`PlayerCarry_Snapshot`, which copies all 34 quantity pairs. Save V5 adds an
optional exact SF2 carry block: equipped item, both ownership words, and all
reserve/loaded pairs. V1–V4 remain readable through the mapped compatibility
path; new SF2 transitions restore the exact block and refresh retail's
checkpoint snapshot afterward. Unit coverage round-trips item 33 alongside
ordinary M16/shotgun state, and the product completion probe still reaches
success-qualified state 11 with coherent carried vitals and inventory.

### Embedded campaign movie handoff — 2026-08-01

The success boundary was extended with a diagnostic-only continuation that
leaves the product interception unchanged. Mission 3 crosses retail campaign
advance `0x8002C95C` once, increments the selected mission from 2 to 3, and
enters application state 4 through the mission overlay request at
`0x80154054(1)`. No new `0x80142E60` playback initialization occurs during
that continuation: the catalog slot 15 observation was the already-completed
`989LOGO.STR` startup request. This separates the native post-mission save
menu from the later campaign STR rather than incorrectly treating state 4 as
the cinematic itself.

SF2 story movies are not ordinary ISO files. They are raw Mode 2 ranges inside
`MOVIE1.HOG`/`MOVIE2.HOG`; the first 2 KiB Form-1 sector supplies a 27-entry
name and physical-sector offset catalog. The native package loader validates
the final sentinel against the ISO extent and exposes the exact `[offset,
next_offset)` raw-sector range through the existing `DiscMovie`/STR decoder.
Mission 3's verified terminal clip is `MOVIE1.HOG:4_1.STR`. The decoder probe
validates its 8,936 sectors as 512x160 at 15 fps with 851 video frames, 1,113
audio chunks, and 2,243,808 stereo sample frames. Its campaign definition now feeds that
clip to the existing native order: success-qualified carry capture, save
menu, exact terminal movie, mission advance. Other SF2 movie mappings remain
empty until their retail transition ownership is established; this avoids
turning archive-name guesses into campaign behavior.

An initial implementation incorrectly treated the catalog's `0x920` Mode-2
payload-size field as a sector-count conversion factor. It started each entry
at the correct physical offset but inflated its length by `2336/2048`, causing
every cinematic to consume the beginning of its successor. `8_2.STR` exposed
the overlap as damaged MDEC slices from `9_1.STR`; final entry `Z17_1.STR`
proved the range error by extending past Disc 2. Exact unscaled deltas decode
all three strictly, and the movie probe now locks Mission 3's sector and frame/
audio counts so this campaign-ending overrun cannot regress.

### Retail TITLE success branch and presentation-qualified pause gate — 2026-08-01

Static analysis of the exact `TITLE.OVL` resolves what the mode argument to
`0x80154054` owns. A non-zero mode enters TITLE substate 11; it does not select
or start an STR. Substate 11 reads the already-advanced mission index and the
mounted-disc byte, classifies Missions 1--8 as Disc 1 and Missions 9--21 as
Disc 2, requests a disc swap only on mismatch, and then emits terminal
TITLE state 18 for the successful continuation. Zero mode emits state 16. This
confirms both the native save-before-movie ordering and the automatic Disc
1-to-Disc 2 boundary while explaining why no post-success movie call appears
at the mission callback itself. The diagnostic continuation gate now requires
exactly one campaign advance, the next mission index, application state 4,
TITLE mode 1, and no new STR initialization before native save-menu ownership.

The pause regression is no longer satisfied by VRAM counters alone. For every
new publication it hashes the complete GP0 stream and requires a gameplay
frame before pause, a distinct non-empty authored state-7 menu frame, and a
fresh state-0 gameplay frame after restore, in addition to exactly one menu
VRAM snapshot/restore pair. Mission 1, Mission 3, Mission 8, and Disc 2 Mission
20 all pass the strengthened `0 -> 9 -> 7 -> 0` gate. Their menu publications
contain 249--267 draw commands and differ from both surrounding gameplay
compositions, catching the earlier sound-only/invisible-player failure class
without opening an interactive window.

### SF2 title-movie host and verified Mission 1 opening — 2026-08-01

Normal campaign startup still attempted SF1's standalone `SOL/*.STR` paths,
even though SF2 stores its frontend movies in `MOVIE1.HOG`/`MOVIE2.HOG`.
`TitleMovies::load` now selects the embedded SF2 catalog and preserves the
native title host's five-slot contract with `989LOGO.STR`, `EIDETIC.STR`,
`LEGAL.STR`, `TITLE.STR`, and `DEMO1.STR`. The default movie probe decodes all
five strictly before checking Mission 1's opening and Mission 3's terminal
movie, covering the same paths used by a normal non-`--scene-test` launch.

A four-frame contact sheet of every Disc 1 story entry identifies `2_1.STR`
as the aircraft/New Game cinematic that leads into Colorado Mountains.
Mission 1 now owns that verified opening movie; the other campaign movie
slots remain deliberately unmapped until their transition ownership is
established. Opening-movie suppression now compares the mission movie against
the clips actually played during startup: SF1's `INTRO.STR` remains suppressed
after its startup sequence, while SF2's separate `2_1.STR` correctly plays
after New Game. The Release probe reports 3,007 frontend/campaign video frames,
3,814 audio chunks, and 7,689,024 stereo sample frames across the seven checked
movies without damaged MDEC slices or catalog-boundary overruns.
Disc 2's default probe independently decodes its five frontend movies and the
final `Z17_1.STR` catalog range (46,976 sectors, 4,692 video frames, 5,868
audio chunks), protecting the opposite archive boundary without assigning an
unverified campaign transition to that entry.

### Fresh all-mission state and combat matrices — 2026-08-01

The final campaign/frontend binary was rerun through both 3,000-update
cross-disc matrices rather than relying on the July 31 results. All 21
player-facing resources pass exact F5/F9 replay. Together those routes cross
15 retail checkpoint restores with zero collision-residency gaps, renderer
repairs, rejected ordering tables, or replay divergence. Mission 21 is
`CHINBOSS` on Disc 2; its guest archive slot is 9 even though its player-facing
campaign index is 20, so matrix launch and automatic disc resolution must use
the supported-game resource catalog rather than infer ownership from the
archive slot.

All 21 missions also pass the fresh combat/input matrix, including 28 retail
checkpoint restores, repeated movement/aim/fire/reload/weapon-change input,
and live SPU/XA activity. No route faults, loses collision residency, repairs
guest renderer state, or rejects an ordering table. AIRBASE and LABS2 retain
their already-localized stale sound-bank containment (30 and 73 rejected
lookups respectively); every other mission reports zero sound-bank rejection.
These remain missing-cue fidelity gaps, not crash or state-corruption paths.

Train Ride initially failed an over-broad radar assertion after the faction
filter was enabled. Instrumentation showed that the five former dots were
target-controller objects with classes `0x2E`, `0x6A`, `0x19`, `0x00`, and
`0x5D`, not the proven hostile/allied actor classes `0x01`--`0x03`. Their
removal is therefore correct rather than a radar regression. The matrix still
requires a valid player heading everywhere and retains its nonzero actor-dot
gate on the emulator-validated Mission 3 route.

The matrix is now a checked-in release tool rather than a shell-history-only
procedure. `tools/run_sf2_validation_matrix.ps1` selects the correct disc for
each player-facing mission, runs both `quickstate` and `combat` by default,
stores one diagnostic log per route, and emits `summary.csv`. It independently
fails on collision gaps, renderer repair, rejected ordering tables/list
merges, or any new sound containment outside the documented AIRBASE/LABS2
exceptions. Its complete default invocation passes all 42 routes against the
Release binary; the generated evidence remains under ignored `build/` data.

### Retail-indexed in-mission FMV handoff — 2026-08-01

The `.SS` action map resolves the remaining authored gameplay movie requests
without filename inference. Action `0x70` calls retail
`MovieRequest_StartDefault` (`0x8002C558`) with the decoded HOG catalog index:
AIRBASE requests indices 3 and 4 (`3_2.STR`, `3_3.STR`), while AIRBASEX
requests index 10 (`6_3.STR`). No other SF2 mission script contains that
handler in the complete action corpus.

SF2 mission packages now load those exact embedded ranges and retain their
retail catalog indices. The runtime lets `MovieRequest_StartDefault` and
`MovieRequest_Start` execute normally so retail copies the complete request,
pushes application state 9, restores the prior state in `MovieLoader`, and
prepares its own completion callback. Only the mapped decoder-init call at
`0x80142E60` is substituted, after its sector range has identified an exact
active-mission catalog entry. This avoids running MOVIE.OVL in the live
mission's shared overlay window while preserving the retail state machine.
Gameplay PCM is retired at that boundary, depth presentation is restored after
the clip, and the host invokes retail `MovieCompletion` (`0x8002BE68`). That
routine owns teardown and emits the exact state-0
`0x001E0000 | catalog_index` event before the same guest mission resumes.
Index-based selection deliberately permits the same clip to replay after an
F5/F9 rewind without relying on a monotonic host-side ordinal.

The stricter Disc 1 movie gate now decodes all three scripted clips in
addition to the five frontend clips, Mission 1 opening, and Mission 3 ending.
The ten-movie result is 6,970 video frames, 8,785 audio chunks, and 17,710,560
stereo sample frames. Exact protected results are `3_2` at 8,032 sectors /
796 frames, `3_3` at 17,304 / 1,721, and `6_3` at 14,528 / 1,446. Release unit
tests lock both the path order and catalog indices, and full AIRBASE/AIRBASEX
package construction succeeds against the retail disc. Deterministic guest
request probes additionally submit the exact retail helper call in fresh
runtimes for all three catalog entries, reach decoder-init handoffs 3, 4, and
10, preserve each pending handoff across F5/F9, execute retail
`MovieCompletion`, and return to stable application state 0 within their
1,200-update windows. The probe intentionally injects each request late because
a synthetic completion event cannot reproduce the surrounding authored script
phase indefinitely. Natural action-`0x70` timing and the downstream script
reactions remain interactive Mission 2/5 playtest items.

The final post-bridge Release gate reran the complete 42-route matrix at 3,000
updates per route. All missions passed in both quick-state and combat/restart
modes, including 43 retail checkpoint restores, with zero collision gaps,
renderer repairs, rejected ordering tables, or rejected list merges. Only the
previously documented AIRBASE and LABS2 stale-sound containment remained. The
Release suite passed 21/21. Compatibility was then rechecked against SF1 USA
Rev 1: all 29 campaign/title movies decoded (17,135 video frames), the retail
CD/DMA probe loaded `SUBWAY.OVL`, and the continuous guest loop published seven
native GPU boundaries. A clean-room scan also found no local research-document
names or download paths in project content.

### Exact all-campaign EOL map and safe decoder yield — 2026-08-01

Retail success-shell continuation was run for all 21 player-facing missions,
with the Disc 2 campaign cursor seeded only at the success entry. Seeding it
for the whole gameplay session was wrong: Disc 2 overlays retain archive-order
state at the same location, and Gregorov exposed the mistake with a repeatable
frame-974 overlay fault. Keeping archive order live through gameplay and
writing the player-facing index at `MissionSuccess` restores Gregorov while
preserving exact Mission 9-to-10 and later campaign advancement.

The observed EOL selections are now native mission metadata rather than name
guesses. Disc 1 selects `2_3`, `3_4`, `4_1`, `5_2`, `7_2`, `8_2`, and `9_2`;
Disc 2 selects `13_2`, `14_2`, `15_2`, `16_1`, `17_3`, `18_2`, `19_2`,
`20_3`, and `21_2`. Missions omitted from those lists advance without an EOL
STR. After Mission 21, the campaign host plays the catalog-final `Z17_1`
stream with skipping disabled. Strict decoder coverage now passes 16 Disc 1
movies (11,993 video frames / 30,409,344 stereo samples) and 15 Disc 2 movies
(14,996 video frames / 37,937,088 stereo samples), including every mapped EOL
range and `Z17_1`.

The in-mission decoder replacement also gained an exact post-host-call yield.
Previously `MOVIE.OVL` could run beyond decoder init in the same guest update
before the native player saw the request. The VM now completes the substituted
call, yields with the live retail stack intact, and prevents further guest
updates until native playback completes. Completion first lets MovieLoader run
its authentic zero-result setup and return, then invokes retail
`MovieCompletion`. Both AIRBASE movies and AIRBASEX's movie still pass their
request -> F5/F9 -> native handoff -> retail completion probes.

The final Release matrix was rerun after the cursor and movie changes. All 42
3,000-update quick-state/combat routes pass with 43 checkpoint restores and
zero collision gaps, renderer repairs, rejected ordering tables, or rejected
list merges (`build/sf2-validation-20260801-060120/summary.csv`). CTest remains
21/21. SF1 compatibility remains unchanged: 29/29 movies, CD/DMA overlay load,
and the continuous retail loop all pass. The remaining publication gate is the
manual Mission 3 save -> `4_1` -> Mission 4 carry test; no package or tag is
created before that observation.

### Human-verified campaign movie order and Disc 1/Disc 2 seam — 2026-08-01

Interactive completion of Mission 8 established the complete retail campaign
order rather than only proving the individual transition states. The observed
sequence is:

1. current mission completion;
2. current mission ending STR (`9_2.STR` for Mission 8);
3. save prompt;
4. next mission opening STR (`10_1.STR` for Mission 9);
5. briefing/loading;
6. next mission's in-engine opening;
7. gameplay.

The host now performs that order directly. Choosing No at the save prompt no
longer suppresses the next opening movie, and the next campaign loop owns the
opening STR before it constructs the briefing/runtime. A complete Mission 8
playtest passed the sequence from `9_2.STR`, through the centered save menu,
across the automatic Disc 1-to-Disc 2 mount, into `10_1.STR`, the Mission 9
briefing and in-engine opening, and live Mission 9 gameplay. This is the first
human-confirmed campaign seam across the two retail discs.

Loading an independently persisted Mission 4 slot also selected its correct
opening STR before entering the mission, confirming that opening selection is
derived from the saved destination mission rather than stale source-mission
state. The prior "Washington Park" label remains an SF1 display-name leak,
not a save-target error. The complete Release suite remains 21/21 after the
flow correction. Remaining adjacent defects are the incorrect SF2 save-slot
labels, Mission 1's prematurely skipped in-engine parachute opening, and the
known Mission 7 music cutoff.

Mission 2 was subsequently played through its authored mid-mission movie
handoff. Both the entry into native STR presentation and the return through
retail `MovieCompletion` resumed the same live mission without a fault or
state loss. This supplies the missing interactive counterpart to the
deterministic AIRBASE request/completion probes.

The misleading save labels had a separate presentation-only cause. Both the
campaign save renderer and the title load renderer resolved a correct SF2
mission index through the no-argument `missionCatalog()`, whose compatibility
default is the SF1 catalog. The renderer now retains the loaded mission's
`GameId` and uses `missionCatalog(game_id)` for titles and bounds, so persisted
SF2 slots name their actual SF2 destination without changing the durable save
format or mission index. The Release executable relinked successfully and the
suite remains 21/21; interactive label confirmation is the last check before
the alpha-5 publication pass.

Interactive loading confirmed both corrected labels: the persisted Mission 4
slot displays `I-70 Mountain Bridge`, and the cross-disc Mission 9 slot
displays `Pharcom Expo Center`.

That Mission 9 load exposed one final Disc 2 index-space error before release.
Its `10_1.STR`, briefing label and save cursor were correct, but gameplay
opened Moscow Club 32. The prior archive translation had conflated three
different identities: player-facing campaign number, reported Disc 2 resource
slot, and MissionArchive selector. Interactive identification gives the
runtime-resource order for player Missions 9--21 as the one-based sequence
`10, 21, 9, 11, 12, 13, 15, 16, 17, 18, 19, 20, 14`. The runtime now maps the
campaign index to that resource slot first, then applies the existing proven
resource-to-archive mapping. Save titles, opening/ending movies and campaign
progress remain in player-facing order.

The four displaced routes (Missions 9, 10, 11 and 21) all pass focused
1,200-update probes after the split. Loading the Mission 9 slot interactively
then entered Pharcom Expo Center gameplay after its correct opening movie,
confirming the full Disc 1-to-Disc 2 seam. The final corrected matrix passes
all 42 3,000-update routes with 45 checkpoint restores, zero collision gaps,
zero renderer repairs and no unexpected sound containment
(`build/sf2-validation-20260801-134546/summary.csv`). CTest passes 21/21.
SF1 compatibility also passes 29/29 movie decodes (17,135 video frames), the
SUBWAY CD/DMA overlay probe and seven-boundary continuous guest loop.

### Alpha 6 audio triage and opening ownership — 2026-08-01

The reported music cutoff belongs to Mission 7 (`TRAIN2`), not Mission 8.
Clock-neutral XA admission counters now distinguish received, admitted,
busy-rejected, decode-rejected and muted sectors, admitted sample frames and
the maximum pre-admission CD FIFO depth. Mission 3's 6,000-update dialogue
route admits all 167 received sectors; focused Mission 7 gameplay admits all
18 received sectors. Neither route rejects a sector for any reason, so the
old ten-frame XA low-watermark is not the cause of either defect. The product
audio trace now also reports callback request shape, PCM loudness and queue
depth once per second; a deterministic Mission 7 input recording is still
needed to cross the approximately 30-second cutoff under authored gameplay.

Dialogue timeline events now capture the exact CD LBA, decoded-CD queue,
reading/mute state, XA set/file/channel, and sector counters at the same guest
instruction as each retail speech/XA call. The authored truck-route exchange
uses cues 183 and 182. Both resolve exactly through retail's `cue / 15` rule
to channels 3 and 2 in descriptor group 12, admit their sectors, drain, and
emit their completion events. A later headless-route sequence starts cue 103
at clock 1460, reads three sectors on channel 13, stops at clock 1467 with
2,940 decoded frames queued, and then starts/stops cue 181 in the same frame.
That sequence belongs to the later route/failure phase, not the truck exchange,
so it must not be presented as the reported cross-mission dialogue defect.
The stop does clear the decoded queue, which rules out a permanently retained
emulated XA FIFO. Reproducing the user's uneven truck conversation still
requires the recorded interactive route and host-output timing.

The script corpus also rules out stale AIRBASE bytecode as the source of the
late cue. HWAY itself authors cue 103 in `CHANCE_PINNED`, program 6, event
record 22 at relative offset 916 (`0x17E7`), after its cue-181 event at offset
760. AIRBASE contains the number 103 only as an operand to unrelated action
`0x27`, not as a scene-speech request. Therefore hearing familiar Mission 2
material after the long delay cannot be fixed by suppressing cue 103 or
swapping mission script tables: the remaining candidates are the authored
global XA cue's actual content, delayed host presentation, or incorrect
completion timing which lets that later HWAY event fire in the wrong gameplay
phase.

`sf_tool probe-sf2-product-runtime` can now consume an `SF2PAD1` transcript
directly in `replay` mode, so the repository's 5,500-update human Mission 3
truck route runs through the full guest diagnostics without opening a product
window. The golden replay completes with valid presentation, collision and
script state, 4,042,500 PCM frames, all 290 received XA sectors admitted, and
zero busy, decode or mute rejection. It independently reproduces the same cue
ownership: truck cues 183 and 182 start at updates 937 and 1,156, while HWAY's
later cues 103 and 181 start at updates 4,105 and 4,351. The emulated PCM is
drained continuously by the probe, so no guest FIFO backlog carries the truck
pair into the later sequence. This further narrows any audible late line to
authored event phase or product host-output timing rather than stale mission
data or failed XA admission.

COLO's former loading bridge forced register `s0` at `0x80029C0C` even though
disassembly identifies `s0` as the decoded Cross-press condition. That input
also skips the unusually early parachute choreography. The bridge now sets
the adjacent retail loading-ready byte at `0x8011F684` and leaves the pad
neutral; the retail dispatcher accepts and later clears that byte itself.
The 600-update neutral bootstrap remains deterministic, while interactive
confirmation of the complete in-engine opening is pending.

Full dispatcher disassembly makes the replacement exact. `s0` starts at zero
and can become one only through the two processed PAD records and their button
bit tests at `0x80029B1C..0x80029C08`. At `0x80029C0C`, a false `s0` falls
through to `LBU 0x8011F684`; zero takes the async-service path at `0x80029D08`,
while nonzero enters the same accepted loading setup as a button edge. After
that setup and `0x8002B0D0` succeed, retail itself clears the byte at
`0x80029CE8`. The host therefore supplies a real retail state latch rather
than manufacturing input, and the authored cutscene remains independently
skippable by a later genuine Cross sample.

AIRBASEX action `0x70` still owns catalog entry 10 (`6_3.STR`) as an authored
scripted movie. A natural retail Mission 5 success continuation does not
request a terminal movie, so the host's duplicate `ending_movie_path` entry
has been removed. The scripted request/F5/F9/completion bridge remains the
sole owner of that clip. Campaign unit coverage passes after the correction.

The decoded `7_1`, `7_2`, `8_1` and `8_2` contact sheets all depict the two
train chapters. A playtest reports that the direct Mission 7 `8_1` opening is
owned by Mission 6; this remains unresolved rather than being changed from a
filename assumption alone. Retail success tracing proves Mission 6 ends with
`7_2` and Mission 7 ends with `8_2`, but has not yet proved which campaign
boundary owns `8_1`.

The Mission 7 cutoff was then narrowed below the XA admission layer. A fresh
3,000-update combat route received and admitted all 18 XA sectors and the
initial stream ended normally at retail clock 8. PCM output remained nonzero
throughout the later cutoff window. Between host updates 720 and 840, however,
the active SPU mask changed from `0x7DFB` to `0x7CF1`: voices 1, 3 and 8
finished, key-on writes stopped advancing at 266, and mean PCM amplitude fell
from approximately 1,083 to 802. The three voices ended on non-repeating ADPCM
end blocks (flag 1) at `0x25E30`, `0x2E460` and `0x04D50`. This matches the
reported audible timing but does not yet identify those voices as music rather
than ambient/SFX channels; the interactive replay and host callback trace are
the remaining discriminator.

Two callback-lifecycle hypotheses were tested and rejected. Installing the
retail deferred wrapper at `0x800FCE58` without its outer lifecycle produced no
SPU voices, while combining that wrapper with the direct flush was identical
to the established path. An initially suspected sequence tick at `0x800F6574`
was likewise identical, and subsequent disassembly proves why: it is a signed
angle wrapper around the sine table at `0x800F65B0`, not an audio routine. All
three experiments were removed; the runtime keeps the proven `0x80104B40`
sound service. Disassembly shows that `0x800FCE58` consumes a one-shot callback
registered through `0x800FCE28` and conditionally flushes through a separate
global, machinery omitted by the shortened mission handoff and therefore not
a safe substitute.

The actual `0x80104B40` service increments its own libsound tick, services all
24 SPU voices, and then invokes two optional hooks stored at `gp+0x804` and
`gp+0x808`. Diagnostics now expose those hook pointers together with the
deferred callback/flush and re-entry globals at `gp+0x7B8`, `+0x7C0`, `+0x7C4`,
`+0x80C` and `+0x810`. The next Mission 7 trace can therefore distinguish a
missing sequence-hook registration from a registered sequencer which stops
issuing key-ons; no speculative replacement tick remains in product code.

The instrumented Mission 7 run resolves that distinction. Both hooks remain
registered as `0x800FDD98` (the sequence interpreter) and `0x800F928C` (XA
completion service). During the apparent cutoff, the sequence cursor remains
deliberately parked at `0x801EFE3E` while its countdown decreases from `0x1F4`
through `0x104` to `0x14`, with stable step `0x3E8`, tempo `0x1E0` and flags
`0x0C`. It then advances to `0x801EFE5B` and key-on writes resume (266 -> 284
-> 318). PCM remains nonzero throughout, all 18 XA sectors are admitted, and
the exact XA span is LBA 205329--205465, file/channel 1/1. This is the signature
of an authored score rest rather than a dead music service. The remaining gate
is an interactive or emulator A/B at the same Train Race timestamp; removing
the wait without that comparison would alter retail music sequencing.

Fresh post-change gates remain healthy. Mission 1 ran 1,800 neutral updates
with a stable player, radar, PCM, collision and renderer, though visual opening
confirmation is still required. Mission 5's catalog-10 scripted movie request
completed its request/F5-F9/native-handoff/retail-completion probe, while a
natural success continuation selected no terminal host movie. Every Disc 2
route (player Missions 9--21) passed a fresh 1,200-update quick-state matrix
with no collision gaps, renderer repairs, OT/list rejection, or new sound
containment (`build/sf2-validation-20260801-152028/summary.csv`); only LABS2's
previously documented stale-sound containment remains.

SF1 compatibility was rerun after the Alpha 6 diagnostics and lifecycle
changes. USA Rev 1 passes all 29 catalog movies (17,135 video frames), loads
`SUBWAY.OVL` through the retail CD-ROM/DMA3 path, and publishes seven native
GPU boundaries through the continuous guest loop. These probes ran against
the same Release libraries and executable generation as the candidate SF2
build.

The golden Mission 3 replay now records the exact retail predicate inputs at
the late cue-103/cue-181 handoff. `CHANCE_PINNED` event 21 tests progress bit
0 and the negation of player packed-state bit 51. At both cue boundaries the
progress word is `0x00000001`, the tagged player-state pointer resolves to
`0x80190D34`, and its selected word is `0x00880000`, so bit 51 is set and the
retail false branch deliberately selects cue 181. Cue 181 occurs nowhere else
in the Disc 1 mission-script action corpus. This rules out a stale AIRBASE
script table or an accidental cue-number remap; if the branch differs from a
retail emulator run, the defect is the lifecycle of player bit 51.

For local clean-room QA, `sf_tool extract-xa-cue <cue> <cue-index> <wav>` can
now decode one user-owned retail dialogue cue to PCM without launching the
game. It applies the retail `cue / 15` group and `cue % 15` channel mapping,
locates the XA group from EOF-marked clips, and feeds matching sectors through
the same XA decoder as the runtime. The generated files stay under `build/`
and are not committed or packaged. On the test disc cue 140 is a single
0.107-second sector, while cue 181 is a 4.373-second spoken clip. Listening
identifies cue 181 as the generic guard-detection/failure callout familiar
from AIRBASE, but that does not make its HWAY reference stale. A temporary
observation-only write trace found that bit 51 is not inherited from the
direct bootstrap: retail's live player update at `0x800A4C60` mirrors it into
the tagged player bitset at frame 916 through the set path ending at
`0x800A5394`, under the recorded route's actual gameplay conditions. The trace
was removed after identification. Clearing that state would falsify retail
behavior without an emulator-matched route, and cue 181 is not treated as the
user's later APC conversation defect.

The reported Mission 3 conversation belongs to HWAY program 0,
`APC_TUNNEL`, which the 5,500-sample truck replay never reaches. That program
contains speech references 220, 51, 6 and 89. Local extraction confirms cue 6
contains the correct APC conversation: meaningful audio ends at approximately
23.69 seconds, matching the in-engine cinematic, while the synchronized XA
allocation continues with about 31.88 seconds of silent padding to a
55.57-second physical EOF. Retail's later authored event-34 request for dummy
cue 89 is the likely logical stop boundary. A full Mission 3 replay through
the APC scene is required to determine whether that event is late/missing or
the dummy request fails to terminate the active XA stream; physical EOF is not
a valid substitute for that evidence.

Fresh forced-success traces also delimit the disputed train movie ownership.
Mission 6 (`TRAIN`) has retail select movie-catalog entry 12 (`7_2.STR`) at
completion, while Mission 7 (`TRAIN2`) selects entry 14 (`8_2.STR`). The
intervening entry 13 is `8_1.STR`, consistent with its role as the opening
bridge into the next chapter rather than Mission 6's terminal request. A
playtester visually associates its footage with Mission 6, so the definition
is retained but remains gated on an emulator campaign-flow comparison; filename
sequence or visual continuity alone is insufficient reason to delete it.

### Standalone-movie skip teardown crash — 2026-08-01

An attempted full Mission 3 APC replay crashed while skipping its `4_1.STR`
opening, before the guest scene or input recorder had started. Windows Error
Reporting identifies a native access violation (`0xc0000005`) in `ntdll.dll`;
the scene-test log ends after `Standalone movie entered` and contains neither
the skip-completion marker nor any guest-runtime line. A complete standalone
decode of `4_1.STR` passes (512x160, 851 video frames, 1,113 audio chunks), and
all 27 embedded Disc 1 movies decode end-to-end, ruling out malformed retail
movie data.

The skip path explicitly reset its still-live queued OpenAL transport and then
immediately destroyed `MovieAudioPlayer`, whose destructor reset the same
transport a second time. Teardown is now idempotent and the precise skip input
edge is logged before cleanup. A symbolized Debug executable/PDB was also
built, and a process-scoped Windows LocalDumps rule writes any repeat fault to
`build/crashdumps`; those diagnostics are local and are not packaged. The
Release rebuild passes all 21 CTests. Interactive skip confirmation remains a
gate, while the APC recording can proceed by allowing the opening movie to
finish naturally.

### SF2 gameplay-XA archive routing and host-audio recovery — 2026-08-01

The full APC play session identifies a lower-level defect than script cue
selection. Retail enters `XaFilteredStream_Start` with relative sector `0x3D2`
and XA channel 6 for the authored `APC_TUNNEL` cue. The guest CD bridge had one
relative-LBA mapping, permanently mounted on the active mission FOG archive.
Consequently the stream began by reading unrelated FOG sectors; after that
relative address exceeded the FOG extent, reads fell through to absolute disc
sectors and eventually admitted unrelated channel-6 dialogue. This accounts
for both reported symptoms: cue 6 is wrong from its first sound and then walks
through later conversations indefinitely.

`Sf2GuestRuntime` now caches both relative extents from the retail ISO. Mission
file reads remain relative to the active FOG, while entry to retail
`XaFilteredStream_Start` (`0x800F957C`) mounts `SCENES1.XA` or `SCENES2.XA`.
The start routine's sole return restores FOG after a rejected start; the sole
`XaStream_Stop` return (`0x800F9A48`) restores it after the retail CD shutdown
has completed. Checkpoint restore also reasserts the mission mapping before its
loader runs. Quick states record which namespace was active so an F5/F9 inside
speech cannot silently redirect subsequent reads. A synthetic CD regression
now proves a 19 -> 20 -> 19 relative-extent remount sequence returns the three
expected sector payloads.

The newly recorded 41,587-sample APC transcript is preserved for interactive
forensics, but it contains a host F9 discontinuity which the current `SF2PAD1`
format does not encode. Replaying it from a clean boot therefore diverges
before cue 6 and cannot serve as the correctness gate. The product needs one
clean no-F5/F9 run through the APC conversation to prove the fixed stream
begins with cue 6, terminates at the authored boundary, and returns control
without cross-mission audio.

The standalone-movie skip gate now passes automatically: Mission 3 skips
`4_1.STR` at decoded frame 60, starts the guest runtime, publishes 120 frames,
and exits normally. The executable retains an 8 MiB Windows stack reserve and
idempotent movie-audio teardown; the temporary symbol/map linker diagnostics
used to isolate the earlier crash are not part of the candidate.

Host gameplay audio no longer turns a recoverable scheduling stall into a
fatal `Gameplay audio timeline exceeded its FIFO bound` dialog. Continuous
streams now synchronize and reset the OpenAL generation, discard stale PCM,
retain the newest bounded input tail, and restart through the existing fade.
One-shot/movie streams keep the strict overflow error. The callback path also
rebuilds one device-update cushion after starvation instead of consuming each
small producer refill immediately and remaining in a permanent silence/pop
loop at equal producer and consumer rates. Diagnostics expose every bounded
FIFO recovery; an interactive long-audio gate is still required to assess
sync and popping on the real device.

### Launch-time controller attachment — 2026-08-01

The SF2 scene path already merges PsyCross's physical controller buttons and
both analog sticks with the independently sampled keyboard/mouse profile. The
missing-controller report instead originated one layer earlier: PsyCross only
opened devices in response to an SDL hot-plug event. A controller connected
before process startup could appear in SDL's device list without ever being
attached to the `PADRAW` buffer registered later by `PadInitDirect`.

LIBPAD registration now enumerates already-connected SDL GameControllers and
opens the first unclaimed configured device for its pad slot. Instance IDs
prevent one physical controller being attached twice, explicit slot mappings
remain authoritative, and the existing hot-plug path is unchanged. Successful
attachment is logged by slot and SDL name. A product smoke opened an
`Xbox One Elite 2 Controller` in slot 1, skipped Mission 3's opening movie,
entered the guest runtime, and exited normally. Physical mapping and seamless
switching between controller and mouse/keyboard remain an interactive gate.

The first final 42-route attempt passed every 3,000-update quick-state route
and combat Missions 1--4. Mission 5 combat completed its requested updates but
the wrapper returned code 8 because a natural death/restoration occurred at
frame 2,871 and the route ended with zero health before the probe's required
post-checkpoint stability window. It was not a guest fault, renderer/collision
containment, or process crash. The Mission 5 gate must be rerun with enough
post-restore time rather than counted as an Alpha 6 pass.

That extended Mission 5 combat gate passes at 4,000 updates. The same authored
death occurs at frame 2,871, followed by a stable restored room, player,
collision list, renderer, PAD cadence and PCM/SPU timeline for the remainder
of the route. No renderer repair, collision gap, rejected OT/list merge or
sound containment is observed.

The first physical-controller playtest then exposed an independent processed-
PAD duplication. PsyCross correctly delivered Xbox A/B as PSX Cross/Circle,
but the SF2 frontend also copied all physical face bits into the runtime's
derived face-axis fields. Cross therefore meant crouch plus negative vertical
axis (walk backward); Circle meant roll plus positive horizontal axis (turn
right), with equivalent latent conflicts on Square and Triangle. Physical
face buttons now remain lossless in the retail button word while those
pressure-style axes stay neutral; movement and look are owned solely by the
actual analog sticks. The Release rebuild and all 21 CTests pass. Interactive
A/B/X/Y and mixed-device confirmation remains pending.

The follow-up Xbox playtest confirms the corrected controller path behaves
normally, including the formerly compound crouch/back and roll/turn actions.
Controller support is therefore no longer an Alpha 6 blocker.

The strengthened 4,000-update matrix then exposed a deterministic Mission 15
failure shortly after a synthetic combat death entered retail checkpoint
restore. The outer scheduler initially reported a misleading fault near the
resident MSF conversion routine. Preserving the nested CD-ready callback
result showed the real failure: an unaligned return to `0xFFB1EC17` from a
callback stack at `0x807F0000`. That KSEG address mirrors physical PS1 RAM at
`0x001F0000`, exactly where the large GULAG checkpoint transfer writes. The CD
DMA therefore replaced the callback's saved return address with streamed
resource bytes. SF2 callbacks now use the reserved low-RAM stack at
`0x8000B000`, below the executable and its mission data. Mission 15 passes the
same 4,000-update death/reload route, Missions 16--21 pass their extended
combat routes, and all 21 CTests remain green.

### Mission 3 XA EOF remount and Alpha 6 warning — 2026-08-01

A deterministic `probe-sf2-apc-dialogue` gate now resolves and activates the
retail `APC_TUNNEL` program, executes its action-`0x17` cue 6 through the
retail speech stack, executes action `0x6a` stop, and then exercises the
authored cue-89 boundary. It proves cue 6 uses `SCENES1.XA` file/channel
`1/6`, cue 89 uses `1/14`, and the relative CD namespace returns to the
mission FOG afterward.

That gate found a second concrete mount leak: cue 89 is a one-sector EOF
marker and naturally clears the XA stream without entering the explicit
`XaStream_Stop` return hook. The runtime now remembers whether a mounted XA
stream was observed active and restores the mission extent when that stream
naturally clears. Rejected starts and explicit stops keep their existing
return hooks.

This is a lower-level routing/lifecycle gate, not proof that Mission 3's
scripted dialogue is correct. Interactive Alpha 6 testing reports incorrect
voice-line locations throughout Mission 3, including unrelated lines, and
Mission 1's in-engine parachute opening still skips after approximately one
second with an incorrect line. Both defects are now prominent known warnings
in the tester package and remain stabilization work.

### SF2 XA absolute-disc addressing correction — 2026-08-01

Interactive testing of the explicitly warned Alpha 6 dialogue-test package
showed that every Mission 3 speech event was still wrong from its first
syllable. Mission 1's parachute scene advanced only slightly farther before
skipping and also used the wrong voice. Rechecking the retail call chain
identified the shared error: `XaCue_ResolveAndPlay` loads the archive handle's
sector at `+0x84` and passes it directly to `XaFilteredStream_Start`. For Disc
1 that value is `0x3D2` (decimal 978), exactly the physical ISO extent LBA of
`SCENES1.XA`, not an offset relative to that file.

The first routing correction had therefore replaced one double-addressing
error with another. The mission FOG mapping changed retail's absolute 978 to
`FOG base + 978`; mounting the XA extent changed it to `SCENES1.XA base +
978`. Both selected unrelated sectors. XA ownership now disables the
mission-relative namespace and exposes ordinary absolute-disc addressing.
Rejected starts, natural EOF, explicit `XaStream_Stop`, checkpoint restore and
quick-state restore still return to the mission FOG namespace at their
respective lifecycle boundaries.

This correction changes deterministic behavior in both affected missions.
Mission 1 now starts its authored first speech cue at absolute sector 978,
ends it naturally, and proceeds through the later script event in a 2,400-host-
update run instead of aborting the opening. The strengthened
`probe-sf2-apc-dialogue` no longer force-stops cue 6: it enters retail file/
channel `1/6`, waits through the padded physical allocation, observes retail's
own `SceneSpeech_Stop` and `XaStream_Stop`, and verifies restoration of the FOG
namespace. The Release build and all 21 CTests pass. Interactive playtesting
then confirmed Mission 1's complete parachute scene, correct opening dialogue
and gameplay handoff, followed by Mission 3's correct dialogue selection and
natural handoff without cross-mission continuation. Those observations close
the wrong-sector dialogue failure.

The same Mission 3 playtest exposed a separate restart-only audio fault. After
a retail death/checkpoint reload, current conversations remained event-aligned
but gunshots and other SPU effects played roughly two to three seconds late.
The host sink was reset when `checkpoint_restores` advanced, but the guest kept
exporting PCM while `retail_restore_active_` deliberately suspended retail's
sound-service callback during the synchronous loader. That loader-era mixture
therefore refilled the empty host FIFO ahead of resumed gameplay. `takePcm()`
now clears and withholds PCM for the duration of the retail restore, making the
checkpoint a true audio-generation boundary; fresh PCM becomes visible only
after stable app-state-0 submissions restore the sound callback. Interactive
Mission 3 testing then confirmed synchronized effects after death and retail
checkpoint reload, closing the restart-only delay.

Mission 7 (`TRAIN2`) was subsequently completed interactively in the corrected
build. Music remained present throughout; effects stayed synchronized and the
mission completed normally. The 180-second host log recorded zero underruns,
zero callback starvation, zero FIFO recovery and no source reset before the
ordinary completion handoff. Every received XA sector was admitted and the
retail sequence cursor continued advancing. The earlier reported cutoff is no
longer reproducible and no speculative sequencer change is warranted.

The final compatibility pass also keeps the original SF1 runtime intact. USA
Rev 1 decodes all 29 catalogued movies (17,135 video frames), loads SUBWAY.OVL
through the CD-ROM/DMA3 path, and advances seven native GPU boundaries through
the continuous guest loop. A dedicated connected-campaign regression now locks
the train chapter to `7_2.STR` (Mission 6 ending), followed by `8_1.STR`
(Mission 7 opening) and `8_2.STR` (Mission 7 ending). The source/documentation
diff remains free of local research paths, contributor names and external
binary-material references.

The fresh post-fix Release matrix passes all 42 3,000-update routes: quick-state
and combat/restart coverage for every player mission on both discs. Mission 21
crosses one quick-state restore and two combat restores; the restore-heavy
Mission 8 route crosses nine without renderer, collision or runtime failure.
The dedicated Mission 2 checkpoint-audio route performs one retail restore and
discards 69,724 loader-era PCM frames, proving that the new synchronization
boundary is exercised rather than merely present in source.

Strict movie decoding was repeated against both sequel discs. Disc 1 passes
all 27 embedded streams with 23,239 video frames and 58,859,136 stereo sample
frames. Disc 2 passes all 27 with 25,113 video frames and 63,550,368 stereo
sample frames, including `21_2.STR` and the post-campaign `Z17_1.STR`. This
closes the decoder/range gate for the Mission 9--21 half independently of the
campaign selection tests.

Current Release success-shell probes also pass independently for all 13 Disc 2
player missions (Missions 9--21). Each route executes its corrected retail
runtime/archive selection, forces success through the guest shell, consumes the
authored terminal movie request where one exists, extracts and reapplies only
valid adjacent carry, and advances to the next player mission. Mission 21
selects `21_2.STR`, reaches final campaign completion and does not manufacture a
twenty-second mission.

### 2026-08-01: Mission 5 interactive regression and Alpha 7 disclosure

An interactive connected Mission 5 run invalidated the earlier inference that
the deterministic scripted-movie handoff was sufficient coverage. Guards were
absent from both the opening and silenced-rifle areas, Homan and the truck did
not move, and later UI flickering escalated into full-screen flickering. The
process did not hit a native fault and exited normally through the pause menu;
the expected catalog-10 `6_3.STR` request was never reached. Alpha 7 therefore
publishes this as a prominent known issue while the authored script/actor
initialization path is investigated.

The first investigation pass found a concrete shared bootstrap error. By the
first host presentation boundary, AIRBASEX already has all 16 script programs
registered, six programs active, and `LEVEL` active. Retail has therefore
completed its own `MissionScript_ResetAndStartLevel` call. The host nevertheless
called that routine again and then redundantly called `MissionScript_Activate`
for `LEVEL`; the second reset could erase activation-time, one-shot authored
state.
The runtime now accepts retail's live `LEVEL` and retains the reset routine only
as a fallback when the registry exists without an active level. The fallback no
longer adds a second explicit activation because the retail reset routine owns
that operation internally.

Diagnostics now record registry size, active-program count, `LEVEL` ownership
at the first host check, reset/start count, and actor residency. The corrected
Mission 5 route reports all 16 programs, six programs already active, exactly
one retail reset/start, and all 41 authored actor instances resident with none
marked dormant. A strengthened 3,000-update matrix passes all 42 quick-state and
combat/restart routes; every mission reports exactly one retail `LEVEL` start,
with no collision gap, renderer containment, rejected OT/list merge, or new
sound containment.

Interactive retesting after that correction reproduced the same AIRBASEX
failure: no opening guards, Homan and the truck remained stationary, and the
silenced-rifle area was empty. The duplicate reset was therefore a real
bootstrap defect but not this mission's root cause. A deterministic opening
trace now records the authentic `LEVEL_START` handoff (event 5, selector 8) at
guest frame 1,492 and reaches gameplay naturally. The next gate is a recorded
interactive route that identifies which authored `FIRST_ZONE` actor/event
stops advancing after that handoff; actor allocation alone is no longer
accepted as evidence that the encounter is active.

The bootstrap confirmation was then tested as an independent variable. With
no loading confirmation, AIRBASEX never reaches its authored opening. Using
the retail loading-ready latch instead of the existing one-poll Cross pulse
does reach the opening, but produces the same actor/controller state and the
same dispatch history through gameplay. The confirmation mechanism is
therefore required but is not the missing-choreography cause.

A bounded dispatch ledger now retains the last 128 nonzero retail mission
events. The neutral opening consistently records the initial program/object
setup and then the authentic `LEVEL_START` event `5/8`; it does not record
`FIRST_ZONE` event `5/0x1B` or event `3/0x35`. Static `.SS` decoding shows the
expected chain: `FIRST_ZONE` activation places source 123 in state 6;
`5/0x1B` hands movement to source 122; and `3/0x35` enables source 165 and the
near-truck encounter. A synthetic navigation attempt cannot reach the real
source-35 crossing without colliding with the authored courtyard route, so a
short recorded interactive traversal remains the authoritative next trace.

### 2026-08-02: AIRBASEX table-5 packet containment

The rendering-instability trace found a second packet producer that the
existing auxiliary-table containment did not cover. Renderer `0x80120CC0`,
build table 5, entered `0x80013040` with the shared packet cursor at
`0x80166F54`; its first draw-mode store at `0x80013224` replaced the live
AIRBASEX `INIT.OVL` callback word at `0x80166F58` (`0x24044000`) with GP0 word
`0xE1000200`. The older hook at `0x80013354` ran after this leading list and
therefore could not prevent the overwrite.

A trial reset of copied tables 2, 3, and 5 at the renderer entry kept the word
at `0x24044000`; instrumented high-water measurements found table 5 used 320
bytes and the largest auxiliary invocation ended at `0x80152A28`. All 21
quick-state and combat routes passed, but interactive testing rejected the
change: it did not restore AIRBASEX guards or vehicle choreography and caused
constant UI flicker plus full-screen cutscene flicker across other missions.
The trial was reverted before commit. `0x80166F58` is therefore not accepted as
a live-code invariant merely because extracted `INIT.OVL` contains an
instruction there; the mission reuses that address during normal rendering.
AIRBASEX activation remains unresolved and requires an interactive replay or
an equivalent trace of the authentic player route.

### 2026-08-02: AIRBASEX difficulty-state correction

The one-byte AIRBASEX boot-flow reconstruction above was misidentified.
Predicate `0x1E` does read `0x8011F638`, but recovered retail mission source
confirms that this predicate is `hard`, not a generic New Game latch. COLO's
authored `if not hard`/`if hard` countdown pair compiles through the same
predicate. The direct runtime's first-level-start write of `1` therefore put
every mission into Hard difficulty; the retail HUD independently reported
`Playing on HARD difficulty`, and interactive comparison confirmed that Hard
is specifically why Falkan runs rather than walks to the helicopter.

The forced write has been removed. Direct construction now retains TITLE's
Normal value `0`, and the AIRBASEX gate asserts that the retail predicate is
read as zero. The earlier value-1 actor-pair result was a valid observation of
the Hard branch, but not proof of correct New Game initialization. It masked
the still-unverified Normal branch and must not be used as the Mission 5
startup invariant.

At Normal difficulty the deterministic AIRBASEX opening still constructs live
sources 122 and 123 and consumes the difficulty predicate as zero. Interactive
validation must confirm the complete Normal choreography—both talking guards,
Homan/truck departure, the silenced-rifle patrol pair, and Falkan's final walk.
The source-150 controller trace remains useful for measuring the terminal
route, but no actor-speed substitution is required.

The rejected table-5 renderer trial remains absent. All 21 configured CTests
pass. The complete full-strength matrix passes all 42 quick-state and combat
routes at 3,000 updates, including ten/eleven checkpoint restores in Mission
8 and one/two in Mission 21. No route enters renderer or collision
containment, and no mission introduces unexpected sound containment.
Interactive Mission 5 validation remains required before this change can be
called fixed: the opening guards, Homan/truck departure, silenced-rifle
guards, and stable cross-mission presentation must all be confirmed together.

The post-gate SF1 compatibility pass remains unchanged against the same
Release libraries: USA Rev 1 decodes all 29 catalogued movies (17,135 video
frames), loads `SUBWAY.OVL` through retail CD-ROM/DMA3, and publishes seven
native GPU boundaries through the continuous guest loop.

### AIRBASEX nested NPC-resource lifecycle — 2026-08-02

The remaining Alpha 7 Mission 5 failure was reproduced without a presentation
fault: opening guards and the silenced-rifle patrols were absent, while Homan
and the truck remained stationary. AIRBASEX uniquely nests its character
resources as `AIRBASEX.FOG:NPC.HOG:{MORGAN.HOG,BUDDY.HOG}`. Retail action
`0x79` schedules those archive reads through `0x800A3F90` and immediately
drains them through `0x800A3ED0`. The runtime's synchronous-file drain was an
HLE no-op because the guest spin cannot advance host CD hardware; consequently
the destination buffers remained zero and retail's `0x800AFD6C` completion
callback never bound the nested models to their actors.

Mission package construction now indexes authored nested HOG payloads. The
`0x800A3F90` bridge copies an exact requested payload into guest RAM, enters
the retail callback with its authored magic argument, and restores the
original guest return when that callback completes. The generic FOG file-open
bridge remains top-level-only. This is synchronous host delivery of retail
data and continuation, not a native actor or mission-script substitute.

Source 123 (BUDDY/Homan) now changes from a zero model and `BADBAD00`
placeholder to model `0x80124C14`, resource `0x8018C820`, positive authored
floor bounds, zero vertical velocity and stable collision response. A product
gate fails if that model/bounds lifecycle regresses. Interactive validation
then confirmed both opening guards, the driver joining Homan and driving away,
and both silenced-rifle patrols. UI, gameplay and cutscenes remained free of
the prior flicker. Detection still failed to trigger the expected mission
failure, and hostile fire was observed without effects audio or damage; those
are separate follow-up defects.

The post-fix Release suite passes 21/21. The full 42-route matrix passes all
21 missions at 3,000 updates in both quick-state and combat modes, including
Mission 5's new nested-resource assertion, without renderer/collision/OT/list
containment regressions. A separate Mission 1 playtest reported a late stop at
guest `0x800F7778` after detonating the C4 and entering the cleared tunnel;
that reproducible gameplay segment is the next stability target.

### Mission 1 post-C4 RawCdSync scheduling — 2026-08-02

The reported Mission 1 stop after detonating the C4 was not an invalid guest
instruction. Retail stopped inside blocking `RawCdSync` at `0x800F7704`; the
captured PC `0x800F7778` is its completion/timeout polling loop. The product
normally advances suppressed PS1 hardware from GPU publication boundaries,
but this loop does not submit another ordering table. Its CD callback and
retrace timeout could therefore never change, and the enclosing 100-million
instruction budget eventually expired while executing valid retail code.

The runtime now services hardware only for instruction-budget slices proven
to remain inside `RawCdSync`, including its two direct calls to retail `VSync`.
Normal gameplay cadence remains display-driven. A dedicated gate constructs
the retail pending-result/timestamp state and enters the real blocking
routine. It now crosses 763 scheduler slices, advances beyond the 60-retrace
timeout and returns through the guest boundary; without the new scheduling
edge it cannot return. The gate is diagnostic-only and discards its runtime
afterward.

Fresh 5,000-update combat and exact quick-state routes pass for both COLO and
AIRBASEX. The full 42-route, 3,000-update matrix also passes every mission in
both modes, all 21 forced connected-completion routes reach their authored
TITLE/campaign handoff, and all 21 configured CTests remain green. The Mission
5 completion gate now distinguishes that successful mission-heap teardown from
a missing BUDDY resource. An interactive traversal through COLO's destroyed
tunnel is still required before declaring the exact reported playtest fixed.

The AIRBASEX report that hostile gunfire caused no damage was also contaminated
by a recording diagnostic left in the user's PowerShell environment:
`SF2_PIN_HEALTH=1`. Health pinning is now honored only when a recording or
replay path is active, preventing a later ordinary playtest from silently
entering diagnostic god mode. AIRBASEX gunshot effects audio and its authored
detection/failure behavior still require a clean interactive retest.

### AIRBASEX combat-observation gate — 2026-08-02

The apparent detection/failure defect was narrowed against the decoded retail
script rather than inferred from the playtest. `INTRUDER_ALERT` owns event-5
handlers for selectors 122, 81, 152, and 86. Each handler activates program 0,
`DAMNIT`; that program randomizes a branch and requests one of dialogue cues
321, 271, 345, 322, or 323. None of those event paths invokes a mission-failure
action. Detection in this area is therefore expected to raise the authored
alert dialogue, not necessarily fail the mission immediately.

The product log now reports combat state whenever retail health, armor, actor
damage dispatch, or player-damage dispatch changes. It includes the retail
clock, both damage counters, their callers, and the first four player-damage
arguments. An ordinary run that inherits `SF2_PIN_HEALTH` also emits an
explicit message that the pin was ignored; the pin remains available only for
input recording/replay. This turns the next interactive AIRBASEX check into a
direct distinction between no attack, a damage request that is rejected, and
damage that reaches the retail health record.

The post-instrumentation Release build succeeds, all 21 configured CTests pass,
the exact RawCdSync scheduling gate still crosses 763 hardware-service slices,
and a 5,000-update AIRBASEX combat/restart route retains live SPU/XA audio and
reaches retail death/checkpoint restoration. Its synthetic input route is not
accepted as proof of the opening guards' effects audio; that remains an
interactive confirmation item.

### COLO completion confirmation and Mission 1-to-2 carry ownership — 2026-08-02

Interactive testing confirms the RawCdSync correction: Mission 1 now traverses
the destroyed C4 tunnel, completes end to end, writes slot 3, and plays Mission
2's opening FMV. The subsequent briefing failed before gameplay with `Campaign
carry could not be applied to retail RAM`.

The failure was a duplicate ownership bug in the native mission-loading shell.
`PsyCrossMissionStart` constructs a `GameplaySession` asynchronously so texture
and room residency are ready behind the briefing. For SF2 that object is only a
native residency shell; the authoritative inventory, vitals, and checkpoint
live in the separately constructed `Sf2GuestMissionRuntime`. The loader applied
the exact SF2 carry to both objects. Mission 2's disposable legacy bridge had
not constructed its player inventory at that boundary, so the unnecessary
first application rejected the valid state before the real guest runtime was
reached.

SF2 carry is now deferred through the briefing and applied once, after the
authoritative guest mission bootstrap. SF1 retains its existing preload-time
carry application. The Release build and all 21 configured CTests pass, and the
forced Mission 1 completion flow still verifies exact sequel carry in Mission
2. Slot 3 provides a short interactive regression path for the actual
briefing-to-gameplay handoff.

The same playtest reached AIRBASEX's final Falkan/helicopter objective. Falkan
ran to the helicopter instead of walking, leaving too little time to reach and
tase him. A new generic script-event probe exposes an optional actor source's
runtime controller mode, position, and velocity. Dispatching AIRBASEX
`TRAILING_MIBS` event 34/selector 67 through retail assigns source 150's
authored movement byte to 7; no native movement substitution has been added.
Interactive retail comparison subsequently identified the apparent pacing bug
as authored Hard-difficulty behavior. The direct bootstrap had incorrectly
forced the shared `hard` predicate to one. Restoring Normal difficulty is the
correct fix; the trace remains as a regression observer for the final route.

### H11 non-zoom optic — 2026-08-02

Interactive testing confirmed that the silenced sniper rifle's digital scope
renders correctly while the H11 optic is absent. Deterministic inventory/aim
probes show that these weapons intentionally use different retail presentation
paths: item 14 constructs the sprite-heavy digital scope, while item 13 enters
the H11 controller at `0x8004F7E8` and emits a small line-based non-zoom optic.
The H11 path reaches the product presentation as four-word Gouraud line GPU
commands (`GP0 0x50/0x52`), but the SF2 renderer only translated flat lines and
fell through to PsyCross for Gouraud lines.

The product renderer now rebuilds `LINE_G2` primitives explicitly, matching the
already-working legacy guest renderer. The scope probe reports line counts per
submission root so the H11 and sniper paths remain distinguishable. Both
Release targets build and all 21 configured CTests pass. Interactive
confirmation of the H11's simple non-zoom reticle remains pending; the working
silenced-sniper scope must remain unchanged.

### SF2 PC camera ownership and campaign loadout — 2026-08-02

Interactive confirmation closed two adjacent gameplay defects: H11 can now be
selected away from and back to with the wheel/number bindings, and Mission 2
starts Lian with her authored low health and empty inventory rather than
Gabe's Mission 1 arsenal. SF2 save data remains intact, but the native shell no
longer injects the preceding character's live loadout into a newly bootstrapped
retail mission.

The first mouse-camera experiment did not survive retail ownership. Writing
the lower-body transform and aim-controller seed before RMB transition left
manual aim facing Gabe's legs, and writing inferred chase pitch fields at the
entry of `0x80049940` produced no vertical motion. Increasing mouse-to-stick
gain also only reached the original 20 Hz analog acceleration ceiling sooner;
it did not provide native relative mouse aim.

Instruction alignment with SF1 located the exact corresponding SF2 boundary:
SF1 `0x80037B08` maps to SF2 `0x80053464`, and the completed sight angles are
consumed at `0x800539D0`. The PC path now accumulates relative mouse deltas
directly into the pitch/yaw angles at that latter boundary while physical
controllers retain the untouched retail analog path. Lock-on-to-RMB defers its
one-shot target bearing to the same boundary so transition initialization
cannot overwrite it. A deterministic forced-pitch probe proved that the
initially inferred `0x80049940` camera path is not reached in ordinary player
gameplay (zero hook calls across 400 updates). Chase pitch therefore runs at
SF2 `0x80053464`, the instruction-exact counterpart of SF1's proven
`0x80037B08` common camera/facing hook. The same probe records 132 live calls,
resolves camera base `0x801285DC`, and drives both desired/rendered pitch to the
authored clamp of 512. Default native angle gains are 4.0 horizontal and 2.5
vertical and remain overridable by the existing sensitivity switches.

The Release product and tool targets build, all 21 configured CTests pass, and
the 1,200-frame H11 scope route remains stable. Interactive confirmation of
direct mouse direction, pitch range, chase pitch, and target-facing entry is
the remaining gate.

### Native mouse camera and widescreen projection — 2026-08-02

Interactive testing closed the remaining native mouse-camera gate. Relative
mouse input now drives both axes in first-person aiming and third-person chase
view without changing the retail controller path. Aim and chase yaw/pitch
sensitivity are independently configurable from the command line, and weapon
selection remains functional after the camera changes. Missions 1 and 2 also
retain their authored character-specific inventories across campaign flow.

The original widescreen presentation widened the host viewport after retail
had already projected and culled the scene for a 4:3 camera. That exposed
empty SCRIM/background strips at the sides and made world meshes and complete
NPC models disappear while still visibly inside the wide viewport. The SF2
guest GTE now applies a Q16 horizontal projection scale derived from the host
aspect ratio while preserving retail Y projection and all non-projection GTE
math. The first gameplay ordering table is compensated for PsyCross's generic
centered-4:3 transform; subsequent HUD, radar, and text tables remain centered
at their authored size.

A focused GTE regression proves the horizontal scale cannot alter projected Y
or depth results. Interactive play through Missions 1-4 confirms the empty
widescreen edges, premature NPC culling, and reported vertex/mesh explosions
are gone. Missions 5-21 remain to be checked against this presentation change.

One Mission 1 restart defect remains open for the public build. If Gabe dies by
falling from the playable parachute opening, retail checkpoint restart does not
replay the complete in-engine opening state. Gabe releases the parachute at an
otherwise inaccessible level boundary and may die from the resulting fall.
The correct restart must replay the in-engine parachute intro and return
control while Gabe is still parachuting. This is documented for Alpha 9 rather
than addressed with an unverified checkpoint-state override.

### Briefing order, scripted camera ownership and Alpha 10 gate — 2026-08-02

SF2's mission briefing records store their strings in forward presentation
order rather than SF1's paired title/directive layout. The shared parser now
recognizes that record shape and exposes the authored location, mission title,
date/time, operative and two directive paragraphs. The retail briefing bridge
uses those fields directly, restoring the complete text layout seen on the
original game. A focused DLF regression protects the sequel ordering.

Interactive testing then exposed native chase pitch leaking into in-engine
cinematics: the scripted camera looked downward until mouse movement recentered
it. The active camera wrapper identifies its owner through the actor pointer at
offset `0xDC`. During Mission 3's cinematic that owner is the scripted actor;
after handoff it becomes the player. Native chase pitch now runs only under
player ownership, clears its pending delta and latch otherwise, and resumes
without a jump after retail returns control. Deterministic probes cover both
the gated cinematic and accepted gameplay states, and the user confirmed the
authored cinematic angle and normal post-handoff vertical mouse look.

The default third-person mouse yaw sensitivity is now 0.75 following the
Missions 1-7 interactive pass. First-person and chase yaw/pitch remain
independently overridable. The remaining presentation defects are non-blocking
for Alpha 10: the mission-failure matte animates at 4:3 before reaching its
full-width settled state, the decorative briefing frame is absent, and Mission
6 NVG can leave a right-edge strip. These are recorded rather than hidden by
unverified special cases.
